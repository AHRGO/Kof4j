name: kof-quality-bot

on:
  push:
    branches: [main, beta-*]
  pull_request:
    branches: [main, beta-*]
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"  # Weekly Monday 6am

permissions:
  contents: read

jobs:
  code-quality:
    name: Code Quality Analysis
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write # upload do SARIF
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Set up JDK 25
        uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3 # v4
        with:
          distribution: temurin
          java-version: '25'

      - name: CodeQL Code Quality
        uses: github/codeql-action/init@3ea06614dafe36dec890db3446326e0d40ce53d4 # v3
        with:
          languages: java
          # config canônico = security-and-quality + paths-ignore (src/test).
          # SEM o config o scan re-gerava os fantasmas dismissados dos testes
          # (20 alerts 15/09). queries: vem do config, nao duplicar aqui.
          config-file: .github/codeql/kof4j-config.yml

      - name: Autobuild
        uses: github/codeql-action/autobuild@3ea06614dafe36dec890db3446326e0d40ce53d4 # v3

      - name: Perform CodeQL Analysis
        # #563/§386: paths-ignore no config NAO suprime suite importada por
        # `- uses:` (medido 20/09) — filtro real e o pos-SARIF abaixo.
        uses: github/codeql-action/analyze@3ea06614dafe36dec890db3446326e0d40ce53d4 # v3
        with:
          category: "/quality"
          upload: false
          output: ${{ runner.temp }}/codeql-sarif-quality

      - name: Filter src/test out of SARIF (#563)
        run: |
          python3 scripts/codeql-sarif-filter.py --selftest
          python3 scripts/codeql-sarif-filter.py "${{ runner.temp }}/codeql-sarif-quality"

      - name: Upload filtered SARIF
        uses: github/codeql-action/upload-sarif@3ea06614dafe36dec890db3446326e0d40ce53d4 # v3
        with:
          sarif_file: ${{ runner.temp }}/codeql-sarif-quality
          category: "/quality"

  security-gate:
    name: CodeQL Gate (alerts API)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: read # codeql-gate.sh le alerts/analyses
      pull-requests: write # comentario do gate no PR
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      # Automation da frente dev: o bot PERGUNTA ao gate (mesmo
      # scripts/codeql-gate.sh do pre-push) quais alertas estao abertos na
      # branch, em vez de so subir sarif. rc=2 (API fora) = warning, nao fail.
      - name: Run codeql-gate --fast
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set +e
          bash scripts/codeql-gate.sh --fast | tee gate.log
          rc=${PIPESTATUS[0]}
          echo "gate_rc=$rc" >> "$GITHUB_ENV"
          # Contrato do gate (cabecalho do script + comentario acima):
          # rc=1 = RED de verdade (falha o job); rc=2 = INCONCLUSIVO
          # (analise atrasada/API fora) = warning, NUNCA fail — senao o
          # bot fica vermelho em todo push enquanto o CodeQL do tip ainda
          # esta rodando (corrida gate-vs-scan, medido 20/09 no tip
          # `c7a09fb9`). rc=0 = green.
          case "$rc" in
            0) exit 0 ;;
            2) echo "::warning::codeql-gate INCONCLUSIVO (analise atrasada ou API fora) — nao bloqueia"; exit 0 ;;
            *) exit "$rc" ;;
          esac

      - name: Publish gate result (job summary)
        if: always()
        run: |
          {
            echo "## 🛡️ CodeQL gate (Security and quality)"
            echo ""
            case "$gate_rc" in
              0) echo "**Status: GREEN** — 0 alerts open nas branches ativas." ;;
              1) echo "**Status: RED** — alertas open (fix na raiz ou dismiss com evidencia <=280c):**" ;;
              *) echo "**Status: NAO-AVALIADO** (API fora — o scan do CI e a porta real).**" ;;
            esac
            echo ""
            echo '```'
            cat gate.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Comment on PR
        if: always() && github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          icon=":white_check_mark:"; [ "$gate_rc" = "1" ] && icon=":x:"; [ "$gate_rc" = "2" ] && icon=":warning:"
          body="$icon **CodeQL gate** (\`scripts/codeql-gate.sh\`) — rc=$gate_rc\n\n\`\`\`text\n$(cat gate.log)\n\`\`\`"
          existing=$(gh api "repos/$GITHUB_REPOSITORY/issues/$PR_NUMBER/comments" --paginate \
            --jq '.[] | select(.body | startswith("'"$icon"' **CodeQL gate**")) | .id' | head -1) || existing=""
          # PR de FORK: o GITHUB_TOKEN e read-only NESSA execucao (qualquer
          # bloco permissions e ilusorio p/ pull_request de fork) — o POST/PATCH
          # responde 403 e nao pode tornar o gate vermelho: o veredito ja vive
          # no step-summary e no rc do passo anterior. O comment e best-effort.
          if [ -n "$existing" ]; then
            gh api -X PATCH "repos/$GITHUB_REPOSITORY/issues/comments/$existing" -f body="$(printf '%b' "$body")" \
              || echo "::notice::gate comment skipped (403: fork PRs have read-only token; see job summary)"
          else
            gh api -X POST "repos/$GITHUB_REPOSITORY/issues/$PR_NUMBER/comments" -f body="$(printf '%b' "$body")" \
              || echo "::notice::gate comment skipped (403: fork PRs have read-only token; see job summary)"
          fi
