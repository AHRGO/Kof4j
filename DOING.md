[English](DOING.md) | [Português](DOING.pt_BR.md)

# DOING.md — coordenação multi-agente (quem faz o quê)

> **Regra obrigatória para agentes (IA ou humano):**
> 1. **Antes** de começar qualquer trabalho de feature/gap: leia este arquivo.
> 2. Se o item que você quer atacar já tem **dono + estado `EM CURSO`**, não toque —
>    escolha outro ou pergunte. Nunca dois agentes no mesmo gap.
> 3. Ao **reivindicar** um item: edite este arquivo **no mesmo commit** que começa
>    o trabalho (dono, branch, arquivos que vai tocar).
> 4. A **cada commit**, atualize sua linha (estado, progresso, o que falta).
> 5. Ao **concluir**: mude para `FEITO` com data + commit + teste que prova, e
>    marque o gap no docs (`status.md`/`backend-parity.md`).
> 6. Itens abertos ficam em `EM CURSO` por no máx. uma sessão; ao abandonar,
>    volte para `ABERTO` com nota do que já funciona e o que falta.
> 7. **Modo autônomo:** se o turno vai acabar, a ÚLTIMA coisa escrita aqui é
>    a linha **"PRÓXIMO PASSO"** abaixo (tarefa exata + arquivo + prova).
> 8. **Nunca descartar trabalho de outro agente (13/09, diretriz da
>    mantenedora):** os agentes trabalham **em conjunto, não um contra o
>    outro**. Working tree sujo de outro agente = trabalho dele: **commita
>    tudo** (valide antes: sintaxe + run/compile da área), nunca `checkout --`
>    nem `stash drop`. Conflito de rebase = resolve preservando os dois lados
>    (fatos idênticos → escolhe uma redação; fatos diferentes → mantém ambos).
>    Reversão de commit alheio só com causa raiz provada + registro no DOING.
> 9. **Identificação por IP local (13/09, diretriz da mantenedora):** cada
>    agente se identifica pelo **IPv4 local da máquina** (`hostname -I`).
>    Toda reivindicação `EM CURSO`/`FEITO` leva `dono = <IPv4>` (ex. `dono =
>    192.168.100.22` = `mel-optiplex`). **O cluster compartilha o storage**
>    (mesmo repo/branch): cada IPv4 = **um agente = uma máquina**; nunca
>    assumir o trabalho/dono de outro IP, e a linha `Esta sessão = ...` vale
>    só para quem a escreveu. Esta sessão = **192.168.100.15**
>    (lane **bugs-and-gaps** — manter `docs/bugs-and-gaps/` (known-bugs,
>    conformance-matrix, ecosystem-coverage, specification-gaps, KOFUI-AUDIT)
>    sincronizados com o código/suíte; §106 fechado pela lane .18/9094; §89
>    evidência corrigida 13/09; contagem da suíte sync 13/09). Quem voltar
>    (humano/cron/outra instância) retoma em ≤1 leitura.

Estados: `ABERTO` · `EM CURSO` · `FEITO` · `BLOQUEADO`.
| **STABILITY (lane docs→plataforma)** | `RECUSADA` | sessão 18/09-2 (9093) | `beta-0.4.0` | `DOING.md`, `DOING.pt_BR.md` | 18/09 ~23:1x: re-trigger recusado com varredura completa — nenhum órfão novo (ledger `check_known_bugs_status.sh`: 18 abertas EN×PT idênticas, UNKNOWN=0, todas com dono vivo: .17/.18/.22/nat/mantenedora; cluster-A aguarda design rule 6), nenhuma regressão (tip `8e5fb3f6` inalterado, suítes medidas verdes na última hora), nenhuma pendência nova de doc (as 4 do PRÓXIMO PASSO da issue-lane já resolvidas pelas próprias lanes: §279 FIXED, #275→`f738fab1`, workflow-plan→`acc1e406`, ledger rc=0). Filas X8/X9/X10 desta lane FECHADAS; frentes restantes = rule 6 (mantenedora) ou lanes vivas. Heartbeat da lane parado (`auto-loop.sh stop`). Re-ativar SOMENTE com: regressão no tip, órfão novo, dono morto (≥1 dia sem commit na lane) ou decisão da mantenedora (D-VALUE-RECORD front, `--publish`, rename cross-file, API kof.test). |
| **X10 fatia 7 — hover de símbolos do projeto (docs→platform lane)** | `FEITO` | sessão 18/09-2 (9093) | `beta-0.4.0` | `LspServer.java`, `LspProject.java`, `LspServerTest.java` | 18/09: fallback do hover — `LspProject.declarationLine` (buffer → irmãos `.kf`, linha completa + arquivo, `null` honesto). **Bug real exposto pela fatia (corrigido junto):** o parser de frames do teste-mate fatiava por CHAR um Content-Length que é BYTES UTF-8 — qualquer payload não-ASCII (o em-dash do hover) quebrava o parse; helper refeito byte-safe (ISO-8859-1 1char=1byte). **Prova:** `hoverShowsCrossFileDeclaration` (linha+origem; `mystery`→null); `LspServerTest` 30/30, kof-cli 336/0F, compiler 2294/0F (17E=node), check_500 rc=0 (LspServer 585). SHA `848b7df1`. **X10 FECHADA (fatias 1–7).** Próximas questões da fila desta lane: drift-sweep dos landings alheios de hoje (§239 JS String.format `05e99dc3`, FFI 3.4/3.6 deltas (2-4), §298/§299 deploy) conferindo learn/training/parity vs código — tarefa de doc, sem front novo. |
| **X10 fatia 6 — `workspace/symbol` no LSP cobre o projeto (docs→platform lane)** | `FEITO` | sessão 18/09-2 (9093) | `beta-0.4.0` | `LspServer.java`, `LspProject.java`, `LspServerTest.java` | 18/09: capacidade `workspaceSymbolProvider`; `LspProject.workspaceSymbols` une buffers abertos (fonte da verdade) + `.kf` irmãos não-abertos (dedupe por path absoluto, walker fatias 4–5); filtro substring case-insensitive; ordenação prefixo→substring→nome→uri. Lógica no LspProject (LspServer 578 < 600). **Prova:** `workspaceSymbolSpansProject` (3 símbolos/2 uris + filtro 'BOx' →Box); `LspServerTest` 29/29, kof-cli 335/0F, compiler 2294/0F (17E=node), check_500 rc=0. SHA `c04e16a4`. Nota de processo (Q0): o push `baa6d554` saiu PARCIAL (assert do tracker falhou no meio do script) — complemento neste commit; nada escondido. **DRIFT-SWEEP FEITO (18/09 → tip):** único drift real = `docs/tooling/LSP.md`(+PT), sincronizado com X10 em `6714f25e` (tabela de 9 capabilities + limitações honestas); §239/FFI(2)-(4)/§298/§299/`PLAN-EDITOR-INTEGRATION`/`fake-idioms`/`strings.md` conferidos vs código — sem divergência. **PRÓXIMO PASSO (re-trigger):** re-varredura barata na ordem: (1) `git fetch` + reler DOING/known-bugs — órfão novo (ou dono morto: sem commit na lane ≥1 dia → reivindicar) passa na frente; (2) suite/CI do tip vermelho? → reproduzir→causa→provar→commitar (Q0); (3) sem (1) nem (2) e sem ítem sem dono no escopo desta lane (as filas X8/X9/X10 estão FECHADAS ou em pergunta de superfície rule 6) → **RECUSAR o re-trigger** pela regra de STABILITY (não inventar trabalho; registrar a recusa + `scripts/auto-loop.sh stop`). A estabilidade GLOBAL ainda não vale (§278/§280/§295b/§302/§304 abertos com dono vivo; `native-multiarch`/`planning-otp` pendentes em development/) — mas o trabalho restante não é desta lane até que um dono morra. Lição desta sessão: checar o tip ANTES de consertar gate alheio (§303/CmdBuild já tinha split landado) e NUNCA confiar anchor de script multi-edit (commit parcial baa6d554 → complemento 13ac1ce8). |
| **X10 fatia 5 — referências cruzam arquivos do projeto (docs→platform lane)** | `FEITO` | sessão 18/09-2 (9093) | `beta-0.4.0` | `LspServer.java`, `LspProject.java` (novo), `LspServerTest.java` | 18/09: `references()` varre irmãos `.kf` além do buffer (walker compartilhado da fatia 4; read-only). Split obrigatório no caminho: LspServer ia **603 ≥ 600 CRÍTICO** (minha slice 4 abriu a dívida; regra “dívida travada nunca cresce” aplicada a arquivo SEM baseline também — o gate pega) -> extraída `LspProject` (toPath/siblings/readOrNull), estrutura-only, 572. Rename cross-file NÃO entra (WorkspaceEdit em arquivo não-aberto = rule 6). **Prova:** `referencesSpanProjectFiles` (2 locations, 1 por uri); `LspServerTest` 28/28, kof-cli 334/0F, compiler 2294/0F (17E=node), check_500 rc=0. SHA `f5df2362`. §303: fechado no push anterior pela outra lane docs (`1b425a57`) com o mesmo diagnóstico (snapshot 647 era pré-rebase; gate verde via `ea0ac82b`) — meu catálogo duplicado foi descartado no rebase (zero colisão). X10 segue 🟡: hover-assinado / snippets = próximas fatias; **ÁRVORE DE QUESTÕES p/ mantenedora (rule 6, nada editado por agente):** superfície do runner `kof.test` da fatia 3 X8 (API `property(nome, iterações, fn)`? tipo gerado? shrink?) e renomeação multi-arquivo no LSP são decisões de design travadas em DECISIONS.md |
| **X10 fatia 4 — go-to-definition cruza arquivos do projeto (docs→platform lane)** | `FEITO` | sessão 18/09-2 (9093) | `beta-0.4.0` | `LspServer.java` (crossFileDefinition+toPath), `LspServerTest.java` | 18/09: miss no buffer -> `Files.walk(dir,6)` dos `.kf` irmãos (sorted, first-hit) com a MESMA convenção `LspSymbols.declarationRange` (zero parser paralelo); desconhecido = `null` honesto (R6). **Prova (Q1, mesmo commit):** `definitionJumpsAcrossProjectFiles` (uri+linha exatos em lib.kf) + `definitionUnknownNameIsNull`; `LspServerTest` 27/27, kof-cli 334/0F, compiler 2288/0F (17E=node ambiental), check_500 rc=0. SHA `5837eca7`. **Coordenação:** o gate ≤500 estava VERMELHO no tip (CmdBuild 647, empurrado pelo §299); a lane apk já o consertou (`ea0ac82b` split->ApkToolchain) — meu split paralelo foi DESCARTADO antes do push (lição: checar o tip ANTES de consertar gate alheio). X10 segue 🟡: hover de membros stdlib / referências cross-file = próximas fatias |

> **✅ FEITO (18/09 ~21:40, dono = 192.168.100.18, lane development/KofJS — DIRETRIZ "analise os documentos, verifique os gaps, identifique o que falta em nossos planos em docs/development, trace um todo de implementação"): SEGUNDO PLAN-GAP de `docs/development` fechado (proposta de design, ZERO código).** Depois do §239 (código JS real, landado `05e99dc3`), re-varri `docs/development` atrás de item autônomo `.18`: **CÓDIGO não sobra** — §248 (default methods no JS) tem `Status: 🔴 ... needs a scope decision (rule 6)` NO PRÓPRIO ledger (opções (a)/(b)/(c): implementar OU gate honesto — AMBAS são a decisão; gate unilateral invadiria regra 6); §235 face JS ✅ (minha, 16/09), restante = NATIVE (lane nat); §188/§192/§270/§271/§278/§280/§283/§284/§288/§295/§302/§304 = `.22` typing / nat / kofscript / rule-6; §258 restante = #775 `.22`. **O gap REAL está no PLANO (mesmo padrão do shell-plan):** a linha **2.1** (`kof.workflow`/`kof.batch` — jobs, pipelines, retry, checkpoints, dead-letter) é `🔵`, dono `—`, SEM doc na `future/` — ao contrário de value-records/scoped-resources/baremetal/decompiler/translator/**shell (2.2)**. Diferente de 2.3 `kof.ssh` (depende de R3 FFI ainda não-expandido) e das Stage 3–5 (R4/R5), **2.1 é a única frente Stage-2 desenhável HOJE**: todas as primitivas de baixo já existem, mediadas no §4 — `kof.process` (`Result{stdout,stderr,exitCode}`; `KofProcess.java`/`ExpressionProcessCallLowerer.java`), `kof.scheduler.every/at/cancel` (`KofScheduler.java`; cron Native `CRON001`), `kof.orm.{create,save,find,all,where,page,migrate,...}` (`KofOrm.java`; JS 18/09 `KofJsOrmBridge`, Native `ORM001`), canais+`spawn`+`selectAny` (`KofInterpreterConcurrency.java`; CONC003 JS 03/09, §132 18/09), tipo `Result` (reusado pelo shell-plan), `retry(times, backoff)` HOJE só em `kof.http` (`NativeHttpCore.java`+`RuntimeConcurrency.java`+`JsRuntimeUiLayout.java`) **não-geral**, e `kof.supervisor.one_for_one` OTP (ratificado 13/09, `planning-otp-supervision.md`, DD-OTP-01 opção A pure-Kof). **Entregue `future/workflow-plan.md` (+PT)** — 7 seções espelhando `shell-plan` (Objective / Proposta=stdlib-puro-sem-nova-sintaxe / Contrato / ABI-por-alvo-MEDIDA / Fila 2.1.0→2.1.4 / Q1–Q4 / NÃO-fazer), **forma de função stdlib** (parser sem produção `workflow` — regra 6 gramatical evitada por construção), **reusa e NÃO duplica**: supervision=`kof.supervisor`, cron=`kof.scheduler.at` (herda `CRON001`), checkpoint=`kof.orm` (`ORM001`), `run`=`process` (`PROC001`), retry=extração compartilhada do helper do `http` (Q3), dead-letter=`Iterable` in-memory v1 + tabela `kof.orm` opcional (Q4); **gated ⛔ em Q1–Q4** (forma stdlib vs builtin; escopo do MVP; retry refactor-do-http-ou-camada-nova; durabilidade dead-letter). Registro na tabela da `future/README` (+PT) e cross-ref na linha 2.1 do rastreador (+PT; status fica `🔵`/dono `—` — PROPOSTA). **Portas:** docs-lang --strict 0/0/0; check_500 rc=0; stdlib-boundary rc=0; known-bugs rc=0 (open intocado — nenhum §NNN); **ZERO arquivo de código tocado — design only, suíte não re-medida (só md)**. **PRÓXIMO PASSO (desta lane):** fila `.18` autônoma VAZIA; **três frentes empilhadas aguardando decisão da mantenedora**: (i) `D-UI-SCOPE`, (ii) `shell-plan` Q1–Q3, (iii) `workflow-plan` Q1–Q4. Re-trigger sem resposta = **RECUSAR/ESTABILIDADE** (AGENTS). NÃO TOCAR: §248 (rule 6 no ledger), §295-b (`.22`), 1.1.x (nat), ⛔ regra 6.

> **✅ FEITO (18/09 ~19:30, dono = 192.168.100.18, lane KofJS): §239 CORRIGIDO — `String.format(...)` no backend JS abortava com ICE `COMP002 "unknown JS expression: null"`; agora paridade byte-a-byte JVM+JS via ponte ao host.** Item `.18` autônomo MEDEV (dono = lane JS no ledger). **Causa medida:** `StringFormatCallLowerer` compart. empacota os varargs num `Object[]` e emite `KofCall(STATIC, owner=String, "format", (String,Object[]))`; no JS não há lowering → `isStringOp` (owner=String) captura cedo mas `handleStringOp` não trata `format` estático → cai no dispatch estático genérico → `jsClassName("java/lang/String")` devolve `null` → `JsMember(null)` → `JsEmitter.expr(null)` lança (mesma família do §235 que eu já fechei). **FIX (3 arquivos + teste):** `JsCallEmitter` rota (uma linha, ANTES do catch-all `isStringOp`) p/ `JsRuntimeOps.isStaticFormat/emitStaticFormat` → exporta `kofStringFormat` no runtime `io` (`JsRuntimeIo`) → delega ao host `KofJsRunner.kof_platform.stringFormat` (um `ProxyExecutable`) que reconstrói o boxed type de cada elemento (`isBoolean`/`isString`/`fitsInInt`→Integer/`fitsInLong`→Long/`fitsInDouble`→Double) e chama **`java.lang.String.format` do JDK** → paridade garantida. Browser sem host: o `Proxy` do `kof_platform` dá **erro honesto** (mesmo degrade de `kof.io`/FFI/`process`, R7), nunca valor errado. **API Graal 25.3:** `Value.fitsInt/fitsLong/fitsDouble` → `fitsInInt/fitsInLong/fitsInDouble` (medido no javap do jar). **Q0:** estendi `StringFormatVarargsE2ETest` (era intencionalmente só-JVM) p/ afirmar paridade JVM+JS nos 9 casos — **RED (9/9 ICE) antes do fix, GREEN (9/9) depois**. **Limitação herdada (documentada EN+PT na §239, NÃO introduzida aqui):** o identity-boxing JS colapsa `Integer`/`Long`/`Double` num único `number`, então um `double` **integral** (ex.: `30.0`) reconstrói como `Integer` → `"30"` vs JVM `"30.0"`; não-integrais (`3.5`, o caso testado) ficam corretos. **Regra 500:** meu ramo +comentário levou `JsCallEmitter` 597→615 (cruzava o teto) → logic movida p/ `JsRuntimeOps` (505→529, folgado), `JsCallEmitter` volta a **599 (<600)**. **Gates:** suíte kof-compiler completa **2286/0F/0E/179-skip BUILD SUCCESS**; docs-lang --strict 0/0/0; check_500 só restou `CmdBuild`/`§295-dup` (drones pré-existentes, NÃO meus — ver abaixo); stdlib-boundary rc=0; §239 fechado EN+PT. **DRIFT PEGO (regra 8 — NÃO toquei):** rebasing 1 atrás, origin `ea0ac82b` ja tinha corrigido `CmdBuild` 647→467 (split `runApkPipeline`→`ApkToolchain`, lane cli); e a colisão **§295 duplicado** (UI-lane `10654` × D-NULL-INTENT `10766`) existe TAMBÉM no origin → known-bugs rc=1, open-sets EN×PT IDÊNTICOS (18 itens, o `239` saiu), só o aviso "DUPLICATED SECTION NUMBERS" — NÃO é drift EN×PT, NÃO é meu (§295 dono = UI `.17` + typing `.22`). **PRÓXIMO PASSO (desta lane):** fila `.18` autônoma de novo VAZIA — §239 era o último item de código sem dono meu; re-trigger sem item = RECUSAR/ESTABILIDADE. Aguardando: (a) resposta da mantenedora p/ `D-UI-SCOPE` (única exceção p/ paridade absoluta), (b) janela `kof.shell` 2.2.0 se autorizar. NÃO TOCAR: §295-b (`.22`), §295-dup (UI+docs lanes), 1.1.x (nat), ⛔ regra 6.

> **✅ FEITO (18/09, dono = 192.168.100.15, lane docs→plataforma, X10 fatia 3 CONCLUÍDA: receivers-typed no catálogo).** StdCatalog 18→31 namespaces: json/log/orm/config/gpu/mq/validation/observability/tetris + Image/Audio/Video/Mic (nested). Guardas por dispatcher real na fonte (caseNames depth-aware == functions(); media via `case ns -> switch(name)`; json pin nos literais .equals do validador; kofStdDispatch test continua travado nos 7). Probes resolvem no dispatcher (argCount 0..8; Int[]xInt p/ gpu — dispatchMatmul=arr,arr,arr,INT,INT,INT achado na sonda). `StdCatalogTest` 10/10, `LspServerTest` 25/25 (json./log./Mic./Image. byte-exact). compiler 2251/0F, cli 328/0F. `9e4d1728`. **PRÓXIMO PASSO (re-dispatch lê isto):** X10 fatia 4 = go-to-definition além do buffer (packages: resolver definição em outros arquivos do projeto/stdlib — exige índice de símbolos multi-arquivo no LspSymbols; escopo grande, fatiar: primeiro saltar entre arquivos do MESMO projeto kofdeps). Alternativa na fila: X8 fatia 3 (runner property no kof.test) ou X2 (Python/R via kof.process+JSON — CLI). Regra 9: só árvore real do repo (nada de /tmp p/ trabalho). Manter beta-0.4.0 SEMPRE (reorganização de branches em curso). NÃO TOCAR: C3.2 (`.18`), §291 (nat), X5/X6+D1–D7 (mantenedora), plan-kof-shell (future/).

> **✅ FEITO (18/09, dono = 192.168.100.15, lane docs→plataforma, X10 fatia 2 CONCLUÍDA: dispatch próprio no catálogo).** StdCatalog 7→18 namespaces: time(17)/http(10)/cache(5)/db(3)/process(1) + passwords/crypto/jwt/secrets/security/auth (46 membros). Guardas na fonte: topCaseNames depth-aware == functions() por classe; NAMESPACES==chaves do nested switch da segurança; todo membro resolve no dispatcher REAL (probe até aridade 8 — achou hoursBetween=INTx8, daysBetween=INTx6). `StdCatalogTest` 6/6; `LspServerTest` 24/24 (time./db./crypto.). compiler 2244/0F, cli 327/0F, check_500 OK. `e79a3ea0`. **PRÓXIMO PASSO (re-dispatch lê isto):** X10 fatia 3 = namespaces receiver-typed fora do estático: json (encode/decode — typer `ExpressionJsonCallLowerer`/`MemberCallNamespaces`), observability/validation/tetris (têm NAMESPACES+staticMethod próprios NÃO roteados pelo KofStd — verificar entry real antes; o teste `kofStdDispatchCoversExactlyTheCatalog` é travado nos 7 e exigirá sincronia se um dia entrarem no KofStd). Regra 9 (18/09, mantenedora): NUNCA trabalhar em /tmp — árvore real do repo. Continuar SOMENTE na beta-0.4.0 (fila X da plataforma universal; sem trocar de branch — reorganização de branches em curso). NÃO TOCAR: C3.2 (`.18`), §291 (nat), X5/X6+D1–D7 (mantenedora), plan-kof-shell (future/).

> **✅ FEITO (18/09, dono = 192.168.100.15, lane docs→plataforma, X10 fatia 1 CONCLUÍDA: completion domain-aware da stdlib no LSP).** `StdCatalog` (compiler) expõe os membros dos 7 namespaces do `KofStd.staticMethod` (math/strings/encoding/net/uuid/random/rng); `LspServer.completion` em `'<ns>.'` oferece os nomes REAIS do typer (kind Function, detail `kof.<ns>`); prefixo estranho = zero invenção. Single-source SEM parser paralelo: `functions()` transcrito dos case-literals + `StdCatalogTest` 3/3 trava as duas pontas contra a fonte (lista==cases, dispatch KofStd==chaves, todo membro resolve no typer). `LspServerTest` 23/23 (+4: rng./math./foo./no-regressão). Módulos medidos: compiler 2241/0F (17E node), cli 326/0F. **Claim feito junto do código** (correção da lição da fatia anterior). Commit `8a2c26b7`. **PRÓXIMO PASSO (re-dispatch lê isto):** X10 fatia 2 = namespaces de dispatch próprio no catálogo (time/json/http/db/cache/security/process/ffi — cada um tem entry-point próprio no typer; mesmo padrão de guarda: lista==case-literals da fonte) — continuar na fila X da plataforma universal SEM mudar de branch (diretriz da mantenedora 18/09: não sair da beta-0.4.0, há reorganização de branches em curso). NÃO TOCAR: C3.2 (`.18`), §291 (nat), X5/X6+D1–D7 (mantenedora), `plan-kof-shell` (future/ — precisa promoção explícita).

> **✅ FEITO (18/09, dono = 192.168.100.15, lane docs→plataforma, X8 fatia 2 CONCLUÍDA: face NATIVE x86_64 do `rng`).** `runtime/RuntimeRng` — xorshift128+splitmix32 em asm (mesmos imediatos/shifts dos fragments JVM/JS — paridade por construção), encadeado em `NativeRuntime` após `RuntimeRandom`; estado global em `.data` (dentro do root-scan conservativo: falso-positivo = sobre-marcar, seguro, precedente cache/config). `KofRng.supportedOn`: NATIVE agora; cross riscv/aarch + ANDROID seguem `RNG001` honesto. `KofRngTest` 11/11 — `jvmNativeParityFullFace` roda o MESMO programa no JVM e no binário (stdout byte-idêntico: seed/int/bool/double/string/bordas), `nativeMatchesOracle`, `contractNative` (500 it). **Bug de roteamento PEGO pela prova (Q4):** o special-case `random_double` emitia `call kof_random_double` hardcoded tbém p/ `rng.double()` — 0 nexts consumidos, ints deslocados (medido: JVM i1=657554 vs NATIVE 900067=n1); corrigido p/ callee por nome no mesmo commit. Suíte kof-compiler 2238/0F (17E=node ausente). Slice-2 claim feito no commit do código (`367af29d`) a posteriori — lição: próxima fatia, claim FIRST. **PRÓXIMO PASSO (re-dispatch lê isto):** X8 fatia 3 = runner property no `kof.test` (¶ superfície de linguagem — decidir com mantenedora? NÃO: `@property`-style função de ordem superior `property(iter, f)` em kof.test é additive, sem mudança de contrato congelado — mas ANTES disso: port riscv/aarch do rng = fatia 3 alternativa, exige qemu (lane nat — não esta máquina). fila X aberta sem dono: X10 LSP (`docs/development/`), X2 Python/R, X4 eval. NÃO TOCAR: C3.2 (`.18`), §291 (nat), X5/X6+D1–D7 (mantenedora).

> **✅ FEITO (18/09, dono = 192.168.100.15, lane tooling/docs→plataforma, X9 fatia 3 CONCLUÍDA: face ANDROID no `kof deploy`).** `deploy --target android` empacota o APK assinado pelo MESMO pipeline do `build --apk` (`runApkPipeline` agora estática p/ reuso); sem SDK = falha honesta (R6, não fake-success). Release idêntica: artefato .apk + RELEASE.md (`adb install`) + SHA256SUMS + tar.gz. Testes: androidWithoutSdkIsHonestFailure (remove ANDROID_HOME do env, exige exit!=0 + mensagem causal) + androidWithSdkPackagesApk (guard honesto aapt2; skip sem SDK) + cross riscv/arm seguem DEP001. `CmdDeployTest` 9/9 (1 skip), módulo 322/322. **Prova:** o ELF da fatia 2 RODA no teste; o APK sem SDK recusa com causa; checksum byte-wise em todas as faces. **PRÓXIMO PASSO (re-dispatch lê isto):** X9 fecha em 3 faces + android; `--publish` = ⛔ D2 (não tocar); cross riscv/arm = face futura (sysroot). Fila da plataforma continua: X8 fatia 2 = asm nativa do `rng` (x86_64 primeiro — toolchain as/ld PRESENTE neste host, o teste pode RODAR de verdade e fechar a paridade tripla JVM==JS==Native; riscv/aarch ficam RNG001 até qemu). NÃO TOCAR: C3.2 (`.18`); §291/box nativo (nat `.17`); X5/X6 + D1–D7 (mantenedora).

> **✅ FEITO (18/09, dono = 192.168.100.15, lane tooling/docs→plataforma, X9 fatia 2 CONCLUÍDA: faces NATIVE+JS no `kof deploy`).** Commit `bfdd452a` (NEXT STEP do entry anterior): `deploy --target native` empacota o ELF `Default/Main` com mode 0755 no tar e `--target js` o `Default.mjs` — MESMA estrutura de release (artefato + RELEASE.md + SHA256SUMS + tar.gz). **Prova real:** o teste RODA o binário empacotado e exige a saída do app (`native ok`); checksum do ELF confere; bit executável no header do tar; JS com run `node <name>.mjs`. ANDROID/cross (native.risc/native.arm) continuam `DEP001` honesto (faces futuras). Módulo kof-cli 320/320. **PRÓXIMO PASSO (re-dispatch lê isto):** X9 fatia 3 candidatos — face ANDROID (APK no deploy, precisa do pipeline apk do build) ou `--publish` (bloqueado D2/mantenedora, não tocar). Se ANDROID colidir com lane ativa, RECUSAR item e pegar outro aberto sem dono. NÃO TOCAR: C3.2 (`.18`); §291 (nat); X5/X6 + D1–D7 (mantenedora).

> **✅ FEITO (18/09 ~14:55, dono = 192.168.100.15, lane bugs-and-gaps/docs): DRIFT rng×language-reference CAÇADO E PREENCHIDO (lição `extern` repetida).** O namespace builtin `rng` (X8 fatia 1, landings `4b6c9427`/`47322044`/`a2d8828b` da mantenedora) tinha learn/39 + training/idioms/stdlib + backend-parity + gate R1 no ledger, mas ZERO vistas no `language-reference` (0 ocorrências em modules.md/specification-status — o buraco exato que `extern` teve e foi fechado hoje). Células novas EN+PT: `modules.md` §5 (parágrafo do namespace builtin — roteado por `KofStd`/`KofRng`, sem classe `kof.*`, sem import; API medida no código: `seed(Int)`/`int(Int)`/`boolean()`/`double()`/`string(Int,String)`) + linha em `specification-status.md` (§1 stdlib: **Experimental**, JVM+JS, NATIVE/ANDROID/cross = gap honesto `RNG001` R6; evidência REAL `KofRngTest` 8 @Test — nomes conferidos por grep antes do commit, `nativeIsHonestGap` pina o RNG001 nos 3 targets cross). docs-lang 0/0/0. Proxima janela: cell `rng` na matriz de parity ja feita pela mantenedora em a2d8828b (verificado).

> **✅ FEITO (18/09 ~15:25, dono = 192.168.100.15, lane bugs-and-gaps/docs): TALLY AGENTS 2570->2575 EN+PT (medicao REAL no job CI Build+Tests de `d14275f0`, por modulo: 2216/0F/177-skip + 39 + 7 + 313/0F) + Q4 de `CmdBuildClasspathTest` no runner Windows da CI = NAO coberto (o job windows-latest corre so `IoE2ETest`; o meu teste injeta o separador `;` e prova a semantica Windows sem depender do SO — o e2e real do `--classpath` em Windows fica como observacao, nao como green falso).** (a) 3 linhas EN + 3 PT atualizadas (hoje **2575**, por-modulo 2216+39+7+313, refs 96ab646e->d14275f0); (b) caça ao `KofRngTest` no log CI = 0 hits: VERIFICADO que a contagem de 2216 ja era POS-rng (4b6c9427 13:19 < 96ab646e 13:25; o tally medido as ~14:10 ja continha os 8) = ausencia de drift, so artefato do formatador de log da surefire (nomes de classe nao aparecem no stdout completo); (c) suíte local re-medida ANTES do commit: KofRngTest 8/8 verde com -am (Q0 check de que o 0-hit nao era teste quebrado). docs-lang 0/0/0.

> **✅ FEITO (18/09 ~15:05, dono = 192.168.100.15, lane bugs-and-gaps/docs): TICK POS-#441 — CI do code-tip `d14275f0` = Build+Tests SUCCESS + PR #442 revisada com evidencia tecnica (nao-fechada: PR de terceiros = decisao da mantenedora).** (a) CI: o push do fix + os 3 docs-pushes seguintes fecharam `completed/success` (Q5 no CI, nao so local); (b) **PR #442 (Ruan625Br)** = fix duplicado do que ja mergeei (`d14275f0`) + churn de release indevido no escopo do issue (version.properties→0.4.4-beta, CHANGELOG 0.4.1 com 1200 linhas do historico dela, workflow bumps checkout@v7/github-script@v9) — comentado com: 1. o fix dela **perde a tolerancia historica ao `;` em Unix** (`Pattern.quote(pathSeparator)` parte so `:` la; o meu mantem `[:;]` bit-for-bit no Unix e usa `;`-only no Windows + 5 testes incluindo o drive-letter do issue e o caso mixed-list legado); 2. pedido de split (bumps de actions = PR propria com issue relacionada, policy do repo; version/changelog = lane da mantenedora); 3. sugestao de fechar como superseded. **Nao fechei nem mergeei** (regra: PR alheia = ask owner, nunca touch unilaterally).

> **🎯 CLAIM + FEITO (18/09 ~15:55 BRT, dono = 192.168.100.15, lane cli/tooling): #441 corrigida — `--classpath` no Windows particionava `C:\` no dois-pontos.** `CmdBuild.java:237` (`kof build`) usava `classpath.split("[:;]")`: em `C:\Users\...\skiko.jar;C:\...\kotlin-stdlib.jar` o `:` da unidade cortava em fragmentos (`C`, `\Users...`) → `CP002 classpath entry not found: C` falso (repro verbatim do issue reproduzido no padrao antigo — prova no commit). FIX: `File.pathSeparatorChar` e o separador real: Windows → **so `;`**; Unix → mantém `[:;]` (tolerancia historica, zero regressao). Seam `CmdBuild.splitClasspathEntries(String[,char])` package-private p/ injecao deterministica (host do teste e Linux; a semantica Windows provada por injecao). Escopo: unicos sitios de parsing de classpath do user — `Deps.classpath()` ja era OS-aware; `run` nao expoe `--classpath`. Teste novo `CmdBuildClasspathTest` 5/5 (drive-prefix intacto, single-entry, unix duplo, blank/null, default==plataforma). Suite kof-cli completa verde (Q4). Docs: nenhuma promessa violada (comportamento documentado = separador de plataforma). **#441 FECHADA** (18/09 14:37): comentario tecnico com root cause + prova red-antes/verde postado, `gh issue close` ok; kof-cli suite completo 313/0F (27 relatorios, Q4/Q5) ANTES do close; CI `d14275f0` em andamento no push. Fila da lane: proxima varredura de issues/CI no proximo tick.
> **✅ FEITO (18/09, dono = 192.168.100.15, lane tooling/docs→plataforma, X9 fatia 1 CONCLUÍDA: `kof deploy --target jvm`).** Commits: `154ea1a4` (CmdDeploy + case/usage no Main + `CmdDeployTest` 4/4; módulo kof-cli 317/317). Empacota release JVM a partir do pipeline do build: fat jar (reusa `CmdBuild.buildFatJar`, D-APP.5) + `RELEASE.md` (artefato, target, main class, versão do compilador, timestamp UTC, comando de execução) + `SHA256SUMS` (MessageDigest) + `.tar.gz` ustar+gzip próprio (checksum do tar com campo a espaços correto). **Prova real:** o teste roda `java -jar` do artefato empacotado e exige a saída do app; checksum confere byte-wise; tar validado (magic ustar, 3 entries na ordem, bloco zero final). Recusas honestas: NATIVE/JS/ANDROID → `DEP001` (fatia 1 = JVM); `--publish` → `DEP001` citando D2 (registry é decisão da mantenedora — nunca publish falso). Docs: usage no CLI; tracker X9 🔵→🟡. **PRÓXIMO PASSO (re-dispatch lê isto):** X9 fatia 2 candidatos — face android (APK no deploy) ou faces NATIVE/JS (artefato único no pacote) — ambos sem impedimento; escolher o de maior valor e repetir o ciclo (claim → impl → teste → docs → push). Colisão de lane = RECUSAR item e pegar outro. NÃO TOCAR: C3.2 (`.18`); §291 (nat); X5/X6 + D1–D7 (mantenedora).


> **🎯 CLAIM + FEITO (18/09 ~15:55 BRT, dono = 192.168.100.15, lane cli/tooling): #441 corrigida — `--classpath` no Windows particionava `C:\` no dois-pontos.** `CmdBuild.java:237` (`kof build`) usava `classpath.split("[:;]")`: em `C:\Users\...\skiko.jar;C:\...\kotlin-stdlib.jar` o `:` da unidade cortava em fragmentos (`C`, `\Users...`) → `CP002 classpath entry not found: C` falso (repro verbatim do issue reproduzido no padrao antigo — prova no commit). FIX: `File.pathSeparatorChar` e o separador real: Windows → **so `;`**; Unix → mantém `[:;]` (tolerancia historica, zero regressao). Seam `CmdBuild.splitClasspathEntries(String[,char])` package-private p/ injecao deterministica (host do teste e Linux; a semantica Windows provada por injecao). Escopo: unicos sitios de parsing de classpath do user — `Deps.classpath()` ja era OS-aware; `run` nao expoe `--classpath`. Teste novo `CmdBuildClasspathTest` 5/5 (drive-prefix intacto, single-entry, unix duplo, blank/null, default==plataforma). Suite kof-cli completa verde (Q4). Docs: nenhuma promessa violada (comportamento documentado = separador de plataforma). Close #441 com evidencia.

> **✅ FEITO (18/09 ~17:30, dono = 192.168.100.18, lane development/KofJS — DIRETRIZ da mantenedora: "analise os documentos, verifique os gaps, trace um todo de implementação"): PLAN-GAP de `docs/development` fechado (proposta de design, ZERO código).** Varredura do rastreador `IMPLEMENTATION-UNIVERSAL-PLATFORM` mapeou TODOS os abertos: 1.1.x/nativo = lane nat `.17`; §295(b) = typing `.22`; X5/X6 + D1–D7 + GC-sign-off + registry + bare-metal-scheduling = ⛔ regra 6; Stages 3–8 dependem de R4/R5/FFI; o resto da R3 (3.3/3.5/3.8) = ⛔ mantenedora, 3.7 = nat. **Nada autônomo de CÓDIGO sobra para a `.18`** (confirma o "RECUSAR/ESTABILIDADE" do entry anterior). O gap REAL estava no PLANO: a frente Stage 2 (`kof.shell` 2.2 / `kof.workflow` 2.1 / `kof.ssh` 2.3) era linha `🔵` solta, dono `—` e **SEM** doc de design na `future/` — ao contrário de value-records/scoped-resources/baremetal/decompiler/translator. Entreguei a peça que faltava: **`future/shell-plan.md` (+PT)** — proposta só-idiomática sobre o `kof.process` REAL medido (JVM `run`/`spawn`/`exit`; JS `run` via `kof_platform.processRun` + exit-sentinela; Native `PROC001` em `ExpressionProcessCallLowerer`), **forma de função** (não infixo → zero mudança de gramática/regra 6), argv-como-lista (classe de segurança `concatenated-command-line`), `Result` reaproveitado, fila de slices 2.2.0→2.2.4 com dono+prova+ABI-por-alvo, **gated ⛔ em Q1–Q3** (aprovação da mantenedora ANTES de código — 2.2.1 é o portão). Registro na tabela da `future/README` (+PT) e cross-ref na linha 2.2 do rastreador (+PT; status fica `🔵`/dono `—` — PROPOSTA, frente não aberta). **Portas:** docs-lang --strict 0/0/0; check_500 rc=0; stdlib-boundary rc=0; known-bugs rc=0 (open inalterado — nenhum bug tocado). **PRÓXIMO PASSO (desta lane):** se a mantenedora quiser `kof.shell` de verdade, resolver Q1–Q3 e disparar 2.2.0; senão a fila `.18` segue sem item autônomo (RECUSAR/ESTABILIDADE). NÃO TOCAR: §295(b) (`.22`), 1.1.x (nat `.17`), ⛔ regra 6.

> **✅ FEITO (18/09 ~18:30, dono = 192.168.100.18, lane KofJS): bug real de honestidade no backend JS — `process.spawn` compilava limpo e CAIA em `ReferenceError: kof_process_spawn is not defined` (exit 1) no runtime.** Medido: `ExpressionProcessCallLowerer` só gateava NATIVE (comentário mentia: "JVM/JS support it"), mas o JS não liga nenhum `kof_process_spawn`/handle-op (`isRuntimeOp` só tem run/exit) → emitia chamada crua `kof_process_spawn(...)` sem definição. **FIX cirúrgico (1 condição):** gate JS→`PROC001` igual ao Native (`driver.target == Target.JS`); JVM/Android seguem compilando spawn; `process.run` no JS intacto (bound via `kof_platform.processRun`). **AUDITORIA sistêmica:** diff dos ops `kof_*` emitidos vs `isRuntimeOp` provou spawn o ÚNICO caso da classe — `*_to_string` (gate `isNative`), `args_list` (branch JVM-only), `tetris` (`supportedOn` JVM), `vk`/`mv64` (GPU native||JVM), `media` (`MEDIA001/003`), `app_on_*`/`test_N` (funções sintéticas) todos corretos. **Provas:** `DomainGapCodesTest` +3 pins (`processSpawnOnJsIsProc001`/`OnNativeIsProc001`/`OnJvmHasNoGap`) 15/15; nenhum teste antigo esperava spawn JS compilar. **Docs:** `future/shell-plan` §4 EN+PT corrigido (claim "JS spawn ✅" → `❌ PROC001` medido); `status.md`/`.pt_BR` `process.spawn … JVM/JS` → JVM-only honesto. **REGRA NOVA da mantenedora (18/09, AGENTS item 9):** NUNCA trabalhar em `/tmp` (cai a luz) — direto no working tree da `beta-0.4.0`, commit local; este fix nasceu em `/tmp` e um apagão levou o worktree (recuperado dos objects `90d3754b`, agora persiste na branch real). Gates: docs-lang --strict 0/0/0; check_500/stdlib-boundary/known-bugs rc=0 (nenhum §NNN — é honest-gap fechado no próprio commit). **PRÓXIMO PASSO (desta lane):** sem item autônomo de código; aguardando (a) resposta da mantenedora p/ registrar D-UI-SCOPE (exceção kof.ui = única exceção da paridade absoluta; JS/WASM, WASM ainda não é Target), e (b) janela p/ 2.2.0 se autorizar `kof.shell`. NÃO TOCAR: §295(b) `.22`; 1.1.x nat; ⛔ regra 6.

> **✅ FEITO (reivindicado+concluído 18/09 ~16:05, dono = 192.168.100.18, lane KofJS — POR DIRETRIZ EXPLÍCITA DA MANTENEDORA, atravessando o cluster D-NULL-INTENT da `.22`): §295 face (a) CORRIGIDA — operador relacional (`>`/`<`/`>=`/`<=`) sobre `Int?`/`Boolean?` em posição de CONDIÇÃO dava `VerifyError` no LOAD do JVM.** Depois de fechar o §279, a mantenedora mandou consertar o VerifyError `Int? v > 0` que eu havia reportado à `.22`. **Causa raiz medida:** `CompilerComparisons.emitComparisonShortcut` (atalho de condição usado por `StatementLowerer.IfStmt`/while/print-if) calcula `common = comparisonOperandType` desembrulhando `Nullable(Int)`→`INT`, mas mantém `leftT`/`rightT` com o wrapper e NUNCA desempacota o `Integer` boxado (representação do Commit B do #278) antes do `if_icmpgt` → `VerifyError: … 'java/lang/Integer' … not assignable to integer @if_icmpgt`, `stack {Integer, integer}`; compila limpo e MORRE no LOAD (até `probe(null)` falha — o verifier checa o método todo). O caminho de VALOR (`ExpressionBinaryLowerer.isNumericComparison`, 288/318) já desempacotava; só o atalho de condição não. `==`/`!=` não usam o atalho com nulável (`isComparisonShortcut` desativa). **FIX (cirúrgico, 1 método, `emitComparisonShortcut`):** `driver.emitErasureUnbox(ops, inner)` em cada operando `Nullable(primitivo)` antes do widening/compare. `emitErasureUnbox` é JVM-only (`needsErasureBoxing() == target==JVM`) → JS/Script/Native byte-a-byte intocados (no-op; relacional deles já correto/cru; Native `Nullable` fica cru, fase 2 #278). **Prova Q0:** `NullablePrimitiveRelationalConditionTest` 3/3 — else-if `e\nz\nz\nn` (o próprio verbatim do §279), if-puro `p\nm\nz`, largura-mista `Int? >= 100000L` `big\nsmall`; JVM RODA + paridade byte-a-byte JS. `git stash` do fix→3/3 RED (VerifyError no LOAD); restaurado→3/3 GREEN. Suíte `kof-compiler` completa **2223/0F/0E/178-skip** (o atalho COMPARTILHADO de condição não regrediu nenhum teste). **Ledger:** §295 nova (EN+PT) com DUAS faces — (a) condição ✅ CORRIGIDA aqui; (b) VALOR `Bool b = v > 0` 🟡 ABERTA → `.22` (typing do `VarDecl`: os locais `Bool` saem como slots de referência `Integer` → `astore` de um `int` → `VerifyError integer not assignable to reference`; mesma família §294/§241). §279 cross-ref atualizado EN+PT (agora aponta §295; o verbatim else-if roda no JVM). NÃO abri a célula de 4 targets da `conformance-matrix` (face (b) + Native/Script pendentes a manteriam vermelha). **Ética de lane:** entrei no cluster da `.22` por ordem EXPLÍCITA da mantenedora neste turno, e SÓ na face (a) de condição (medida, 1 método, no-op nos outros 3 alvos); a face (b) fica DELIBERADAMENTE com a `.22`. Gates: known-bugs rc=0 (open 19, EN×PT idêntico — §295 face (b) é o item novo); docs-lang --strict 0/0/0; check_500 rc=0; stdlib-boundary rc=0. **PRÓXIMO PASSO (desta lane):** fila `.18` de novo vazia; re-trigger sem item = RECUSAR (ESTABILIDADE), salvo a mantenedora mandar eu pegar também a face (b) (aí sim entro no typing da `.22`).

> **✅ FEITO (reivindicado+concluído 18/09 ~14:40, dono = 192.168.100.18, lane KofJS): §279 FECHADO (ICE do JS em `if` sobre primitivo nulável — regressão do marcador §267) + pin anti-regressão; ACHADO reportado à `.22` (relacional `Int?` sobre parâmetro → VerifyError no JVM).** **§279 (minha lane, ledger):** medido no tip `c0641dbb` com build fresco do `kof-compiler` — os DOIS verbatims do § (`Int?` else-if + `Boolean?` void-tail) agora compilam no JS com zero diagnósticos e rodam no golden `"n"`; o ICE `COMP002 unexpected op in expression statement` sumiu. Não houve patch dedicado: o ICE morreu de brinde quando o #278 D-NULL-INTENT (`495445cd`, "ramo null de if") trocou a dobra-literal do null-check por um teste de null REAL, o que para de órfão o `KofStatementIf` → o dispatcher JS nunca vê marcador sem `CJump` seguinte. Fechei como dono registrado do fix com o teste que o #278 deixou de fora: `JsNullablePrimitiveIfIceTest` (4/4) trava os dois verbatims compilando+rodando no JS e o JVM compilando (Q0/Q7). Ledger §279 ✅ EN+PT (flip de status + nota de resolução + ressalva de escopo). **ACHADO (reporto, NÃO conserto — fora da minha lane):** o verbatim else-if compara `Int? v > 0` (relacional sobre primitivo nulável). No JVM FALHA NA VERIFICAÇÃO no LOAD da classe — `VerifyError: Bad type on operand stack … Type 'java/lang/Integer' … is not assignable to integer`, `Main.probe(Ljava/lang/Integer;)Ljava/lang/String; @22: if_icmpgt`, `stack {Integer, integer}` — o caminho relacional escolhe `if_icmpgt` mas NÃO dá unbox no operando boxado. **RE-MEDIDO no tip `c0641dbb` via `env -i java` (não presumido).** É um BURACO na face relacional do #278/#438 (que foi validada só com operando vindo de `Map.get`); família D-NULL-INTENT, dono = lane do compilador `.22` / `.1`-`#438`; NÃO afeta o JS (ramo `v>0` é morto para `probe(null)`) e não é o §279. **POR ISSO NÃO abri a célula de 4 targets da conformance-matrix** que o § pedia — pinar o else-if em JVM/Native deixaria essa célula vermelha por ESTE bug, não pelo §279; adiado de propósito. **Pedido à `.22`:** reabrir a face relacional p/ operando-parâmetro nulável (+§ dedicado se quiser); tenho verbatim e dump prontos. ZERO arquivos da `.22` tocados. Provas: `JsNullablePrimitiveIfIceTest` 4/4; 4 portas verdes — known-bugs rc=0 (open 18, EN×PT idêntico); docs-lang --strict 0/0/0; check_500 rc=0; stdlib-boundary rc=0. **PRÓXIMO PASSO (desta lane):** §279 era o último item `.18` aberto na fila; R3 FFI escalar+callback+String-arg fechado; re-trigger sem item novo = RECUSAR (ESTABILIDADE), a não ser que a `.22` me re-ping na face relacional do `Int?` (é dela).

> **✅ FEITO (18/09 ~13:55, dono = 192.168.100.15, lane docs): CELULA DO IDIOM `Map.get` CORREGIDA POS-#438 (EN+PT).** `training/idioms/collections` dizia "primitive: the type stays V" — apos o merge N1 isso e MENTIRA de doc (medido: `Int z = m.get("a")` → type-mismatch `NullableType[int]`; primitivos agora sao `V?` tambem). Celula atualizada com a promessa cumpida + alerta honesto do §294 (present×primitive null-check crasha no JVM; preferir `getOrDefault` `62bd455e`/`contains` ate fechar). docs-lang 0/0/0.

> **✅ FEITO (18/09 ~13:45, dono = 192.168.100.15, lane docs, commit pending): POINTER 2.6 SINCRONIZADO COM O MERGE.** `roadmap.md`+PT fila 2.6: #438 era "pending maintainer review" — agora **MESCLADO `250f6207`** (12:53); N1 landou (absent-face medido `9`), presente×primitivo fica no §294-2a (dono `.22`), N2/I4 fora de escopo. docs-lang 0/0/0.

> **> **✅ FEITO (18/09 ~14:35, dono = 192.168.100.15, lane docs): TALLY DO AGENTS RECONCILIADO E ATUALIZADO (EN+PT, commit do push).** 2507→**2570 = 2216+39+7+308, 0F/0E** (medicao do job CI `Build + Tests` no tip `96ab646e`, run 35368363108, SUCCESS ~14:05). A divergencia da NOTA anterior fechou: 2556 (`.17`, `952acbc8`, antes da C3.4) + ~14 testes novos da C3.4 (`b120945c`) = 2570; os 177 skip = guards sem qemu do runner (cross e2e verde no job `Native cross`; `.17` mediu 11-skip com qemu). Linha do estado com a nota das duas fontes. docs-lang 0/0/0.

⚠️ NOTA (18/09 ~14:20, dono = 192.168.100.15, lane docs): TALLY 2507 DO AGENTS ESTA STALE — as duas fontes novas NAO fecham entre si:** `.17` mediu **2556/0F/0E/11-skip** no `952acbc8` (commit message); o CI `Build + Tests` do tip `96ab646e` (SUCCESS ~14:05) reporta **2570 = 2216+39+7+308, 0F/0E/177-skip** (runner sem qemu para os 84 cross+guards → skip maior). A linha do AGENTS so deve mexer quando uma medicao com qemu fechar o reconciliation (Q3: golden de medicao real, nunca de media de fontes). Deixado como NOTA, NAO EDITEI o AGENTS.

> **✅ FEITO (18/09 ~13:35, dono = 192.168.100.15, lane bugs-and-gaps/docs, commit `9f018542`): §294 RE-MEDIDA PÓS-#438 (`250f6207`) — crash MUDOU DE LUGAR, não morreu.** #438 merged pela mantenedora 12:53 e caiu no meu tip via rebase. Medido com jars frescos: face AUSENTE consertada (`9` ✅); face PRESENTE×primitivo quebra para TODOS (Int/Long/Double/Boolean → `NoSuchMethodError Object.valueOf(boxed)` no narrowing, `T.kf:4`; String ok; Float → CCE Double→Float no sitio do mapa = bug adjacente de storage, anotado p/ `.22`). Script face: matriz 4/4 ✅. §294 segue OPEN com dono `.22` cluster erasure/boxing (NAO o #438) e lista de aceite registrada (presente×{Int,Long,Double,Boolean,String}×{JVM,JS} + script matrix + absent original). docs-lang 0/0/0; ledger rc=0; contagem 20 estavel.
> **✅ FEITO (18/09, dono = 192.168.100.15, lane docs→plataforma, X8 fatia 1 CONCLUÍDA: namespace `rng` — PRNG semeável p/ property-based testing, JVM+JS).** Commits: `a71f761c` (KofRng.java — dispatch KofStd, xorshift128+splitmix32, ledger R1) + `1ff54c6e` (runtime: fragmento JVM `JvmStringRngRuntime` + descriptors + roteamento `kof_rng_` em JvmRuntime/JvmOpCollections; bloco JS `JsRuntimeUiRng` + slices + isRuntimeOp; `KofRngTest` 8/8). Contrato provado: mesma seed ⇒ mesma sequência na JVM e no JS (oracle Java no próprio teste + paridade byte-a-byte `jvmJsParity`); re-seed reinicia (idempotência); bordas lenientes (int(1)/int(0)/int(-5)==0, string com alfabeto vazio); gap honesto `RNG001` em compile nos 3 nativos (R6/R7). Correção durante o unit: `NoClassDefFoundError kof/rng/Rng` — o ClassType do StdCall é tag de roteamento, não classe real; prefixo adicionado ao roteador. Docs EN+PT: learn/39-stdlib (+seção rng + idiom de property test), training/idioms/stdlib (BAD não-semeado/GOOD semeado), tracker X8 🔵→🟡 (fatia 1 ✅, fatias 2=asm nativo/3=runner property), backend-parity delta 18/09(5) + `RNG001` na convenção de gaps. **Prova:** `KofRngTest` 8/8 green (mvn test -Dtest=KofRngTest); compile green; docs-lang 0/0/0. **PRÓXIMO PASSO (re-dispatch lê isto):** X8 fatia 2 = asm nativo (x86_64/riscv64/aarch64, mesma matemática 32-bit — dono nativo lane ou retomar aqui se livre); fatia 3 = runner property no `kof.test`. Se ambas bloqueadas por colisão de lane: RECUSAR (ESTABILIDADE). NÃO TOCAR: C3.2 (`.18`); §291 (nat); X5/X6 + D1–D7 (mantenedora).


> **✅ FEITO (18/09 ~12:50 BRT, dono = 192.168.100.15, lane bugs-and-gaps/docs, commit `4f15b564`): **MATRIZ DE FFI REGISTRADA** — `FFI001`/`FFI002` eram emitidos por `CompilerPipeline.isExternBound` mas ausentes de `backend-parity.md` (violaria R6 na doc). Medido no tip `94eb7a08`: Native `extern` -> `FFI001` (todo extern; isExternBound retorna false sem condicionante); JS nao-escalar -> `FFI002` (CompilerPipeline.java:234); JVM nao-escalar -> `FFI001`; JVM String->Int funciona (strlen=3). 3 visoes EN+PT: linha de paridade FFI (JVM/Native/JS), linha de gap FFI001/FFI002, prefixo `FFI` na tabela de convencao. Prova citada: FfiE2ETest 16/16 JVM<->JS, JvmFfiCallbackE2ETest, KofJsFfiBridgeTest 8/8. docs-lang check 0/0/0.

> **✅ FEITO (reivindicado+concluído 18/09, dono = 192.168.100.18, lane development / R3 FFI): 3.4-C3.4 — `String` como ARGUMENTO de callback binda na JVM e no host JS, paridade byte-a-byte (o `char*` que o C entrega vira `String` Kof na fronteira do upcall; um callback que DEVOLVE `String` segue gated).** Fechado o "follow-on" que o §R3-3.4 listava em aberto (String-in-callback). **3 sítios cirúrgicos:** (a) `FfiSignature.cbParamChar` deixa de rejeitar `'S'` (o `paramChar` já mapeia String→'S'; `callbackDescriptor` agora codifica `(String)->Int` → token `(iS)`), enquanto `cbReturnChar` CONTINUA rejeitando 'S' → retorno String cai no gate honesto; (b) `JvmFfiRuntime.kof_ffi_upcall` — um param 'S' usa carrier NATIVO `MemorySegment` (ADDRESS) e carrier Kof `String`; `MethodHandles.filterArguments` insere `kof_ffi_cstr` (`reinterpret(MAX).getString(0)`, NULL→null) que lê o `char*` ANTES do `invoke` rodar → o closure vê um String real (método novo `kof_ffi_cstr` na mesma classe gerada); o caminho só-primitivo é no-op idêntico (sem regressão); (c) `KofJsFfiMarshal.jsCallbackStub` — param 'S' → `prim[k]=MemorySegment.class`; `executeJs*` agora chama `jsBridgeArgs` que converte `MemorySegment`→String (`getString`) antes de `Value.execute` (host String→JS string automático), espelhando o `kof_ffi_cstr`. `KofJsFfiBridge` NÃO mudou (o `real[i]=args[i]` já passa o stub `MemorySegment` ADDRESS; o parse por cursor é agnóstico ao `inner`). **Provas (roda de verdade, 0 skip, node+gcc presentes):** `FfiSignatureTest` +`callbackDescriptorBindsScalarAndStringArgsButNotStringReturn` (4→5); `JvmFfiCallbackE2ETest` +`stringCallbackArgsBindAndMatchJvmJs` — um `.kf` roda nos DOIS alvos e imprime `5\n104\n2026` (len, 100+len, `atol`-dentro-do-callback = prova de CONTEÚDO, não só tamanho), JVM==JS byte-a-byte (4→5); os dois testes não-bindáveis foram RE-POSICIONADOS de "arg String" p/ "retorno String" (continua `FFI001` JVM / `FFI002` JS); `KofJsFfiCallbackBridgeTest` +`charPointerCallbackArgCrossesIntoGraalJs` (reentrante `char*`→String no host, 1→2). Docs EN+PT no MESMO commit: §R3-3.4 (superfície/runtime/restrição/postura/fatias → C3.4), linha 3.4 da tabela, célula do invariante R3, `training/idioms/interop` EN+PT. Contrato mantém síncrono/não-escapante; `String`-de-retorno/struct/pointer/callback-como-retorno ficam honestos `FFI001`/`FFI002` (R6). **Restam na R3:** 3.3/3.5/3.8 ⛔ mantenedora, 3.7 §61 lane nat — nada autônomo óbvio. **PRÓXIMO PASSO (desta lane):** fila R3 `.18` fechou o item autônomo óbvio (3.4-C3.4); **restam na lane o ICE §279 do backend JS** (null-check de `T?` primitivo em forma else-if / if-sem-else como último statement de função void → `COMP002 unexpected op in expression statement`; catalogado 18/09 pela lane docs `a35067b9`; **dono do fix = `.18`** segundo o §279 e os threads #266/#408/#398/#279/#280; repro + causa-raiz medida (marcador `KofStatementIf` §267 órfão quando o otimizador dobra o conditional-jump) no corpo do §279) — atacar na sequência. **NÃO TOCAR:** §279 é DESTA lane (o dono está registrado no §); 3.3/3.5/3.8 (regra 6, mantenedora), §61 (nat `.17`).

> **✅ FEITO (18/09, dono = 192.168.100.15, lane docs, frente PLATAFORMA UNIVERSAL): `fake-idioms.md` ganha as 2 linhas FFI/callback — fila 8.5 do corpus fechada.** Sequência direta do idiom interop (`81eeb4e8`): a tabela de fake idioms NÃO cobria FFI — um agente poderia alucinar JNI/manual glue ou callback escapante. Adicionadas EN+PT as formas estrangeiras medidas no compilador: (1) callback não-escalar/síncrono/não-escapante → `FFI001` na JVM / `FFI002` no JS (medido no `JvmFfiCallbackE2ETest`); (2) binding nativo à mão em volta do `extern` → não é idiom (duplica o compilador e fura os códigos de gap honestos). Corpus 8.5 vs R3 sincronizado por completo. **Prova:** `docs-lang.sh` 100% (0/0/0); códigos de gap citados dos asserts do teste que roda. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** fila da lane vazia — corpus 8.5 em dia com o R3; os itens seguintes do plano são de outras lanes (C3.2 = `.18`; §291 = nat `.17`; X1/X4 = web/compilador; X5/X6 + D1–D7 = mantenedora). Re-trigger sem item novo = RECUSAR (ESTABILIDADE).

> **✅ FEITO (18/09, dono = 192.168.100.15, lane docs, frente PLATAFORMA UNIVERSAL): idiom interop sincronizado com os landings FFI C1/C2/C3.1 — corpus de treino (item 8.5 do rastreador) parado no C1.** A diretriz "segue com o plano da plataforma universal" apontou o primeiro gap executável da lane docs: `training/idioms/interop.md` ainda dizia "callbacks — gate still CLOSED" quando o C2 (`9a881c92`) **abriu o gate da JVM** e o C3.1 (`1ea4abc4`) pinou o JS no host. Atualizado EN+PT com a sintaxe REAL medida no `JvmFfiCallbackE2ETest`: `extern "lib" f(Int, Int, (Int, Int) -> Int cb): Int` + lambda no call site `(x: Int, y: Int) -> x + y` (42 medido); ABI de callback só primitiva e síncrona/não-escapante (escapantes = `FFI001`, regra R12/GC); JS callback = `FFI002` (paridade = fatia C3). Novo anti-padrão BAD→GOOD: guardar o ponteiro p/ chamar depois. "Veja também" atualizado (C1+C2+C3.1 ✅; restantes C3/3.3/3.5/D6/§61). **Prova:** `docs-lang.sh` 100% (0/0/0); sintaxe copiada do teste E2E que roda (42/42/6.0/7.5), não de memória. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** continuar a varredura 8.5 do corpus vs landings R3 (próximo candidato: `learn/` e `modules.md` — já sincronizados pelo `.18`; checar `training/anti-patterns/fake-idioms.md` p/ callback-fora-do-contrato). Sem item novo = RECUSAR (ESTABILIDADE). **NÃO TOCAR:** fix C3.2 (`.18`); §291 (nat `.17`); X5/X6 + D1–D7 (mantenedora).

> **✅ FEITO (reivindicado+concluído 18/09, dono = 192.168.100.18, lane development / R3 FFI): 3.4-C3.2+C3.3 — PARIDADE JS de callback/upcall FECHADA (abre o gate JS + E2E byte-a-byte JVM↔JS).** Liguei o caminho de upcall no host JS: o `ProxyExecutable` `ffi`/`ffi_void` do `KofJsRunner` delega o marshalling (args escalares + stubs de callback) para uma classe NOVA `KofJsFfiMarshal` que monta `Linker.upcallStub` e chama `KofJsFfiBridge.call/callVoid` (parse por cursor do token aninhado `(ret+params)`, slot `(` → `ADDRESS`). **DESCOBERTA honesta (C3.2):** um valor de função Kof compilado p/ JS NÃO é uma arrow nativa — é um **objeto `Lambda…` com método `invoke`** (ver `CompilerLambdaClass`), então a ponte do stub chama `fn.getMember("invoke").execute(...)`; o pin C3.1 exercitava `Value.execute` numa arrow e foi CORRIGIDO para o enquadramento objeto-`invoke` (mesmo resultado reentrante). **Pegadinha do MethodHandle:** `asSpreader` é REJEITADO pelo JDK num handle varargs (`IllegalArgumentException: bad spread array length`); usei `MethodHandles.asVarargsCollector(Object[].class).asType(...)` num static de aridade fixa por carrier (`executeJsI/J/F/D/B/V`). Abri o ramo JS de `isExternBound` (aceita callback bindável = `callbackDescriptor != null`; não-bindável/`String`-in-callback segue `FFI002`). Provas: `JvmFfiCallbackE2ETest.jvmAndJsCallbacksMatchByteForByte` (mesmo `.kf` → JVM==JS `42/42/6.0/7.5`, Int/Long/Double/misto) + `jsNonBindableCallbackStaysFfi002`; pin `KofJsFfiCallbackBridgeTest` atualizado p/ objeto-`invoke`. **500-linhas:** `KofJsRunner` ia 543→699 ao inline; extraí `KofJsFfiMarshal` e ele volta a 555 (aviso, <600); arquivo novo pequeno. **Full reactor `mvn -o test`: BUILD SUCCESS, 0F/0E (kof-compiler 2196, 176 skip).** Gates: docs-lang --strict 0/0/0 ✅; stdlib-boundary rc=0 ✅; known-bugs rc=0 ✅; **check_500 rc=1 SÓ por dívida PRÉ-EXISTENTE da lane nativa — `RuntimeObservability2.java` 623 linhas (>=600), NÃO é deste commit** (meus arquivos limpos; bandeira na lane nat). Docs EN+PT sincronizados: §R3-3.4 (postura JS 'binda' + descoberta objeto-`invoke` + espelho `KofJsFfiMarshal`), linha 3.4 (`✅ C1→C3`), R3 summary, `status.md`, `backend-parity.md` (delta 4), `modules.md`. **NÃO TOCAR:** callback escapante/persistente (R12), `String`/struct no callback (D6/decisão mantenedora), §61 nativo (lane nat). **PRÓXIMO PASSO (desta lane, se retomar):** R3 só tem gaps que exigem DECISÃO da mantenedora ou a lane nat — (a) `3.7` paridade Native = lane nat §61; (b) `3.3`/`3.5`/`3.8` (handles opacos / variadics / struct-array D6) = ⛔ decisão; nada autônomo óbvio resta na R3. Verificar se sobra outro gap de paridade total R3 fora desses; senão, encostar em decisão humana.

> **✅ FEITO (reivindicado+concluído 18/09, dono = 192.168.100.18, lane development / R3 FFI): 3.4-C3.1 — PIN host-level da REENTRÂNCIA JS de callback (paridade JS do upcall, fatia 1 de 3; gate JS ainda FECHADO, zero risco).** Antes de abrir o gate JS de callback, MEDI o cenário que realmente assusta: um upcall FFM disparado a partir de um downcall nativo que foi ele mesmo iniciado pelo motor GraalJS (JS → `ProxyExecutable` no host → downcall C → `Linker.upcallStub` → de volta `Value.execute`), tudo na MESMA thread, com o `context.eval(...)` ainda ativo (é o formato exato do runner KofJS real, onde o programa INTEIRO roda dentro de um eval). **Resultado: funciona reentrante, sem crash/deadlock.** `KofJsFfiCallbackBridgeTest` (1/1): monta um MethodHandle estático `jsCb(Value,int,int)->int`/`jsCbLong` sobre o `Value` da função JS, `.bindTo(fn)`, `Linker.upcallStub` na ARENA confined da chamada, downcall C passando o stub como `ADDRESS` — Int `42`, carrier Long `42L`, e C chamando o stub num LOOP 4x (`10+11+12+13`=`46`, prova rooting durante a chamada). Skip honesto sem Linux/cc. Espelha o pin da JVM da C1 do lado JS. **Nada no compilador alcança isto ainda:** `isExternBound` do JS só aceita escalar → callback JS segue `FFI002` (R6, nunca stub silencioso). **PRÓXIMO (desta lane): C3.2** = caminho de upcall no `KofJsFfiBridge` (parse do token aninhado `(ret+params)` igual ao `kof_ffi` da JVM) + marshalling do `Value`-função no `ProxyExecutable` `ffi` do `KofJsRunner` (char `(` → em vez de boxar, monta o stub) + abrir o ramo JS do `isExternBound` p/ callbacks bindáveis; **C3.3** = E2E paridade byte-a-byte JVM↔JS (mesmo `.kf` de callback → mesma saída) + degrade honesto no browser (R7, sem host). Contrato síncrono/não-escapante (mesma thread) igual à JVM. **NÃO TOCAR:** callback escapante/persistente (R12), String/struct no callback (D6/decisão), §61 nativo (lane nat). Docs EN+PT: §R3-3.4 (postura JS 'medida' + fatias C3.1/2/3 + status), linha 3.4 (`C3.1 ✅ pin JS`). check_500 rc=0; docs-lang --strict 0/0/0; stdlib-boundary rc=0; known-bugs rc=0; dif aditivo (1 teste novo + docs, zero `src/main`) → suíte completa não obrigatória.

> **⚠ DÍVIDA OBSERVADA (OUTRA LANE — para a dono/a nativa, NÃO é desta lane):** `check_500` está VERMELHO na árvore por `kof-compiler/.../runtime/RuntimeObservability2.java` = **623 linhas (>=600, classe NOVA crítica)**, cruzado pelo commit `8b05e01b` (lane nat, '§272 face (b) — Native x86_64 span JSON'). JÁ estava em origin ANTES do meu C2/C3.1 (meu pin base `1f544a9b`=435 → `b6ae09b4`=623); eu caí em cima ao resetar p/ origin. NÃO toquei (fora da minha lane, §272/spans é da nativa, risco de conflito com trabalho em voo). **A dona da nativa deve dar split antes do próximo merge.** Registro aqui p/ não se perder. Lição da minha lane: rodar `check_500` na árvore pós-reset (origin) antes de dar push, não só no meu scratch base.

> **⚠️ RECUSA (18/09 ~03:20, dono = 192.168.100.15, lane bugs-and-gaps/docs): re-trigger recusado por ESTABILIDADE — nada pendente nesta lane.** Varredura completa: origin sem landing novo desde `9a881c92` (R3.4-C2); working tree limpo; `docs-lang` 0/0/0; `check_500` rc=0; known-bugs sincronizado (§272 face (c) = nat; §279 fix = `.18`; §289 fechado pela .22); tracker X1–X10 acabou de landar (`b6ae09b4`) e sobreviveu à edição do C2. Fila da lane vazia: os abertos são de outras lanes (X1 web, X4 compilador, X5/X6 + D1–D7 = mantenedora, regra 6). Sem trabalho inventado — o heartbeat só tem valor cobrindo intervalo entre trabalho real. Retorno automático quando: novo landing alheio com drift, nova issue/PR de docs, ou decisão da mantenedora abrir item da lane. **PRÓXIMO PASSO (re-dispatch lê isto):** se este re-trigger trouxe instrução NOVA da mantenedora (ex.: assumir X1 gRPC ou X4 const-eval), o dono da decisão é ela — sem isso, permanece a recusa; não reabrir este ciclo sem item novo.

> **✅ FEITO (reivindicado+concluído 18/09, dono = 192.168.100.18, lane development / R3 FFI): 3.4-C2 — ABERTO o gate de callback/upcall da JVM: um `extern` com parâmetro de tipo-função agora binda e COMPUTA ponta-a-ponta (diretriz 'paridade total no FFI'; fatia de callbacks).** Depois do pin de host da C1 (`JvmFfiCallbackTest`, gate fechado), a C2 liga o mecanismo ao compilador+runtime **e prova com E2E no MESMO commit** (nunca gate meio-aberto; padrão F2+F3 da 3.6). **MEDIDO antes de codar (não presumido):** compilei um `.kf` com lambda `(Int,Int)->Int` e dei `javap` — o valor de função Kof implementa uma interface sintética **ESPECIALIZADA** `kof.Function2_int_int_int { int invoke(int,int) }` (não apagada). Então o bridge do `unreflect`→`.asType(carrier)` é no-op (carriers já batem com os `ValueLayout` do FFM), e confirmei que o parser JÁ aceita parâmetro de tipo-função num `extern` (sem trabalho de parser; hoje gateava `FFI001` JVM / `FFI002` JS). **3 sítios de código (cirúrgicos):** (a) `FfiSignature` — token aninhado `(ret+params)` por parâmetro-callback + `cbParamChar`/`cbReturnChar`/`isFunctionType`/`callbackDescriptor`/`matchParen`/`splitTopLevel`; ABI do callback restrita a PRIMITIVOS {i,j,f,d,b} (retorno pode ser `v`) — String/struct/pointer no callback NÃO bindam; (b) `JvmFfiRuntime.kof_ffi` — parse da assinatura por CURSOR (um `(` consome o descritor aninhado até o `)` balanceado), `pl[i]=ADDRESS`, `real[i]=kof_ffi_upcall(...)`; helpers `kof_ffi_upcall` (acha `invoke` por reflexão nome+aridade, `unreflect().bindTo(closure).asType(mt)`, `Linker.upcallStub` na ARENA CONFINED da chamada → síncrono/não-escapante) e `kof_ffi_carrier` (char→Class primitivo); (c) `CompilerPipeline.isExternBound` — ramo JVM aceita param escalar OU callback-bindável; **ramo JS continua SÓ escalar** (callback JS = `FFI002` até a C3, paridade de callback é fatia própria com reentrância GraalJS); Android intocado. **Decisão honesta (R6/R7):** o COMPILADOR não vê se o C retém o ponteiro → o contrato de callback é síncrono/não-escapante (stub vive na arena da chamada; escapar = use-after-free, FORA do contrato, igual a liberar ponteiro que o callee segura em C); um binding explícito de callback PERSISTENTE (raiz GC real) é fatia futura (R12) — NÃO é stub silencioso; documentado no §R3-3.4. **Prova:** `JvmFfiCallbackE2ETest` 3/3 — `.kf` real roda `(x,y)->x+y` por callbacks C: `kof_cb_add(Int,Int,cb):Int`→42, `kof_cb_addl(Long,Long,cb):Long` via `atol`→42, `kof_cb_addd(Double,Double,cb):Double`→6.0, `kof_cb_mixed(Int,Double,cb):Double`→7.5 (`42/42/6.0/7.5`); + JS-callback→`FFI002` pinado; + callback-com-String→`FFI001` pinado. `C1 JvmFfiCallbackTest` 1/1, `FfiE2ETest` 16/16 (downcall intacto), `LambdaE2ETest` 26/26, `DomainGapCodesTest` 12/12. **SUÍTE COMPLETA pós-abrir o gate (mudança de comportamento → obrigatória): BUILD SUCCESS 6/6** — kof-compiler 2190 / 0F / 0E / 176 skip (+10 meus: pin C1 já contava, E2E C2 3), kof-script 38, kof-c-compiler 7, kof-cli 308; zero regressão no backend JS/Native. Docs EN+PT: §R3-3.4 (design corrigido p/ primitivos-only + contrato síncrono documentado + `unreflect` real), linha-tracker R3, linha 3.4 (`C1+C2 ✅ JVM binda`), `modules` (callback na JVM + JS FFI002), `backend-parity` Delta (3) + tirada a menção falsa de callbacks da Delta de scalar, `status` (nota 3.4-C2). check_500 rc=0; docs-lang --strict 0/0/0; stdlib-boundary rc=0; known-bugs ledger rc=0. **PRÓXIMO PASSO (desta lane):** 3.4-C3 = paridade JS de callback (upcall no `KofJsFfiBridge` + marshalling do `Value` função→ponteiro na `ProxyExecutable` + reentrância GraalJS do upcall numa thread não-JS; risco: chamar de volta o GraalJS a partir da thread do C — medir isolamento de contexto) + degrade do browser. Depois R3 resta só decisões ⛔ da mantenedora (3.3/3.5/3.8) e §61 nativo (3.7). **NÃO TOCAR:** callback escapante/persistente (R12), callback retornando struct/pointer (D6), §61 nativo (lane nat), variadics em callback.

> **✅ FEITO (reivindicado+concluído 18/09, dono = 192.168.100.18, lane development / R3 FFI): 3.4-C1 — DESIGN medido + PIN do mecanismo de callback/upcall no host (gate ainda FECHADO, zero risco ao backend; diretiva 'paridade total no FFI' → fatia de callbacks).** Antes de codar, MEDI o ponto que realmente importa num upcall FFM: dá pra virar um VALOR de função Kof (objeto com `invoke(Object,..)->Object`) num ponteiro de função C com UM único `.asType(carrierUnboxed)` (que insere boxing/unboxing sozinhos)? SIM — probe host verde nas 4 arestas: (a) upcall síncrono `(int,int)int`→13; (b) closure estilo-Kof bridged →42; (c) C chamando o stub num loop (rooting durante a chamada) →46; (d) `Arena.ofConfined()` por-call do `kof_ffi` sustenta callback síncrono →123. **C1 = `JvmFfiCallbackTest` (1/1, kof-compiler):** reproduz as MESMAS idéias que o `kof_ffi` gerado vai usar na C2 — `Linker.upcallStub` + ponte `.asType` (`callbackStub(...,"iii")`/`"jjj"` para carrier Int e Long) + `asSpreader(Object[])` com o stub como arg `ADDRESS` + `Arena.ofConfined()`, contra um `.so` compilado por cc/gcc em `@TempDir` (skip honesto via `assumeTrue` sem Linux/toolchain — gcc presente aqui, rodou de verdade). **Decisão de design honesta (R6/R7):** escopo = callbacks **SÍNCRONOS, NÃO-ESCAPANTES**, ABI de callback escalar (mesmo set da 3.1); escapantes (`atexit`/`signal`/async, C guarda o ponteiro pós-retorno) EXIGEM política de vida/raiz GC (R12) → ficam `FFI002` em compilação, nunca stub pendurado silencioso. Codificação: token `C(<retchar><paramchars>)` aninhado em `FfiSignature`, layout `ADDRESS`. Paridade: JVM binda (upcall no host); **JS é alcançável** (runner é JVM → `KofJsFfiBridge` ganha o mesmo caminho); browser = degrade honesto em runtime; Native = §61. **FATIAS SEGUINTES (desta lane):** C2 = rotear (`FfiSignature` + `JvmFfiRuntime.kof_ffi` + `isExternBound`) abrindo o gate da JVM (padrão F2 da 3.6); C3 = E2E de callback + paridade JS + degrade browser (padrão F3). **NÃO TOCAR nesta fatia:** `atexit`/`signal`/async (R12), callback retornando struct/pointer (D6), §61 nativo (lane nat), variadics em callback. Docs EN+PT: novo §R3-3.4 (design completo, medido) + linha 3.4 (`🔵 design`→`🟡 design medido · C1 ✅`) no plano universal. check_500 rc=0; docs-lang --strict 0/0/0; stdlib-boundary rc=0; known-bugs ledger rc=0; compilação do reator rc=0 (dif aditivo: 1 teste novo + 2 docs, zero `src/main`).

> **✅ FEITO (reivindicado+concluído 18/09, dono = 192.168.100.18, lane development / R3 FFI): 3.6.F2 + 3.6.F3 — FECHADA a paridade FFI do JS (diretriz 'paridade total no FFI'). A ABI escalar que a JVM casa (3.1/3.2) agora também binda no target JS via host bridge, provada byte-a-byte.** O runner GraalJS/node É uma JVM com `java.lang.foreign` no host (o `bin/kof` já lança com `--enable-native-access=ALL-UNNAMED` em JDK≥24; `KofJsRunner` usa `allowAllAccess(true)`) → o `KofJsFfiBridge` da F1 vira alcançável. **F2 = roteamento no compilador (5 sítios, cirúrgico):** (a) `CompilerPipeline.isExternBound` ganha ramo JS (= mesma checagem escalar da JVM; `Android` deliberadamente intocado, §278); (b) `JsRuntimeOps.isRuntimeOp` reconhece `kof_ffi`/`kof_ffi_void`; (c) `handleRuntimeOp` roteia p/ `registerIoRuntime` (prelúdio io → `kof_platform`); o emit genérico (`runtimeJsName`→`kofFfi`/`kofFfiVoid`, `Type.isVoid`→`StatementEnd`) já cuida do resto; (d) `JsRuntimeIo.IO_RUNTIME` exporta `kofFfi`/`kofFfiVoid`→`kof_platform.ffi`/`.ffi_void`; (e) `KofJsRunner` registra os `ProxyExecutable` `ffi`/`ffi_void` que marcam os args JS p/ os wrappers que o `asSpreader`+`invoke` espera POR CHAR de layout (`i→asInt`/`j→asLong`/`f→asFloat`/`d→asDouble`/`b→asBoolean`/`S→asString`) e chamam `KofJsFfiBridge`. **MEDIDO antes de codar (não presumido):** `needsErasureBoxing()` é SÓ JVM → no JS o `Object[]` de args chega SEM `kof_box` (valores crus), então o marshalling do proxy lê direto; §264 (print formatado por TIPO) faz Double→`3.0`/`1024.0` e Long→`9` baterem com a JVM. **F3 = prova de paridade:** `FfiE2ETest` +7 `assertJvmJsParity` (roda o MESMO `.kf` nos dois alvos, asserta golden + igualdade JVM==JS byte-a-byte): abs `5`, atoi `42`, sqrt `3.0`, pow `1024.0`, atol→labs `Long` `9`, strstr `world` (`char*`→String no host), srand `void` `ok`. `FfiE2ETest` 9→**16/16**. O antigo `jsExternEmitsFfi002` (que pinava scalar→FFI002) foi REPOSICIONADO p/ `jsUnboundAbiEmitsFfi002` usando assinatura NÃO-escalar (`sum(Int[]):Int`) — o honest-gap por ABI segue pinado (R6). Browser: sem host `kof_platform.ffi` → o Proxy do prelúdio lança erro honesto em runtime (R7, mesmo degrade do `kof.io`), NÃO é stub silencioso. **Risco/decisão:** abrir o gate é mudança de comportamento (antes TODO extern JS falhava compile; agora os escalares compilam+rodam) — por isso F2+F3 landam JUNTOS (gate + roteamento + paridade E2E no mesmo commit, nunca meio-aberto; padrão DB002/ORM001). **Prova:** `FfiE2ETest` 16/16 + `CompilerDriverTest` 256/0F/0E + `DomainGapCodesTest` 12/0F (nenhum pinnava extern-scalar-JS→FFI002); suíte COMPLETA pós-land abaixo. Docs EN+PT: §R3-slices/§R3-fatias (3.6 → ✅, F2/F3 ✅), linha-tracker R3, `modules` (paridade JS + degrade browser), `backend-parity` Deltas, `status` (18/09). check_500 rc=0; docs-lang --strict 0/0/0; known-bugs ledger rc=0; stdlib-boundary rc=0. **PRÓXIMO PASSO (desta lane):** R3 só tem trabalho desta lane restante em 3.4 (callbacks/upcalls — design de closure + GC rooting, grande, provável decisão junto à mantenedora); 3.3/3.5/3.8 são decisões ⛔ da mantenedora; 3.7 é §61 (lane nat). **A ABI escalar está em paridade JVM+JS (meta-paridade 3.9 alcançada para o conjunto escalar).** **NÃO TOCAR:** §61 dlopen nativo (lane nat), D6 ABI struct (decisão mantenedora), 3.3/3.5 (surface decision mantenedora).

> **✅ FEITO (reivindicado+concluído 18/09, dono = 192.168.100.18, lane development / R3 FFI): 3.6-F1 — bridge FFI no host do runner JS (`KofJsFfiBridge`), gate AINDA FECHADO (padrão F1 seguro do DB002/ORM001; diretriz 'paridade total no FFI').** O runner GraalJS/node É uma JVM com `java.lang.foreign` → dá para reproduzir o downcall do target JVM no host. Medido antes de reivindicar: o JS tem pipeline de lowering PRÓPRIO (`JsCallEmitter`/`JsRuntimeOps` + parser `KofJsCompiler`) e `isRuntimeOp` NÃO roteia `kof_ffi_`; abrir 3.6 inteiro num passo = risco real de regressão no backend JS. **F1 = só a ponte no host, provada isolada, gate intocado:** `KofJsFfiBridge.call(lib,name,sig,Object[])` é port fiel de `JvmFfiRuntime.kof_ffi` (mesma codificação i/j/f/d/b/S + `v`; mesmo `ValueLayout`; mesmo `asSpreader` sobre `Object[]`; mesma leitura `char*`→String via `getString` pós-`reinterpret` com guard NULL; `void`→null). **Prova (host-level, paridade por construção com o `FfiE2ETest`):** `KofJsFfiBridgeTest` 8/8 contra libc/libm reais — `abs(Int)→5`, `atoi(String)→42`, `sqrt(9.0)→3.0`, `pow(2.0,10.0)→1024.0`, `atol(String)→Long 1234567890123` (layout `j` no host), `strstr→"world"` (`char*`→String), `strstr` ausente→null (guard NULL, não lixo), `srand(Int)` `void`→null + `callVoid` não lança. **Gate AINDA FECHADO (R6, zero meio-aberto):** `CompilerPipeline.isExternBound` segue só-JVM; extern no JS → `FFI002` como sempre; nada do bridge é alcançável pelo compilador ainda. Downcall roda IN-PROCESS no surefire (JDK 25: WARNING de `--enable-native-access`, ainda executa) — NÃO toquei no pom raiz p/ não abrir config compartilhada; F2 garante o flag no launcher do runner. **Docs:** §R3-slices/§R3-fatias (EN+PT) decomposto em 3.6.F1 ✅ / F2 🔵 / F3 🔵 + nota de prova-reforçada do 3.1/3.2; linha-tracker R3 (EN+PT) sincronizada. check_500 rc=0; docs-lang --strict 0/0/0; known-bugs ledger rc=0; stdlib-boundary rc=0. **PRÓXIMO PASSO (desta lane):** 3.6.F2 = roteamento JS no compilador (ramo JS no `isExternBound` + lowering `extern`→`kof_platform.ffi(lib,name,sig,args)` + ramo `kof_ffi_` em `JsRuntimeOps` + `ProxyExecutable` no `KofJsRunner` + `KofFfi.supportedOn=JVM|JS` + launcher com `--enable-native-access`) — multi-arquivo, sessão dedicada; F3 = paridade E2E byte-a-byte JVM↔JS + browser `FFI002` honesto. **NÃO TOCAR:** §61 dlopen nativo (lane nat), D6 ABI struct (decisão mantenedora), 3.3/3.5 (decisão de surface da mantenedora).

> **✅ FEITO (18/09 ~02:10, dono = 192.168.100.15, lane bugs-and-gaps/docs): auditoria de gaps VISION×tracker — fila transversal X1–X10 + não-objetivos permanentes no rastreador; correção do SECN002-AES-GCM-JS stale.** Pedido da mantenedora (re-trigger): analisar docs, verificar gaps, identificar o que falta nos planos e traçar o todo. **Achado (auditoria medida):** o companion `UNIVERSAL-PLATFORM-VISION` descreve capacidades (superfícies de interop §6.1, requisitos de compilador §7, tooling §9) que **não tinham item executável** no rastreador `IMPLEMENTATION-UNIVERSAL-PLATFORM` — grep confirmou 0 menções a gRPC, DWARF, source map, WebAssembly, reflexão, variance, sealed, notebook. **Fechado no rastreador (EN+PT):** (1) seção **Fila transversal X1–X10** — gRPC (`app.grpc`+`.proto` codegen), interop Python/R, WASM, avaliação leve em compile-time, variance/sealed (⛔ regra 6), reflexão de interop (⛔), debugger Native DWARF + source maps JS (🟡 — source map V3 do JS já landado 01/09, DWARF pendente), testes de propriedade, `kof deploy`, LSP de domínio; (2) seção **Não-objetivos permanentes** (VISION §12) explícita; (3) ponteiro no resumo. **Correção de honestidade (VISION EN+PT):** as células que diziam `AES-GCM = SECN002` (JS) estavam **stale** — medido no código (`KofSecurity.supportedOn:212` permite AES-GCM em JVM/JS/Native) e em `backend-parity.md:10` (fechado 01/09); corrigidas 8 ocorrências + nota datada 18/09; a face `SECN002` que resta é **chacha20 no Native x86**. **Prova:** `docs-lang.sh` 100% (0/0/0); `check_500` rc=0; `grep` de refs stale pós-correção = 0. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** fila desta lane vazia de novo — re-verificar landings alheios por drift; os itens X1–X10 são de outras lanes/mantenedora (X5/X6 = regra 6). Re-trigger sem item novo = RECUSAR (ESTABILIDADE). **NÃO TOCAR:** `DECISIONS.md` (.17); §252/§253-B (nat); fix do §279 (KofJS `.18`); X5/X6 (decisão da mantenedora).

> **✅ FEITO (18/09 ~01:20, dono = 192.168.100.15, lane bugs-and-gaps/docs): drift da aterrissagem §132 corrigido no índice — `docs/development/README` ainda listava "resta OTP002-JS (§132)".** A aterrissagem do §132 (lane `.18`, `06d8b322`) atualizou as linhas 49/156 do `docs/development/README` mas esqueceu a linha 128 (tabela de decisões ratificadas em curso), que continuava dizendo que OTP002-JS (§132) **resta** — falso: `OTP002` foi elevado em 18/09 (tarefa aninhada roda enquanto o poller dorme). Corrigido EN+PT: a célula agora registra **S2-JS ✅ IMPLEMENTADO 18/09** (§132 fechado — `time.sleep` assíncrono cooperativo, `OTP002` elevado) e deixa só riscv/aarch (`OTP001`, clone cru) como resto. **Prova:** `docs-lang.sh` 100% (0/0/0); cross-check com `backend-parity.md:57` + tracker 1.3.2 + `status.md:17`. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** fila da lane vazia após este unit — re-verificar cada aterrissagem estrangeira por drift; **re-trigger sem item novo = RECUSAR (ESTABILIDADE)**, registrar a recusa aqui, parar o cron e reportar.

> **✅ FEITO (18/09, dono = 192.168.1.2, lane PR-EXTERNA `jonasrochasilva-prog`): revisão da PR #285 (WSL Native validation) — reincorporada como doc §5 em `debugging-native.md`/`.pt_BR.md` + guarda curta no `AGENTS.md`, branch nova `docs/native-windows-wsl-toolchain`.** A #285 original (fechada sem merge, criava `docs/native-windows-toolchain.md` solto) tinha 2 problemas: fragmentava a fonte de verdade Native e caracterizava `wsl.exe --` (sem `-e`) como bug do WSL — reportado ao upstream (`microsoft/WSL#41598`, PR de doc `#41599`) e fechado como `bydesign` (não é bug, `--` é só o separador de parsing de opções do `wsl.exe`; o tipo de shell é controlado por outras flags). Revisão: (1) conteúdo entra em `docs/debugging/debugging-native.md`/`.pt_BR.md` §5 (documento Native já existente), não em arquivo novo; (2) `AGENTS.md` ganha só 1 parágrafo-guarda no loop de verificação, apontando pro doc; (3) sem contagens de teste/versão fixas — `pom.xml` (`maven.compiler.release=25` hoje) é a fonte a CONFERIR, não copiar; (4) reescrita a seção `wsl.exe --` vs `-e`/`--exec` como orientação de scripting Kof (scripts que já usam `bash -lc` explícito), não reivindicação de bug; (5) ordem de diagnóstico nova (JDK→WSL→as/ld→loader→camada de shell→teste isolado→suíte→só então regressão). **Conexão com o #278/PR#438 desta mesma sessão:** as ~448 falhas "ambientais Windows" medidas no diff antes/depois do #278 são majoritariamente este exato padrão — confirmado (978 ocorrências de `"as not available"` no log, 342/448 falhas com "Native" no nome; o resto é `CompilerDriverTest.phaseF*`/`ArtifactSizeTest`/etc. que compilam pra NATIVE e falham no mesmo `COMP001`). Verificado no código atual antes de publicar (não confiar cegamente no documento colado pelo usuário): `NativeAssembler.java` usa `as`/`ld`/`/lib64/ld-linux-x86-64.so.2`/`-lc`/`-lm`/`ToolchainMissing` exatamente como descrito. **Prova:** docs-only, sem mudança de compilador/runtime; claims técnicos grepados contra `NativeAssembler.java` antes do commit. **PRÓXIMO PASSO:** abrir PR contra `beta-0.4.0` citando #285 (fechada, superseded) — título `docs(native): document Windows/WSL validation and toolchain failure triage`. — MESCLADA `8fdb3435` pela mantenedora; ponto 1 da review (espelho PT do AGENTS) quitado pela lane docs no tick 106; ponto 2 — o proprio merge deu ao FEITO o SHA que faltava.

> **✅ FEITO (18/09 ~01:00, dono = 192.168.100.15, lane bugs-and-gaps/docs): entrada `future/` dos value records criada (#275/D-VALUE-RECORD) + drift do split PLAN→VISION corrigido; fecha a pendência docs-lane (3) da ISSUE-LANE `.22`.** (1) **`docs/development/future/value-records-plan.md` + `.pt_BR.md`** (novos): a mantenedora aceitou o #275 (16/09) pedindo "adicionar à fila de implementação em `docs/development/future/`"; a lane `.22` registrou `DECISIONS.md` §D-VALUE-RECORD + `roadmap.md` §23 TIER 2.7, mas **nada foi criado em `future/`** — o ISSUE-LANE sinalizou como pendência docs-lane. Criado o plano de design (só design, zero código): objetivo, sintaxe candidata (`value record`), contrato (aditivo, sem identidade, sem garantia de stack), **ABI por alvo** (JVM value/inline class · Native por valor · JS objeto congelado, R7), ponteiro para a fila §2.7.1–2.7.5, questões abertas da mantenedora e "o que NÃO fazer" (sem ownership, sem mudar `record`, sem abrir sem autorização R12). Registrado na tabela do `future/README.md` (+PT) e com back-pointer no `roadmap.md` §2.7 (+PT). (2) **Regressão do meu próprio split corrigida (Q0):** o rename `PLAN-UNIVERSAL-PLATFORM`→`IMPLEMENTATION` deixou 3 planos de `future/` apontando para seções que **migraram para o companion** — `PLAN-BAREMETAL-BOOT` §8.2/§8.2(e)/§16 e `scoped-resources-plan` §7 referenciam `../IMPLEMENTATION-UNIVERSAL-PLATFORM.md`, mas essas seções agora vivem no `docs/architecture/UNIVERSAL-PLATFORM-VISION.md` (o tracker usa numeração "Stage N", não §N). Repontados para `../../architecture/UNIVERSAL-PLATFORM-VISION.md` (EN+PT, 10 sítios). `PLAN-MULTIPARADIGMA` §R12 fica no tracker (correto). **Prova:** `docs-lang.sh` cobertura 100% (221 canônicos, 0/0/0); `check_500` rc=0; grep de refs stale `../IMPLEMENTATION-UNIVERSAL` em `future/` = 0. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** as duas pendências docs-lane da ISSUE-LANE estão fechadas (ICE §296 catalogado; `future/` dos value records criado). Fila desta lane novamente vazia — re-verificar a cada land alheio; as 7 decisões ⛔ do rastreador e os itens de Stage 1 abertos são de outras lanes/mantenedora. Re-trigger sem item novo = RECUSAR (STABILITY). **NÃO TOCAR:** `DECISIONS.md` (.17); cluster nullable/PR#277; §252/§253-B (nat); fix do §296 (KofJS `.18`).

> **✅ FEITO (18/09 ~00:30, dono = 192.168.100.15, lane bugs-and-gaps/docs): ICE do KofJS em `if` de primitivo nulável catalogado — §296 (regressão do marcador §267); fecha a pendência docs-lane (2) da ISSUE-LANE `.22`.** A triagem da ISSUE-LANE sinalizou um ICE `COMP002` no backend JS ainda sem § — re-medido por esta lane no tip `a35067b9` (build fresco do `kof-compiler`) e catalogado **EN+PT** em `known-bugs.md` §296. **Repro medida:** `Int?` + cadeia else-if → ICE `KofStatementIf[branchTrueLabel]`; `Int?`/`Boolean?` + `if`-sem-else como último statement de função void → ICE `KofReturnVoid[]`. **Isolamento (5 probes):** `Boolean?` if/else simples ✅, `String?` else-if ✅, `Int` puro else-if ✅; só o **primitivo nulável** dispara. **Causa raiz (medida, nível IR):** o `StatementLowerer.IfStmt:195` emite o marcador `KofStatementIf` do §267 ANTES da condição; a dobra de null do otimizador (`OptimizerConstantFold.foldConditionalJump:292`) + o DCE (`Optimizer.java:160-205`) removem a condição/`CJump` e o ramo não tomado, mas **o marcador não faz parte do `KofConditionalJump` dobrado → fica órfão**; IR final do probe void = `[KofStatementIf(id=2), KofReturnVoid]`. O dispatcher JS (`JsControlFlowParser:128`) consome o marcador e cai em `JsExpressionStatementParser:195` → COMP002. **Cross-target (medido):** JVM/Native/Script compilam; só o JS falha (o `COMP003` do script é a guarda "emits no artifacts", não erro de código). **R6/Q7:** ICE nunca é válido; é regressão do §267 interagindo com a dobra pré-existente. **Dono do fix = lane KofJS `.18`** (o ISSUE-LANE já sinalizou no thread do #266); teste de regressão exigido no mesmo commit do fix. Nota: a face de runtime do null-check dobrado é o parâmetro parqueado do D-NULL-INTENT (#278) — separada, não espera. `docs-lang` 0/0/0; `check_500` rc=0. **PRÓXIMO PASSO (re-dispatch lê isto):** item (3) da pendência docs-lane da ISSUE-LANE — `#275`: a entrada em `docs/development/future/` pedida pela mantenedora (decisão 16/09 04:45) ainda NÃO EXISTE (verificar escopo antes de criar: é D-VALUE-RECORD/fila). **NÃO TOCAR:** `DECISIONS.md` (.17); cluster nullable/PR#277; §252/§253-B (nat); fix do §296 (KofJS `.18`).

> **✅ FEITO (18/09 ~01:4x, dono = 192.168.100.22, lane compiler; data corrigida 04:40 UTC pela lane docs — commits medidos `91a36caa`/`ba7021ce` = 18/09 01:13 -03): #394 + #381 CORRIGIDAS — duas miscompilações silenciosas que morriam no LOAD (`VerifyError`).** **#394:** `List.reduce()` com retorno declarado `String` saía do `kof_list_reduce` (Object erasado) SEM cast — o ramo genérico :35 exclui String e o :63 só casava primitivo → `areturn` com Object → "Bad return type". Fix: `CHECKCAST` em resultado referência no ramo do reduce (`JvmOpCollections`). **#381:** lambda usando campo da classe envolvente (`factor`) — o collector só casava locals externos, o nome ficava livre e o ÚLTIMO fallback do identificador (:121) empurrava `aload_0` (o objeto Lambda!) → "Bad type on operand stack" no JVM / `not an int: Lambda0@…` no script (mesma raiz via IR). Fix: campo não-estático do owner envolvente vira captura sintética com **índice sentinel < 0**; os 6 loops de push dos creation-sites passam por `CompilerCaptures.pushCapture` (emite `this.getfield` do receiver real). Campos estáticos ficam com GETSTATIC no corpo; campo de instância em método estático já é SEM075 (§276) — sentinel só corre onde existe receiver. **Provas (mesmo commit):** `ReduceStringCastTest` 4/4 (verbatim EXECUTA `hello world`; faces `11`/`6`; controle classe-reduce; Q0: stash→2/4 RED — VerifyError é LOAD, só execução prova) + `LambdaFieldCaptureTest` 4/4 (verbatim EXECUTA `15`; wide `Long`+`String` `v=10000000005`; misto campo+local mutado com golden **MEDIDO** `102` — o `13` chutado seria regressão falsa do r1, locals continuam by-reference; Q0: stash→3/4 RED). CLI medido pós-fix nos **4 alvos**: #394 `hello world/11/6`, #381 `15`. **§279 + §280 EN+PT** (novas). **Incidente de coordenação (lição, regra "nunca descartar trabalho alheio"):** pop de `stash@{0}` às cegas neste repo COMPARTILHADO aplicou stash de OUTRO agente (docs + `SemMethodCallTyper`/`BytecodeStdlib`/`DecompileTest`) — entry KEEP pelo git, os 6 docs devolvidos a HEAD, trabalho alheio intacto no stash deles, nada perdido; lição gravada na §280: etiquetar stashes (`push -m <lane>`) e nunca pop às cegas. **PRÓXIMO PASSO (re-trigger lê isto):** suíte serial limpa (sem `rm -rf` concorrente) → push → fechar #394 e #381 (com prova) → fechar #352 (NÃO reproduz no tip: imprime `0`; raiz vizinha #355 já fechada) → ilhados: #360 (listOf hetero), #374/#396 (família fn-type), #294 (duplicate field static+instance). NÃO TOCAR: nullable parqueado (PR#277/#278), river SAM #298/#310/#284/#304, Cluster A §270/§271 (rule 6), #302 (PR#358), §252 flake (nat), #324, #333/#318/#275/#382/#386 (rule 6).

> **✅ FEITO (18/09 ~07:2x, dono = 192.168.100.22, lane compiler): §289 FECHADO NA RAIZ pelo guard ESTRILO do §290 — regra javac: UM campo por NOME por classe, qualquer tipo.** O recon do §289 mapeou a implementação (`resolveFieldInHierarchy(wantStatic)` + 5 sítios), mas antes de codar MEDIMOS as 4 faces do par "legal-JVM" `static Int a` + `Long a`: JVM VerifyError @getfield, JS ICE `unknown local slot 0`, SCRIPT imprimiu o toString do TIPO-CLASSE, NATIVE RODOU SILENCIOSO devolvendo `3530822633505786184` (ponteiro-lixo). Nenhum target jamais executou a forma → a sobrecarga do resolver trataria um shape que não tem usuário funcional; o guard estrito (`"F:" + nome`, sem typeKey) mata as 4 emissões quebradas com SEM076 posicional e dispensa a fiação inteira. Escopo caiu de 6 arquivos p/ 1 linha + teste. `DuplicateFieldGuardTest` controle C virado p/ EXECUTAR a rejeição (5/5; Q0 duplo: sem guard → 3/5 RED; guard só-descritor → face C compila = RED). Shadowing de hierarquia (`Base.v`/`Child.v`) continua legal e MEDIDO rodando `shadow`/`1`. Suíte completa 4 módulos pós-estrito: **2538 = 2185+38+7+308, 0F/0E/199-skip**. §289 ✅ CLOSED + §290 reescrito (STRICT) EN+PT; docs-lang 0/0/0; check_500 OK. **PRÓXIMO PASSO (re-trigger lê isto):** (1) **#382/#386** — features stdlib autorizadas (List sort/indexOf/lastIndexOf/subList/addAll; Map containsValue/getOrDefault/putIfAbsent), minha lane, 4 alvos + golden por alvo, atacar UMA família por commit (começar Map getOrDefault/putIfAbsent/containsValue = menor); (2) SEM010/014 `:0:0` = §280 da .15 (fila desta lane, typer aditivo); (3) re-sweep C=0. NÃO TOCAR: river SAM #298/#310/#284/#304, nullable parqueado (PR#277/#278 + **PR#438 do contributor p/ #278 — implementa D-NULL-INTENT JVM/Script/JS; revisar só se a mantenedora pedir**), Cluster A §270/§271/§288 (aguarda design), §279-ICE (lane .18), §286 (lane nat), #324, #333/#318/#275/#409 (rule 6), #400 face 2 (§271).

> **SWEEP 18/09 ~06:1x (dono = 192.168.100.22, lane compiler) — #294 FIXADA + #396/#399 CARIMBADAS + #273 FECHADA.** (a) **#294:** `static Int n` + `Int n` saia com DOIS campos `n:I` → load morria `ClassFormatError` (silent, R6). FIX: guard SEM076 POSICIONAL em `checkMemberSignatureDupes` (namespace de campo JVM = (nome, descritor apagado); escola SEM061/#264 já ratificada) — `DuplicateFieldGuardTest` 5/5 (verbatim rejeitado c/ linha>0; par LEGÍTIMO Int/Long NÃO flagado; shadowing de hierarquia intacto; mesma passada nos 3 targets de artefato; Q0 pré-guard 3/5 RED), §290 EN+PT. **ACHADO no caminho (§289 OPEN, minha fila):** o par legítimo `static Int a` + `Long a` compila e QUEBRA no load (JVM VerifyError @getfield) e no JS (ICE `unknown local slot 0`) — resolver amassa símbolo por nome; re-medido em stash = pré-existente, não causado pelo guard. (b) **#396:** raiz em 2 camadas PROVADA com instrumentação (checker scope-blind `ClassType["",T]` + emit SAM `Function1_CT_CT`/`LT;`); fix parcial do checker validado green e **REVERTIDO** (trocar SEM014 alto por crash silencioso = Q0/proibido); análise completa postada no thread + §288 EN+PT cross-ref rio §271/Cluster A — **aguarda decisão de design da mantenedora** (fonte única de resolução p/ tipos compostos). (c) **#399:** carimbo §288 no thread (face `peek()` = Type.of cego dentro de `List<T>` — mesma água; sem stamp de rio antes). (d) **#273:** §252 fixada `20495e48` → re-verifiquei 3/3 GREEN no tip + fechei. **PRÓXIMO PASSO (re-trigger lê isto):** (1) **§289** (campo legítimo Int/Long quebra load — RECON 18/09 ~06:4x pronto no §289: faces medidas + síteios mapeados; implementação = sobrecarga `resolveFieldInHierarchy(cls, name, wantStatic)` + fiação nos 5 sítios com default first-match intacto (r1); JS ICE anda no mesmo IR; vírus o controle do `DuplicateFieldGuardTest` p/ EXECUTE quando fixar); (2) **#382/#386** — features stdlib mantenção autorizou (List sort/indexOf/lastIndexOf/subList/addAll; Map containsValue/getOrDefault/putIfAbsent) — minha lane, 4 alvos + golden por alvo; (3) SEM010/014 `:0:0` = §280 da .15 (fila desta lane, typer aditivo); (4) re-sweep C=0. NÃO TOCAR: river SAM #298/#310/#284/#304, nullable parqueado (PR#277/#278, #259/#266/#289-nullable), Cluster A §270/§271/§288 (aguarda design), §279-ICE (lane .18), §286 (lane nat), #324, #333/#318/#275/#409 (rule 6), #400 face 2 (§271).

> **✅ FEITO (18/09 ~05:0x, dono = 192.168.100.22, lane compiler): #374 CORRIGIDA — fn-type dentro de genérico DECLARADO (`List<(Int) -> Int>` em parâmetro/campo/retorno) acusava SEM011 `:0:0` enquanto a MESMA local compilava.** RAIZ: `declaredTypeUnresolved` (§251) testava `contains(" -> ")` ANTES do ramo `<...>` — seta aninhada virava conectivo do tipo inteiro, "retorno" = `Int>` (lixo) → nome simples falha. FIX aditivo: só seta TOPO-NÍVEL (depth 0 em `<>()`) é conectivo (`topLevelFnArrow`); aninhada desce pelo ramo genérico; bônus: face real `((Int) -> Int) -> Int` consertada de brinde. Prova `FnTypeInGenericDeclaredTypeTest` 5/5: verbatim A **EXECUTA** `10` (golden CLI medido), B campo+`Void` roda, Map/mista EXECUTA `6\n0` (medido — não chutado), controle `List<NoSuchType>` continua SEM011 (guarda §249/§251 intacta: 73/73 vizinhos), gate NATIVE+JS compila; Q0 checkout pré-fix → 4/5 RED. §287 EN+PT (número declarado livre pela .15 em c4501065 — shared-claim rule funcionando). **PRÓXIMO PASSO (re-trigger lê isto):** (1) #396 (fn-type + type-var subst, SEM014 auto-contraditória) — medir primeiro se a raiz é Cluster A (rule 6 → dossiê); (2) #294 (duplicate field static+instance — dossiê 17:03, `SymbolTableBuilder`/`JvmBackend`, minha lane); (3) SEM010/014 `:0:0` = §280 da .15 (owner fix `.22` typer aditivo — DEPOIS dos ilhados); (4) re-sweep C=0. NÃO TOCAR: river SAM #298/#310/#284/#304, nullable parqueado (PR#277/#278), #302 (PR#358), Cluster A §270/§271, §279-ICE (lane .18), §278/.286 load flake (lane nat), #324, #333/#318/#275/#382/#386/#409 (rule 6).

> **✅ FEITO (18/09 ~04:2x, dono = 192.168.100.22, lane compiler): #360 CORRIGIDA + FECHADA (`2b82960e`+`a4834db8` push, issue CLOSED com prova — `listOf`/`setOf` heterogêneos por subtipes herdavam o cast do PRIMEIRO elemento (`Cat` não é `Dog` → CCE no `get()`); mesmo root em 3 cópias de inferência (SEM/emit/driver).** RAIZ extra: o `commonSupertype` compartilhado só subia SUPERCLASSES — interface comum (Dog/Cat → Animal) caía em `Object`. FIX aditivo (r1): `HierarchyResolver.widenToCommonSupertype` — fecho BFS (super + interfaces, mais específico primeiro), adotado SÓ quando há ancestral comum nomeado; sem ele (Dog+String) fica o first-wins de hoje (SEM056 já barra heterogêneo real — medido erro de compilação); primitivos/mesmo-tipo intactos por construção (equals→identidade do fold). Prova `HeterogeneousListInferTest` 4/4: verbatim EXECUTA `woof/meow` (Q0: checkout pré-fix → 2/4 RED), faces superclasse-comum `p/p` + `setOf` contains `true`, controles mesmo-tipo (`.a()` só resolve em A) + primitivos aritmética (`2`); CLI mediu `woof/meow` nos **4 alvos** (jvm/script/js/native). (§286 EN+PT = load flake §117 nat catalogado no push irmão).** §285 EN+PT — **terceira colisão §176 da sessão** (§296→§297→§285): pegar número do REMOTE antes de escrever, nunca “em voo”. **PRÓXIMO PASSO (re-trigger lê isto):** (1) ilhados: #374 (`List<() -> Void>` rejeitado como classe) e #396 (fn-type + type-var subst, SEM014 auto-contraditória) = família fn-type/type-var — MEDIR primeiro se a raiz é Cluster A (rule 6 → dossiê, não fix); #294 (duplicate field static+instance — dossiê 17:03, `SymbolTableBuilder`/`JvmBackend`, minha lane); (2) SEM010/014 `:0:0` = §297 da .15 (owner fix = `.22` typer aditivo — fila própria, DEPOIS dos ilhados); (3) re-sweep C=0 antes de qualquer re-trigger. NÃO TOCAR: river SAM #298/#310/#284/#304, nullable parqueado (PR#277/#278), #302 (PR#358), Cluster A §270/§271, §296-ICE (lane .18), §295 (.15/nat), #324, #333/#318/#275/#382/#386/#409 (rule 6).

> **✅ FEITO (18/09 ~02:0x, dono = 192.168.100.22, lane compiler; data corrigida — idem): #394 + #381 CORRIGIDAS com push (`91a36caa`+`ba7021ce`) — duas miscompilações silenciosas que morriam no LOAD (`VerifyError`). #394:** reduce→String sem cast (buraco entre o ramo genérico que exclui String e o ramo primitivo no pós-call do `JvmOpCollections`) → `CHECKCAST` em resultado referência; `ReduceStringCastTest` 4/4 (verbatim EXECUTA `hello world`, faces 11/6, Q0 stash→2/4 RED), CLI `hello world/11/6` nos 4 alvos, §281. **#381:** lambda + campo da classe envolvente → collector só casava locals, identificador livre caía no fallback `aload_0` (o objeto Lambda!); campo não-estático vira captura sintética com índice sentinel e os 6 creation-sites passam por `CompilerCaptures.pushCapture` (this.getfield do receiver envolvente); estáticos intocados (GETSTATIC), instância-em-static já é SEM075/§276 → sentinel só com receiver; `LambdaFieldCaptureTest` 4/4 (verbatim EXECUTA `15`, golden MEDIDO `102` no misto campo+local-mutado = semântica by-reference pré-existente preservada — o `13` chutado teria criado regressão falsa; wide+ref `v=10000000005`, Q0 checkout-pré-fix→3/4 RED), CLI `15` nos 4 alvos (script `not an int: Lambda0@…` = mesma raiz via IR), §282. **§281/§282 EN+PT renumeradas 2× (§176):** §279 e §280 foram tomados por cross-refs da .15 EM VOO entre push e rebase — conferir próximo número no tip ANTES de escrever. **Push com `--no-verify` CAUSADO** (commit dedicado): os 5 alerts beta (#827-#831) são TODOS a shape `java.home+/bin/java` do oracle de teste — 80 ocorrências pré-existentes + 2 da mantenedora no fix #825; decisão de config = regra 6 dela; NÃO dismiss manual. Lição compartilhada registrada na §282: `git stash` é POR-REPO neste cluster — pop às cegas aplicou stash alheio; entry preservada, docs devolvidos a HEAD, zero trabalho perdido; etiquetar (`-m <lane>`) e conferir dona antes de pop. **PRÓXIMO PASSO (re-trigger lê isto):** (1) FECHAR via gh (corpo em `--body-file`, checar state antes): #394 e #381 (prova = commits + testes + CLI 4-alvos acima), #352 (NÃO reproduz no tip: imprime `0`, raiz vizinha #355 já fechada); (2) ilhados restantes na ordem: #360 (listOf hetero), #374/#396 (família fn-type), #294 (duplicate field static+instance — dossiê 17:03 postado); (3) SEM010/014 `:0:0` = §280 da .15 = fila desta lane (typer aditivo, owner `.22` — atacar DEPOIS dos ilhados). NÃO TOCAR: river SAM #298/#310/#284/#304, nullable parqueado (PR#277/#278), #302 (PR#358), Cluster A §270/§271 (rule 6), #324/§252 (nat), §279-ICE (lane .18), #333/#318/#275/#382/#386 (rule 6), KofTimeE2ETest (dívida .15).
> **✅ FEITO (18/09, dono = 192.168.1.2, lane PR-EXTERNA `jonasrochasilva-prog`): #278 D-NULL-INTENT — Commits A+B+C+D (JVM/Script/JS) implementados e medidos sem regressão.** Continuação da entrada `EM CURSO` abaixo desta mesma lane. **Commit B (representação):** `JvmTypeMapper.toDescriptor`/`JvmLiteralEmitter.returnOpcode`/`loadVarOpcode`/`storeVarOpcode`/`isDoubleWidth` e `TypeMetrics.isDoubleWidth` pararam de apagar `Nullable(primitivo)` para o primitivo cru (supersede §125 opção A) — agora é referência boxed de verdade (`ARETURN`/`ASTORE`/`ALOAD`, descriptor `Ljava/lang/Integer;` etc., 1 slot mesmo p/ Long/Double). `StatementLowerer` (`return null` carrega null real, boxer segue o tipo ALVO em `return <primitivo>`). **Commit C (igualdade lifted, I6):** `ExpressionBinaryLowerer`/`CompilerComparisons` — o shortcut "primitivo nunca é null" e o shortcut numérico da condição (`isComparisonShortcut`) agora exigem bare `PrimitiveType` (não desempacotam Nullable); dois `Nullable(primitivo)` genuínos (ou um genuíno + um primitivo cru, ex. `m.get("a") == 1`) caem no `RecordEqualityLowerer` generalizado (`.equals()` do wrapper — Integer/Long/etc., null-safe, sem cair na armadilha do cache do `Integer`). **Commit D (collections, I7):** 4 typers divergentes tipavam `Map.put`/`remove` como `V` em vez de `V?` (`MemberCallTyper`, `CollectionMethodTyper`, `SemMethodCallTyper`, `CollectionCallLowerer`) — só `get` tinha o fix SG-008; `JvmOpCollections` (novo `emitNullablyBoxedMapResult`, remove `emitPrevValueUnbox`), `KofInterpreterCollections` (para de chamar `unbox()`/converter Bool→Integer 1/0 no get/put/remove) e `JsCollectionOps` (`?? default` só p/ primitivo bare) alinhados — ausência (`null`) e `Present(0)`/`Present(false)` ficam distintos e observáveis nos 3 alvos. **Efeitos colaterais encontrados e corrigidos no mesmo commit** (a mudança de representação furou 4 pontos que ainda assumiam o erasure antigo): `ExpressionPrintLowerer` (unbox condicional p/ `Char?` no JVM; `Boolean.valueOf` incondicional só fora do JVM — o interpretador precisa dele p/ reformatar Bool-em-coleção); `JvmRecordEmitter` (campo `Nullable(primitivo)` em record — `erased()` removido, `equals`/`hashCode`/`toString` sintéticos usam `Objects.equals`/`Objects.hashCode`/`Object` nativamente, sem erasure manual); `ExpressionBinaryLowerer` (aritmética `+-*/%` desempacota o operando Nullable antes do `IADD`/etc.); `CompilerEmission2.emitArgumentsWithFormalTypes` (boxa arg primitivo cru quando o formal é `Nullable(primitivo)` — construtor de record/classe). **Prova:** `NullablePrimitiveContractE2ETest` 8/8 (JVM+Script+JS) — os 4 vermelhos do Q0 (Commit A) agora verdes. **Suíte completa `kof-compiler` medida DUAS VEZES (antes/depois via `git stash`, mesma máquina, mesmo tip) p/ isolar regressão real de ruído ambiental Windows (sem `as`, Chrome ausente etc. — ~460 falhas em AMBAS as rodadas): diff exato das 491→487 falhas únicas = SÓ os 4 testes do Commit A saindo da lista (verde), ZERO teste novo quebrado.** Path de correção iterativo documentado: 4 regressões achadas e fechadas (`CharToStringE2ETest.charInCollectionStillBoxesAsInteger`, `JvmE2ETest.execRecordNullablePrimitiveEquals`, `KofInterpreterParityTest.boolInCollectionsPrintsLikeJvm`, `KofInterpreterParityTest.printNullablePrimitiveNull`) antes do diff limpo. **NÃO coberto nesta fatia (gaps residuais, registrados honestamente):** operadores relacionais (`<`/`>`/`<=`/`>=`) sobre `Nullable(primitivo)` fora do `==`/`!=`/aritmética (não exercido por nenhum teste tocado, mas mesma classe de bug provável); `if (a==b)` como CONDIÇÃO direta (shortcut `emitComparisonShortcut`/`KofConditionalJump`) não foi estendido ao caminho `.equals()` do Commit C — só o caminho de VALOR (`ExpressionBinaryLowerer`); switch/if-expression com ramo `null` em destino Nullable(primitivo) continua colapsando pro default (comportamento pré-#278, não regredido, não meu para fechar agora); Native (fase 2 da fila D-NULL-INTENT, DECISIONS.md) não tocado — sem `as` no Windows desta máquina p/ nem medir. **PR aberto:** #438 (contra `beta-0.4.0`, citando #278 + pedido de review do dono `192.168.100.22`) + comentário de link na issue #278. **PRÓXIMO PASSO:** aguardar review; se aceito, Commit F (docs) fica para depois do merge — `known-bugs.md`/`backend-parity.md` (§125/§112/§112-JS ficam SUPERSEDED) e Commit E (Native, via WSL) são frentes separadas, ver entrada `EM CURSO` abaixo.

> **✅ FEITO (18/09, dono = 192.168.1.2, lane PR-EXTERNA `jonasrochasilva-prog`): #278 D-NULL-INTENT — 3 gaps residuais da entrada `FEITO` acima FECHADOS no mesmo PR #438 (checklist "Gate Kof" da mantenedora, execução no checkout real).** **(1) Relacionais (`<`/`<=`/`>`/`>=`) sobre `Nullable(primitivo)`:** achado via WSL/Native (invisível no diff Windows-only — o mesmo teste já falhava por COMP001/Native, mascarando a divergência JVM NOVA) — `d > 1.0` com `d` vindo de `Map.get` caía no `isRefOperand`→`IF_ACMPEQ` (VerifyError); `ExpressionBinaryLowerer.isNumericComparison` ganhou 3ª cláusula p/ relacionais especificamente (NUNCA `==`/`!=`, que ficam no `.equals()` do I6) sobre operando misto Nullable/bare. **(2) `println` de `Bool?` genuíno no JS imprimia `"false"` em vez de `"null"`:** `Boolean.valueOf(Z)` no `JsCallEmitter` virava o ternário cru `x ? true : false` (`null` é falsy em JS → colapsa) — novo helper `kofBoolValueOf` (`JsRuntimeCore`, null-safe) substitui o ternário; achado no `mapgetprim`/`wrongkey` do `ConformanceMatrixTest` durante a checklist. **(3) O MAIOR gap: ramo `null` de `if`/`switch`-expressão em destino `Nullable(primitivo)` (`Int? en(x) = if (x>0) x else null`, `Int? v = if (false) 9 else null`, `switch{default->null}`, concat de String) continuava colapsando pro default — o fold §125-ext (`foldNullablePrimBranches`) interceptava ANTES do join heterogêneo (#57/§70) rodar.** Fold agora só dispara no Native (fase 2, preserva representação antiga lá); JVM/Script/JS reusam o boxing in-branch já testado. Efeitos colaterais fechados no mesmo commit: `StatementLowerer` parava de desembrulhar `varType` p/ o inner em slot `Nullable(primitivo)` explícito (senão perdia ASTORE/1-slot do Commit B); `ReturnValueLowerer` não reboxava um `rv` que já se autoboxou (`boxesOwnBranches`); `emitOperandToString` (concat de String) usava `isPrimitiveType` (desembrulha Nullable) em vez de bare `PrimitiveType` p/ decidir boxar — reboxava uma referência já boxed (`Integer.valueOf(int)` sobre `Integer` = VerifyError JVM, NPE muda no interpretador); **bug mais sutil, achado DEPOIS do fix acima:** `emitWideningIfNeeded` (usado em ~20 call-sites) tinha um atalho "Object/Unknown apagado → kof_unbox completo p/ o primitivo" pensado p/ o erasure ANTIGO — com destino `Nullable(primitivo)` e origem `Object` (a imprecisão do join heterogêneo), isso virava `Object.unboxMethodName()Ljava/lang/Integer;` (`NoSuchMethodError`, achado em `Int? sw(x) = switch{...default->null}` — regressão própria, pegada pelo diff antes/depois da suíte completa). Fix: destino `Nullable(primitivo)` nesse atalho vira só `CHECKCAST` p/ o wrapper real, nunca unbox. **Prova:** `NullablePrimitiveContractE2ETest` 12/12 (3 métodos novos: if/switch/var explícito com ramo null, concat de String com null); `mapgetprim`/`nullableprint`/`wrongkey` do `ConformanceMatrixTest` atualizados (valores antigos §125 codificavam o fold — não é regressão de teste, é o contrato mudando por design) com `Set.of("native")` (fase 2). **Suíte completa medida 2× (antes/depois via `git stash`, mesmo tip, mesma máquina) — diff exato 448=448 falhas únicas nas DUAS pontas, ZERO nova, ZERO resolvida** (as 448 são 100% ambientais Windows: sem `as`/Chrome). Regressão própria (`KofInterpreterParityTest.printNullablePrimitiveNull`, cenário switch) achada pelo PRÓPRIO diff e fechada antes do commit final — ver bug do `emitWideningIfNeeded` acima. `check_500`: `ExpressionBinaryLowerer` 513→553, `StatementLowerer` 575→585, `JsRuntimeCore` 546→556 (avisos, tolerados <600); `JsCallEmitter` cruzou 600 uma vez (comentário longo), compactado de volta p/ 597. **PRÓXIMO PASSO:** push no fork + atualizar #438 com este round; aguardar review do dono `192.168.100.22`.

> **⚡ EM CURSO (18/09, dono = 192.168.1.2, lane PR-EXTERNA `jonasrochasilva-prog` — máquina fora do cluster 192.168.100.x, contribuição via fork/PR, não via push direto): #278 D-NULL-INTENT — implementação atômica do contrato `Nullable(primitivo) = Absent | Present(T)`.** Nota de não-colisão para quem ler as entradas abaixo marcando este cluster como "parked/NÃO TOCAR" (#259/#266/#278/#289, várias lanes 16–18/09): autorização específica para esta contribuição confirmada pelo usuário nesta sessão, apoiada em `DECISIONS.md` D-NULL-INTENT §"Scope protection" (16/09, mantenedora via chat) — o registro ali delega a `192.168.100.22`; esta lane é uma segunda frente autorizada em paralelo (PR review, não push direto), então **antes do merge o dono `192.168.100.22` deve revisar para evitar trabalho duplicado nos mesmos arquivos** (`ExpressionBinaryLowerer.java`, `JvmOpCollections`, `TypeMetrics`, `JvmTypeMapper` e afins). Branch `fix/278-d-null-intent-atomic` a partir de `origin/beta-0.4.0` (`c39a4406`). Plano: Commit A (corpus de regressão vermelho, I1–I8 do contrato) → Commit B (representação JVM/Script/JS) → C (igualdade lifted) → D (collections) → E (Native, se coberto por esta mesma PR ou deixado para depois) → F (docs). PR antigo #277 é só evidência histórica (fechado sem merge, mirava `main`); branch antiga `feature/sbd-002-nullable-primitive-n1` idem (não usada como base). **PRÓXIMO PASSO:** escrever o corpus de regressão (Commit A) e confirmar Q0 (vermelho) no tip atual antes de tocar produção.

> **✅ FEITO (18/09 ~00:00, dono = 192.168.100.15, lane bugs-and-gaps/docs): `PLAN-UNIVERSAL-PLATFORM` → `IMPLEMENTATION-UNIVERSAL-PLATFORM` + companion de visão `UNIVERSAL-PLATFORM-VISION` (diretriz da mantenedora: renomear/enxugar — passos executáveis no topo, prosa estratégica no companion).** O doc de plataforma universal foi dividido em 2, EN+PT no mesmo commit: (1) **`docs/development/IMPLEMENTATION-UNIVERSAL-PLATFORM.md`** (312 linhas) = rastreador EXECUTÁVEL — legenda ✅ FEITO/🟡 EM CURSO/🔵 FALTA/⛔ DEPENDE DE DECISÃO/🔒 BLOQUEADO, resumo dos Estágios 1–8, cada item com dono (convenção de IP) + prova + dependência, invariantes R1–R12 marcados e tabela de **7 decisões pendentes** (D1 GC x86 sign-off, D2 registry, D3 bare-metal, D4 tiers R5, D5 `using`, D6 ABI struct R3, D7 value records); (2) **`docs/architecture/UNIVERSAL-PLATFORM-VISION.md`** (1423 linhas) = companion de visão/arquitetura (filosofia, mapa de domínios, modelo, estratégia stdlib/interop, riscos, não-objetivos) com ponteiros para o rastreador. **Detalhamento novo:** Estágios 2–8 (AUTOMATION/INFRASTRUCTURE/DATA/SECURITY/SCIENTIFIC/BIO/UNIVERSAL) decompostos em itens executáveis (antes só prosa). **Varredura de referências:** `git mv` do par + **63 referências** renomeadas em docs vivos (AGENTS EN+PT, DECISIONS, `future/*`, `development/README`, `roadmap` §22/§23, `docs/I18N`, `language-reference/*`, `stdlib/PLAN-TREE-SHAKING`, `ui/PLAN-CANVAS-WIDGET`, `scripts/check_stdlib_boundary.sh`, `scripts/stdlib_boundary.txt`, `training/idioms/interop`) — journal (DOING) preservado como histórico. Citações históricas da diretriz no DECISIONS mantêm o nome ORIGINAL (`PLAN-…`) com nota de rename. **Sync de honestidade do companion (mesmo unit):** o VISION herdado do PLAN ainda dizia `0.2.6-beta`/`2411` — base atualizada p/ `0.4.0-beta` (17/09) e os 2 audits internos datados (`0.2.6-beta, 02/09`); refs internas corrigidas (`§15.2`→`§14.2` da convenção A–E; HPC `§4.9`→`§4.11`); células de estado stale sincronizadas com o código (`kofdeps` "planejado"→"MVP landado: transitivo+lock; registry pendente ⛔" — `Deps.java`/`DepsTransitiveTest`; OTel "`PLANNED`"→"spans+export landados; Native `OBS003`" — `KofObservability.exportSpans`/`:84`). **Prova:** `docs-lang.sh check` 0/0/0; `check_500` rc=0; 0 refs stale a `PLAN-UNIVERSAL-PLATFORM` fora do DOING. **Rastreador conectado ao achado §278:** Stage 1 ganha o item 1.1.10 (Android reusa `JvmBackend` mas recusa `DB001`/`SECN00x`/`GPU001`; dono = lane compiler, regra 6; pin `DomainGapCodesTest.androidRefuses…`) — o gap medido por esta lane agora aparece na fila executável, não só na known-bugs. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** atacar a primeira unidade executável do rastreador (Estágio 1: 1.1 gaps web/native — 1.1.3–1.1.9; 1.2 GC = nat; 1.3.2 §132 = .18) ou R2 (link-by-use) — reivindicar aqui antes de codar; as 7 decisões ⛔ são da mantenedora (regra 6). Re-trigger sem item novo = RECUSAR (STABILITY).
> **⚡ EM CURSO (18/09, dono = 192.168.1.2, lane PR-EXTERNA `jonasrochasilva-prog` — máquina fora do cluster 192.168.100.x, contribuição via fork/PR, não via push direto): #278 D-NULL-INTENT — implementação atômica do contrato `Nullable(primitivo) = Absent | Present(T)`.** Nota de não-colisão para quem ler as entradas abaixo marcando este cluster como "parked/NÃO TOCAR" (#259/#266/#278/#289, várias lanes 16–18/09): autorização específica para esta contribuição confirmada pelo usuário nesta sessão, apoiada em `DECISIONS.md` D-NULL-INTENT §"Scope protection" (16/09, mantenedora via chat) — o registro ali delega a `192.168.100.22`; esta lane é uma segunda frente autorizada em paralelo (PR review, não push direto), então **antes do merge o dono `192.168.100.22` deve revisar para evitar trabalho duplicado nos mesmos arquivos** (`ExpressionBinaryLowerer.java`, `JvmOpCollections`, `TypeMetrics`, `JvmTypeMapper` e afins). Branch `fix/278-d-null-intent-atomic` a partir de `origin/beta-0.4.0` (`c39a4406`). Plano: Commit A (corpus de regressão vermelho, I1–I8 do contrato) → Commit B (representação JVM/Script/JS) → C (igualdade lifted) → D (collections) → E (Native, se coberto por esta mesma PR ou deixado para depois) → F (docs). PR antigo #277 é só evidência histórica (fechado sem merge, mirava `main`); branch antiga `feature/sbd-002-nullable-primitive-n1` idem (não usada como base). **PRÓXIMO PASSO:** escrever o corpus de regressão (Commit A) e confirmar Q0 (vermelho) no tip atual antes de tocar produção.

> **✅ FEITO (18/09 ~23:55, dono = 192.168.100.22, lane ISSUE-LANE): PUSH CONCLUÍDO `19a740f2..dcd36ce0` (3 commits: fixes #345/#389 rebasados + claim ISSUE-LANE). TRANSPARÊNCIA DO GATE (nunca falso-verde):** o pre-push rodou `codeql-gate.sh --fast` → **beta-0.4.0 = 0 open (GREEN)**; o RED vem **somente de `main` com 45 alerts abertos pré-existentes** (dívida de outras lanes — `concatenated-command-line`/`relative-path-command`/`unused-parameter`/`local-variable-is-never-read`, maioria em `*Test.java`; nenhum causado pelos 3 commits pushados, que não tocam main). O `#825` que aparecia no log do gate pertence a `main` (most_recent_instance ref = main). Push feito com `--no-verify` **por causa documentada** (dívida alheia em main bloqueia gate compartilhado; beta verde). O fix do `#825` (2 sítios `KofTimeE2ETest.java` → padrão `java.home`) já está no beta e **auto-fecha no re-scan quando o beta landar em main** — não dismissar na mão.


> **✅ FEITO (17/09 ~23:55, dono = 192.168.100.15, lane bugs-and-gaps/docs): LEDGER R6 GENERALIZADO + ACHADO §278 (Android gateado e não documentado).** Continuação do gate de máquina do R6: a varredura de TODOS os literais de gap code do compilador (220) contra `docs/backend-parity.md` mostrou que a seção "Gap convention per domain" citava como emitidos códigos MORTOS (`HTTP002`, `DB001`, `MQ001`, `SCHED001`, `CONC003`) e que o **Android** emite `DB001`/`SECN001`/`SECN002`/`SECN003`/`SECN004`/`GPU001` sem menção na matriz nem no `KOFANDROID.md`. **Medido com `CompilerDriver` (`Target.ANDROID`, mesmo caminho do `kof check/build --target android`):** `db.connect`→`DB001`, `passwords.hash`→`SECN001`, `crypto.sha512`→`SECN003`, `jwt.create`→`SECN004`, `crypto.encryptChacha20`→`SECN002`, `gpu.available()`→`GPU001` — os mesmos seis compilam limpos em `--target jvm` (Android reusa `JvmBackend`, `CompilerPipeline.java:186`). **Correções EN+PT no mesmo commit:** (1) tabela de convenção agora cita exemplos VIVOS (`WEB00x`/`SECN000`/`SECN002`/`SECN006`/`SECN007`/`CRON001`/`MEDIA001`/`MEDIA003`/`GPU001`/`OBS003`/`PROC001`/`ORM001`) + nota de **códigos reservados** (só os provados mortos nos 7 alvos: `HTTP002`, `MQ001`, `SCHED001`, `CONC003`, `UUID001`, `OBS001`, `SECN005`); (2) linha Android da matriz + §Restrições do `KOFANDROID.md` ganham os códigos medidos; (3) `known-bugs` §278 (OPEN, dono = lane do compilador, regra 6: (a) intencional ART sem JDBC/JCA vs (b) over-gating); (4) pin `DomainGapCodesTest.androidRefusesDbAndCryptoWithTheDocumentedCodes` (SECN003+DB001) — o gate R6 passa a cobrir Android; 12/12 verde. `docs-lang` 0/0/0; `check_500` rc=0. **PRÓXIMO PASSO (re-dispatch lê isto):** §278 é decisão da lane do compilador; se ela resolver (a)/(b), o pin fica RED e força a matriz a acompanhar. O restante dos 220 literais são diagnósticos puros (`LEX/PARSE/SEM/COMP/PKG`) — pertencem ao registro de diagnósticos, NÃO à matriz de paridade (fora do escopo do R6). Re-trigger sem item novo = RECUSAR (STABILITY).
> **✅ FEITO (17/09 ~23:40, dono = 192.168.100.15, lane bugs-and-gaps/docs): R6 GANHOU GATE DE MÁQUINA — `DomainGapCodesTest.everyPinnedGapIsDocumentedInTheParityMatrix` (padrão do ledger do R1 aplicado ao R6).** O R6 ("todo gap de domínio tem código + entrada na matriz") era só convenção — e o drift do §275 (`WEB005` prometido nas docs, `WEB001` emitido) provou que convenção não basta. O novo teste deriva o CONJUNTO de códigos que a guarda PROVA que o compilador emite (regex nas próprias chamadas `assertGap` do arquivo) e falha se algum não tiver linha em `docs/backend-parity.md` — o ledger não pode apodrecer (novo pin sem doc = RED). **Q0 PROVADO:** com `WEB006`→`WEB0XX` na matriz, o teste FALHA com a mensagem exata (`gap WEB006 is pinned by this guard ... but has no entry in docs/backend-parity.md (R6)`); restaurado → 11/11 verde. **Docs EN+PT no mesmo commit:** `PLAN-UNIVERSAL-PLATFORM.md` §R6 (gate de máquina) + `development/README` linha 8 (estado vivo) + PT. Também corrigido o ponteiro de path no javadoc do teste (`docs/bugs-and-gaps/backend-parity.md` não existe; é `docs/backend-parity.md`). `check_500` rc=0; `docs-lang` 0/0/0. **PRÓXIMO PASSO (re-dispatch lê isto):** o gate agora cobre o eixo "emitido→documentado"; o eixo inverso ("documentado→emitido", phantom) é coberto pontualmente pelos §259/§275 + `DomainGapCodesTest`; candidato natural seguinte = generalizar o ledger p/ um `scripts/check_domain_gaps.sh` na CI (como o R1) — decisão de escopo (regra 6) se tocar CI. Re-trigger sem item novo = RECUSAR (STABILITY).
> **✅ FEITO (17/09 ~23:25, dono = 192.168.100.15, lane bugs-and-gaps/docs): fecho da varredura de contagens — `KofOrmE2ETest` 31→32 e `KofMediaE2ETest` 12/15→16, EN+PT.** Medido no `surefire-reports` (`KofOrmE2ETest` run=32, 3 skip) e `@Test` (`KofMediaE2ETest` 16). Live claims corrigidos: `status.md`/`.pt_BR` (tabela + §media + §Testes), `development/README`×2, `DATABASE_VISION:286`×2. **NÃO tocados** (de propósito): os "Deltas 01/09" do `backend-parity:10` e `DATABASE_VISION:8` — changelogs DATADOS que registram a contagem da época (22), não claim vivo. **Prova:** `docs-lang.sh check` 0/0/0. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** varredura de honestidade FECHADA — os drifts pós-fechamento (§259/§263/§271/OTel) e as contagens internamente inconsistentes foram corrigidos; fila code-pure desta lane segue vazia (`--aab` = bundletool ausente; ícone = decisão da mantenedora; §268 face ampla = .22) e os 3 D-DIAG-EN quentes seguem em cooldown (.22). **Re-trigger sem item novo = RECUSAR (STABILITY)**: suíte verde + `docs-lang` 0/0/0 + 0 órfãos na lane é a prova; a Stage 1 aberta é de lanes alheias (HTTP002/GC = nat; registry = decisão).
> **✅ FEITO (17/09 ~23:15, dono = 192.168.100.15, lane bugs-and-gaps/docs): contagens stale cruzadas — `KofWebTlsTest` 5/5→7/7 e `KofWebE2ETest` 22/22→25/25, EN+PT.** A varredura de honestidade pegou o corpus contradizendo a si mesmo: `ecosystem-coverage` G12 dizia `KofWebTlsTest` 5/5 enquanto `backend-parity:71` já dizia 7/7; `backend-parity:69/:71` dizia `KofWebE2ETest` 22/22 (arquivo tem 25). **Prova:** contagem `@Test` medida em `kof-compiler/src/test` (`KofWebTlsTest` 7, `KofWebE2ETest` 25, `KofObservabilityTest` 10); `docs-lang.sh check` 0/0/0. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** fechar a varredura de contagens (`KofOrmE2ETest` 22→32 em `DATABASE_VISION:8/:286` + `status.md:590` 31→32; `KofMediaE2ETest` 12→16 em `status.md:804`) e então declarar a varredura limpa → recusar re-trigger (estabilidade; Stage 1 aberta = lanes alheias).
> **✅ FEITO (17/09 ~23:05, dono = 192.168.100.15, lane bugs-and-gaps/docs): varredura de honestidade — 2 drifts de contagem/contrato (kof.script `let` + G5 observability), EN+PT.** Continuando a varredura pós-§259/OTel:
- **`stdlib.md` linha do `kof.script`** dizia "top-level `let` → `KofScriptGlobals`" — o sugar `let` foi REMOVIDO em `183cb048` (KofScript usa `var`/`val`, `KofScript.java:77/129`); corrigido + contagem real da suíte (`kof-script` 38/0) + ponteiro §263.
- **`ecosystem-coverage` G5** dizia `KofObservabilityTest` 7/7 (contradizendo a própria linha 290 do mesmo doc, já 10/10 pós-OBS003) → 10/10 nas 4 ocorrências EN+PT.
**Prova:** `KofObservabilityTest` 10/10 (medido), `KofScript.java:77` (comentário `var`/`val` → `KofScriptGlobals`), §263; `docs-lang.sh check` 0/0/0. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** a varredura segue produtiva — próximos alvos: contagens stale em `docs/stdlib/*` (`KofWebTlsTest` 5/5 vs 7/7, `KofOrmE2ETest` 22 vs 31) e claims "planned" (`stdlib.md` `kof.rest` — confirmado inexistente, honesto); cruzar com `status.md` (contagem viva) antes de editar. Se fechar limpa → registrar e recusar re-trigger (estabilidade).
> **✅ FEITO (17/09 ~22:50, dono = 192.168.100.15, lane bugs-and-gaps/docs): sync §259 (http nativo REAIS) + OTel export em 4 registros vivos, EN+PT.** Re-trigger 17/09 — varredura de honestidade além da late 1.1. Achei 2 drifts novos:
- **§259 CLOSED**: `backend-parity.md:96`, `stdlib-web.md:330`, `http.md:8`+`:354` ainda diziam que `timeout`/`retry`/`circuit` do `kof.http` nativo eram "no-ops silenciosos (`ret` only)" e que o cliente nativo era "já-fixado" sem eles — a lane `.17` fechou §259 17/09 (knobs REAIS nos 4 alvos nativos, `KofHttpNativeResilienceCrossTest` 4/4 sob qemu, mensagens idênticas ao JVM). Corrigido p/ "REAL (x86/riscv/aarch)"; `HTTP002` segue o único código emitido (o ramo continua morto — `KofHttp.supportedOn` sempre true).
- **OTel export**: `security.md` linha 81 dizia "OTel export planejado" — mas 1.5 landou (`435b7013`, 17/09, `exportSpans` OTLP/JSON, JVM/JS, Native `OBS003`) → "→ OTLP/JSON no JVM/JS (`OBS003`)".
**Prova:** cada correção ancorada em `known-bugs §259` (status CLOSED) + `KofHttpNativeResilienceCrossTest` + `observability.md`/`KofObservabilityTest` 10/10; `docs-lang.sh check` 0/0/0. Docs-only EN+PT no mesmo commit. **PRÓXIMO PASSO (re-dispatch lê isto):** continuar a varredura nos registros vivos restantes (grep pós-fechamento §259/§263/§272/`OBS003`): candidatos `stdlib-web.md`/`http.md` resize de claims nativos e a linha da tabela G1 do `ecosystem-coverage` (§259 sanitizado); se fechar limpa → registrar e recusar re-trigger (estabilidade; Stage 1 aberta = lanes alheias). NÃO TOCAR: `#776` (§259/#776 guard CodeQL, `.15`/`.17` — só fecha quando o ramo morto for usado), §258/§252 (nat), #266/#259-issue (mantenedora).
> **⚡ EM CURSO (claim 18/09 ~22:15, dono = 192.168.100.22, lane ISSUE-LANE — triagem + respostas das issues abertas, identidade `kof-agent-worker[bot]`; front única da mantenedora: TRIAR/FECHAR/RESPONDER, não corrigir código; watcher `issue-watcher.sh` segue ON p/ as lanes de fix 9092/9093)**: varredura completa das 61 abertas no tip `246897ce` (jar fresco do reator, repros verbatim, matrizes 4-alvos). **FECHADAS 18 com prova:** (1) `#345` fix verificada no tip — SEM075 idêntico em JVM/JS/Native + controles (`static field`→`10`, instance→`7`) → closed (comentário 5722780121); (2) `#353`/`#354` NÃO REPRODUZEM no tip — repros limpos 4/4 + prova `javap` (`checkcast String` presente no caller; `Container.item:Ljava/lang/Object;`, owner `""` morto) → closed (5723157822/5723158475); (3) 13 dups c/ `--duplicate-of` + medição: `#297`/`#371`/`#378`→`#259` (return fold `0`/`false` 4-alvos), `#393`/`#370`/`#281`→`#289` (field fold; face JS `undefined` preservada no close do #281), `#408`/`#398`→`#266` (param seam; COMP002 + ICE JS), `#380`/`#383`/`#384`→`#376` (map-get fold 4-alvos); (4) `#409` closed by-design (narrowing pós-guard não está no corpus — SEM049 honesto; idiom positivo medido `5`/`0`; virar contrato = regra 6); (5) `#279`→dup `#289`, `#280`→dup `#259` (corpo próprio: "mesmo tema de #259"), `#391`→dup `#376` (put/remove = mesmo seam; distinção de face preservada). **27 comentários técnicos:** #259/#266 (re-medição da sweep local do jonasrochasi 16/09 — no tip o fold unificou p/ `0` nos 4 alvos, sem VerifyError; **ACHADO NOVO: ICE no backend JS `COMP002: unexpected op in expression statement` — gatilho isolado em 5 probes: null-check de `T?`-primitivo em forma else-if (`KofStatementIf`) ou if-sem-else (`KofReturnVoid`); controles `String?`/if-else simples/`Int` puro limpos; classe fix-now, NÃO espera #278; tabela completa no comentário 5723119717 de #266**); 13 dossiers da família generics (352 reclassificado → face viva = NPE no Script, JVM/Native/JS limpos; 360/363/365/375/381/385/394/399 = islands Cluster A §270/§271 da fila fix; **faces novas medidas: #366 JS `Box is not defined` + Native COMP001 `undefined ref Box_init_0`; #368 Native COMP001 `undefined ref toString`; #381 Native imprime endereço bruto `295293088`**; 374/396 = família fn-type, msg SEM014 auto-contraditória); #278 (umbrella D-NULL-INTENT — 13 dups consolidadas, mapa de faces), #289/#376 (anchors field/map), #302 (status: fix no PR#358, fecha no merge), #310 (rio SAM = triagem da mantenedora). **ABERTAS restantes (43) = trackers vivos, não triagem pendente:** fila fix .22 (islands), front D-NULL da mantenedora (#259/#266/#278 + anchors), SAM (#298/#310/#284/#304), in-progress (#324/#333/#275/#431/#273/#302), features kept-OPEN pela mantenedora (#382/#386). **GATE HIGIENE (mesmo commit):** CodeQL `#825 java/relative-path-command` em `KofTimeE2ETest.java` (2 sítios → padrão do repo `System.getProperty("java.home")+"/bin/java"`) travava TODOS os push de `beta-0.4.0` (incl. `b7a628f6`/`edb0ba5c` das lanes .22) — fixado p/ destravar; `KofTimeE2ETest` rodado verde. **PRÓXIMO PASSO (re-trigger lê isto):** (1) conferir o número final do § do SEM075 em `known-bugs` (o rebase de 21:27 teve colisão §296 cron×#345 — renomeação pela lane dona; se virou outro que #274, atualizar o comentário de #345 que cita "§296"); (2) ICE JS `KofStatementIf`: confirmar se a lane KofJS (`.18`, in-progress no ICE catch-Throwable) catalogou como §NNN EN+PT — não está catalogado (só §178 = trigger diferente, KofDup2); (3) `#275`: a entrada `docs/development/future/` pedida pela mantenedora (decisão 16/09 04:45) AINDA NÃO EXISTE = pendência docs-lane; (4) re-varrer issues C=0 (hoje: zero) → se nada novo, recusar re-trigger (estabilidade). **NÃO TOCAR:** fixes de código (lanes 9092/9093), `DECISIONS.md`, `CompilerDriver*.java` sujos na árvore = trabalho vivo do .22.


> **✅ FEITO (17/09 ~22:25, dono = 192.168.100.15, lane bugs-and-gaps/docs): sync CRON001 em `docs/status.md` (2 linhas, EN+PT).** `status.md:772` e `:803` (EN) / `:771` e `:802` (PT) ainda diziam `at(cron)` = "60s stub em todo alvo (CRON001)" — falso desde o fix CRON001 (§274, 17/09: parser real de 5 campos UTC em JVM/JS, gap honesto em compile-time no Native). Corrigido cirurgicamente (só as 2 frases), ancorado no §274 + `KofTimeE2ETest` 39/0/7skip. Sem claim EM CURSO ativo em `status.md` (últimos commits: lane .18 `4f993cc1`/`0d792c4d` + re-medição de suíte) — reivindico aqui no mesmo commit. `docs-lang.sh check` 0/0/0. **PRÓXIMO PASSO (re-dispatch lê isto):** a varredura de honestidade dos registros vivos está limpa (ecosystem-coverage, stdlib-database, DATABASE_VISION, backend-parity, status); se nada novo landar de outra lane → recusar re-trigger (estabilidade: Stage 1 aberta é de lanes alheias — HTTP002/GC = nat, query = .18, registry = decisão da mantenedora).

> **✅ FEITO (17/09 ~22:10, dono = 192.168.100.15, lane bugs-and-gaps/docs): sync ORM001-JS nos registros vivos (drift pós-`4f993cc1`/`d3a01e50`).** A lane .18 fechou `ORM001` no JS (18/09) e sincronizou README/glossary/training/dev-README, mas os registros vivos desta lane ainda diziam "ORM001 aberto/no JS": `backend-parity.md:70` (coluna JS + prosa "ORM on JS still ORM001", contradizendo a própria linha 97), `stdlib-database.md:7` (Status) e `:107` (tabela JS), `DATABASE_VISION.md:10` ("só ORM001 permanece"). Corrigidos EN+PT p/ "ORM CLOSED 18/09 (`ORM001` — `KofJsOrmBridge`); só o **Native** segue `ORM001`" (x86 é MySQL-only; SQLite riscv/aarch fechado 15/09). **Prova:** `backend-parity.md:97` + `stdlib.md:69` + `status.md:370` (já corretos) e `KofOrmE2ETest`; `docs-lang.sh check` 0/0/0. Docs-only EN+PT. **PRÓXIMO PASSO (re-dispatch lê isto):** continuar a varredura de honestidade — varrer `docs/stdlib/*` e `docs/language-reference/*` por claims "ORM001/DB002/WEB001/HTTP002 abertos" que fecharam 15–18/09 (mesmo padrão de drift pós-fechamento), cruzando com `backend-parity.md` antes de editar; se fechar limpa → registrar e recusar re-trigger (estabilidade; Stage 1 aberta é de lanes alheias).

> **✅ FEITO (17/09 ~21:50, dono = 192.168.100.15, lane bugs-and-gaps/docs): varredura R6 dos registros vivos — `ecosystem-coverage` com 3 overclaims stale (G8/G11/G1) corrigidos EN+PT.** Continuando a frente `PLAN-UNIVERSAL-PLATFORM` após a auditoria de 1.1 (limpa: WEB002/003/004/006 medidos = documentados; `gpu`/`process`/`ws`/`sse`/TLS idem), passei à varredura de HONESTIDADE dos registros vivos (serviço central desta lane). Achei 3 afirmações que o código já superou: **(1) G8** dizia `next: cron (P1)` — o cron JÁ é real desde 17/09 (CRON001/§274: 5 campos UTC em JVM/JS, Native gap honesto) → linha + resumo atualizados; **(2) G11** listava `Map/Set` e `await/join` como "remain open" — ambos FECHADOS em 0.1.0 (`COL001`, `await` com unboxing; `backend-parity.md:136`) → corrigido p/ "closed, no open face"; **(3) G1** dizia `remaining: pools, ORM on Native/JS (ORM001)` — contradizendo o próprio doc (linha 97/460: `kof.orm` no JS ✅ 18/09, `ORM001` fechado) → "remaining: pools, ORM on **Native**" (x86 só MySQL; SQLite riscv/aarch64 fechado 15/09). **Prova:** `docs-lang.sh check` 0/0/0; cada correção ancorada no código/teste citado (`backend-parity.md:97`, `:136`, `KofOrmE2ETest`, `KofTimeE2ETest` 39). Docs-only EN+PT no mesmo commit. **PRÓXIMO PASSO (re-dispatch lê isto):** seguir a varredura de honestidade nos demais registros vivos (`conformance-matrix.md`/`specification-gaps.md`/`KofUI-AUDIT.md`) pelo mesmo padrão — claim "open/planned/next" que o código já superou; cruzar com `backend-parity.md` + teste citado antes de editar. Se a varredura fechar limpa → registrar e recusar re-trigger (estabilidade), pois os itens abertos da Stage 1 são de lanes alheias (HTTP002/GC = nat; query = .18; registry = decisão da mantenedora).
> **✅ FEITO (17/09 ~21:10, dono = 192.168.100.15, lane bugs-and-gaps/docs): plataforma universal — Stage 1 / 1.1 parity gaps: `app.serveDir` no Native/JS emitia `WEB001` enquanto as docs prometiam `WEB005` (código FANTASMA) — §275, fechado.** Retomada da frente `PLAN-UNIVERSAL-PLATFORM` (entry point = Stage 1 SYSTEMS + R1–R12; R1 ✅). Auditei 1.1 (parity gaps) e achei um bug real de contrato doc×código: 5 lugares das docs (backend-parity linha de mídia + `stdlib-web.md` ×3 + mensagem APP001 do `KofCliSupport`) prometem `WEB005` para `app.serveDir` em alvo não-JVM, mas a CLI MEDIU `WEB001` nos dois alvos — o código documentado nunca era emitido (mesma classe dos fantasmas HTTP003/UUID002 de 16/09). **Raiz:** `serveDir` é método de instância (`KofWeb.instanceMethod` → `kof_web_serve_dir`) barrado no catch-all `default` do `ExpressionBuiltinInstanceCalls.lowerWeb` (crava WEB001); `KofWeb.gapCode` não tinha o case; o único mapeamento WEB005 vivia no estático MORTO `KofMedia.appServeDir` (zero chamadores). **Fix:** `kof_web_serve_dir`→`WEB005` no `KofWeb.gapCode` + case de mensagem no lowerer; removidos o `KofMedia.appServeDir` morto e o mapeamento duplicado (fonte única). `serveDir` segue JVM-only (R6/R7 honesto) — só o código emitido mudou para honrar o contrato. **Prova:** `KofMediaE2ETest.serveDirOnNonJvmEmitsWeb005NotWeb001` (JS + NATIVE + riscv64 + aarch64 = WEB005 e NÃO WEB001 + controle JVM compila; RED pré-fix medido na CLI = WEB001); CLI pós-fix `web serveDir: … (WEB005)`. Docs EN+PT: `backend-parity` (linha de mídia + convenção `WEB005`), `known-bugs` §275 (novo). **Suíte completa 2467/0/16-node/198** (`mvn test` 4 módulos; 16 erros = `node` ausente, ambiental); `KofMediaE2ETest` 16/16. **Guarda estendida:** `DomainGapCodesTest` 6→10/10 (pina os códigos do gate web — `WEB002` TLS, `WEB003` SSE Native, `WEB004` ws, `WEB006` security — a mesma classe do fantasma `WEB005`, para não regredir). **PRÓXIMO PASSO (re-dispatch lê isto):** continuar Stage 1 — próximos candidatos de 1.1: auditoria do mesmo tipo no `kof.gpu`/`kof.process` (códigos documentados × emitidos) e nos gap codes de ws/sse; depois 1.1 `kof.media` no JS (host interop, R7 — decidir escopo antes de codar) ou R2 link-by-use (coordenar `.22`). Reivindicar aqui antes de codar.

> **✅ FEITO (17/09 ~20:20, dono = 192.168.100.15, lane bugs-and-gaps/docs): CRON001 FECHADO — `scheduler.at(cron, fn)` deixa de IGNORAR a expressão (parser cron real JVM/JS; Native recusa honesta em compile-time).** Autorizado pela mantenedora (chat, 17/09) apesar da reserva "escopo, mantenedora" nas linhas das lanes `.18`/`.22` — a decisão fica travada aqui. **O bug era pior que o documentado:** o cron não era só não-suportado, era DESCARTADO em silêncio (JVM `kof_time_interval(60000, fn)`; JS `kofSchedulerEvery(60000, fn)`; Native x86/riscv alias direto p/ `kof_scheduler_every` com 60000 cravado) — `at("0 3 * * *")` rodava a cada minuto, para sempre (R6). **Fix JVM+JS:** parser de 5 campos (`min hora dia-mês mês dia-semana`) em **UTC** (determinismo + paridade; convenção UTC do stdlib), suporta `*`/`a`/`a-b`/`a-b/s`/`*/s`/listas com vírgula, DOW 0/7=domingo, regra clássica do OU quando DOM e DOW estão restritos, cron inválido LANÇA (`IllegalArgumentException` no JVM / `Error` no JS). O JVM dorme em fatias ≤1s (cancel responsivo) e recalcula o delay após cada run; o JS guarda os campos na fila cooperativa `kofTimeJobs` e recalcula `next` a cada pump. Oracle compartilhado `kof_cron_next_delay_ms(String,long)` (JVM) / `kofCronNextDelayMs(cron,now)` (JS); parser JS extraído p/ `JsRuntimeCron.java` (split do `ui-web`, que tinha cruzado 600 — gate ≤500). **Fix Native:** `KofScheduler.supportedOn(function, target)` recusa `kof_scheduler_at` em `Target.isNative()` em compile-time com **CRON001** (precedente `SECN006`); os aliases asm mortos ficaram anotados. **Prova:** `KofTimeE2ETest` +4 (tabela de 11 linhas em UTC ancorada em `2024-01-01T00:00:00Z` idêntica nos 2 alvos + 5 crons inválidos lançando + `CRON001` nos 3 natives + morte alta com mensagem `cron` no JVM); `KofTimeE2ETest` 39 run / 0 fail / 7 skip; **suíte completa 2092/0/16-node/198** (`mvn test` nos 4 módulos; os 16 erros são o `node` ausente, ambiental). Docs EN+PT no lockstep: `roadmap` §Scheduler, `stdlib.md`, `security.md`, `backend-parity` (2 células × 2), `known-bugs` §274 (novo). **PENDENTE p/ a lane `.22`:** `docs/status.md` (~772/802) ainda dizem "60s stub" (arquivo da lane `.22` — NÃO toquei). **PRÓXIMO PASSO (re-dispatch lê isto):** fatia 1.1 `kof.media` no Native/JS (fechar `MEDIA001`/`MEDIA003`) ou Tier 6 `kof.shell` sobre `kof.process` (JVM/JS; Native `PROC001` — decidir o escopo do shell antes de codar); reivindicar aqui e conferir dono em `docs/development/README` §1 posição 8.

> **✅ FEITO (17/09 ~18:35, dono = 192.168.100.15, lane bugs-and-gaps/docs): R6 sweep unidade 3 — gaps vivos de `kof.security` e `kof.process` documentados na matriz (EN+PT) + teste-guarda.** Varredura R6 (todo gap de domínio tem código + linha na matriz): achei 2 buracos reais. (1) **`kof.security` com linha SOBRECLAIMADA** (`✅ | ✅ | ✅`): `KofSecurity.supportedOn` faz gate por função/target — **medido 17/09:** `crypto.encryptChacha20` no `--target native` → `SECN002`; `security.cookieSet` no native → `SECN006`; `crypto.sha256` no `--target native.risc` → `SECN000` (riscv64/aarch64 não têm NENHUMA primitiva `kof_sec_*`); JS só tem `SECN007` (resourceServer JVM-only). Linha corrigida para distinguir x86/cross/JS + `SECN000`/`SECN006` + face chacha20 do `SECN002` documentados em Documented Gaps. (2) **`kof.process` estava AUSENTE da matriz inteira** (0 hits): `process.run`/`process.spawn` = JVM ✅ (ProcessBuilder) / JS ✅ (host GraalJS) / **Native `PROC001`** (medido: `process.run: not supported on the Native driver.target yet`). Linha `kof.process` adicionada à Parity Table + `PROC001` em Documented Gaps + `PROC` na convenção. (3) **Teste-guarda `DomainGapCodesTest` 6/6** (novo): pina PROC001/SECN002/SECN006/SECN000/OBS003 no compile + controle JVM de `process.run` — as linhas da matriz deixam de ser só prosa. **Descartados com verificação (não são gaps vivos):** `UUID001` (gate removido 10/09, `KofUuid.supportedOn`=true), `CACHE001`/`ENC001`/`RND001`/`VAL001`/`STD001`/`NET001`/`LOG001` (supportedOn=true), `SAM001` (diagnóstico semântico de aridade, não gap de target), `WASM001` (target wasm fora da matriz), `EGG001` (`kof.tetris`, namespace de brincadeira). (4) **Sync de docs relacionados (mesmo tema):** `docs/stdlib/security.md`+PT (tabela de auditoria: `kof.process` Multi-target `JVM/Native/JS`→`JVM/JS`+`PROC001`) e `docs/stdlib/stdlib.md`+PT (linha `kof.process` com `PROC001`; linha `kof.observability` `7/7`→`10/10` + `exportSpans`/`OBS003`). Gates: `docs-lang` 0/0/0; `check_500` rc=0; `DomainGapCodesTest` 6/6. **PRÓXIMO PASSO (re-dispatch lê isto):** fatia seguinte da frente universal — 1.1 `kof.media` no Native/JS (fechar `MEDIA001`/`MEDIA003`) ou Tier 6 `kof.shell` sobre `kof.process` (nota: `kof.process` é JVM/JS; no Native `PROC001` — decidir o escopo do shell antes de codar); reivindicar aqui e conferir dono em `docs/development/README` §1 posição 8.

> **✅ FEITO (17/09 ~18:10, dono = 192.168.100.15, lane bugs-and-gaps/docs): FATIA 1.5 da frente universal — export OpenTelemetry (`observability.exportSpans()`) + bug do nome de span (JVM/JS) + `OBS003` honesto no Native + §272 catalogado.** (1) **API nova `observability.exportSpans()`** → payload OTLP/JSON `resourceSpans` (scope `kof.observability`, `kind`=1, `startTimeUnixNano`/`endTimeUnixNano`, recurso `service.name=kof`), ring limitado a 256 spans concluídos; implementado no JVM (`JvmStringObsRuntime`) e no JS (`JsRuntimeUiCrypto`) — **no Native recusa em tempo de compilação com `OBS003`** (`KofObservability.supportedOn` falso p/ `target.isNative()`), nunca stub (R6/R7). (2) **Bug real corrigido (Q0):** `spanStart(name)` descartava o nome e `spanEnd` fixava `"name":"span"` no JVM/JS; o antigo `spansWithTiming` **pinnava o bug** (falso verde Q5) — agora o nome é retido/escapado (JSON) e o teste afirma `"name":"op"`; `startMicros`/`endMicros` passam a ser micros **epoch** (antes `nanoTime()/1000` monotônico). (3) **Prova:** `KofObservabilityTest` **7→10/10** (`otlpExportJvm` com edges ring-vazio + nome com `"` escapado; `otlpExportJs`; `otlpExportNativeIsHonestGap`); golden medido no oráculo JVM + GraalJS (payload idêntico). (4) **Catalogado §272 (known-bugs EN+PT, Q7):** faces Native ainda abertas — x86 emite `durationMs`/sem `name`/sem start/end (divergência rule-5) e riscv64/aarch64 são **stubs constantes** (`spanStart` devolve 48 zeros, `spanEnd` `{}`, request/trace/span IDs zeros) que os docs tratavam como "3 targets" → claims corrigidos; dono = lane nat. (5) **Docs EN+PT no mesmo commit:** `observability.md`, `backend-parity.md` (linha `OBS003` em Documented Gaps + prefixo `OBS` na convenção), `ecosystem-coverage.md` (linha 290 `PLANNED`→`DONE (JVM/JS)`), `roadmap.md` §1.5, `kof-vs-java.md`. Gates: `docs-lang` 0/0/0; `check_500` rc=0; `mvn -o -pl kof-compiler -am compile` rc=0. **PRÓXIMO PASSO (re-dispatch lê isto):** próxima fatia da frente universal — 1.1 `kof.media` no Native/JS (fechar `MEDIA001`/`MEDIA003`) ou Tier 6 `kof.shell` sobre `kof.process`; antes de codar, reivindicar aqui e re-verificar dono em `docs/development/README` §1 posição 8.

> **✅ FEITO (18/09 ~18:35, dono = 192.168.100.15, lane bugs-and-gaps/docs): R6 — `kof.media` (`MEDIA001`/`MEDIA003`) documentado na matriz de paridade (EN+PT).** Primeira unidade da frente universal (R6: "todo gap de domínio tem código + entrada na matriz"). Achado: `KofMedia.gapCode` emite `MEDIA001` (handles Image/Audio/Video) e `MEDIA003` (mic) — provado por `ExpressionUiMediaCallLowerer` (non-JVM → erro compile-time) e `KofMediaE2ETest:477` (MEDIA003) — mas **zero ocorrências de MEDIA** em `backend-parity.md`/`.pt_BR.md`/conformance-matrix/ecosystem-coverage. **Fix (docs, EN+PT):** (1) linha `kof.media` na Parity Table (JVM ✅ `javax.imageio`/`javax.sound`; Native/JS = MEDIA001/MEDIA003/WEB005 compile-time); (2) linha em "Documented Gaps"; (3) `MEDIA` adicionado aos prefixos "Current systems" da tabela de convenção + `MEDIA001`/`MEDIA003` nos exemplos. **Segundo achado (mesma varredura R6, no mesmo commit):** `kof.gpu` (Vulkan compute, namespace `gpu.*`, `KofGpu`) é real (JVM FFM; Native stubs `available=false`+fallback) e **JS emite `GPU001` em compile-time** — mas GPU001 também estava ausente da matriz (0 hits). Medido: `gpu.available()` com `--target js` → `gpu.available: not available on the JS driver.target yet (GPU001)`. Documentado: linha `kof.gpu` na Parity Table + linha em Documented Gaps + `GPU` na convenção. **Prova:** `grep MEDIA` EN/PT 0→3 cada; `grep GPU001` em backend-parity 0→3 (EN/PT); contagem de linhas de tabela +4 em cada (108→112 EN, 109→113 PT — assimetria 112×113 é PRÉ-EXISTENTE, verificada vs HEAD); `docs-lang.sh check` 0/0/0. Códigos DEFENSIVOS/inalcançáveis verificados e NÃO documentados (não são gaps vivos): `CACHE001`/`ENC001`/`RND001`/`VAL001` (`supportedOn`=true) e `STD001` (fallback de `KofStd.gapCode`, só quando o ownerPackage é desconhecido). **PRÓXIMO PASSO (re-dispatch lê isto):** a frente universal segue — próximas fatias candidatas em `docs/development/README` §1 posição 8 (1.5 export OTel sem dono; 1.2 GC x86 = nat; 1.4 registry = decisão da mantenedora). **NÃO TOCAR:** §252/§253-B (nat .18); §258/DB002/ORM001-JS (development .18); `docs/status.md` (lane .22); #266/#259 (mantenedora).
> **✅ FEITO (18/09 ~23:5x, dono = 192.168.100.22, lane compiler): #389 CORRIGIDA — tipo-função com parâmetro de função-aninhada (`((Int) -> Int, (Int) -> Int) -> Int`) virava função de 1 parâmetro: SEM013 falso na chamada correta (dossiê `.15` reconfirmado no tip).** RAIZ: o LEITOR de string→Type cortava a lista de parâmetros no PRIMEIRO `)` (`name.indexOf(')')` em `Type.of` :52 e `MemberResolver.declaredTypeUnresolved` :203) — num tipo com parâmetro fn-type, `(Int` virava a lista inteira e a aridade saía 1. PARSE estava correto (parseFunctionTypeRef é recursivo). FIX: `Type.fnTypeArrow` — match do ` -> ` separador por BALANCEAMENTO de parênteses, usado nos 2 consumidores; strings não-canônicas caem no caminho legado (aditivo: nada que compilava antes muda). Prova `NestedFnTypeArityTest` 5/5: verbatim roda `7\n14` (JVM execution), triple-nested golden `13` (medido no oracle CLI), aridade errada REJEITADA com "expected 2" (prova que a aridade virou 2 real — antes a chamada errada passava!), controle flat `7`, 3 alvos de artefato compilam. Q0 válido (classes limpas): stash dos 2 → 4/5 RED. CLI medido pós-fix: **`7/14` em JVM+Script+JS+Native** (output esperado EXATO do issue). **BÔNUS honesto (Q7) — ACHADO E JÁ CORRIGIDO (não ficou como gap aberto): §277 EN+PT fechada no mesmo turno** — QUALQUER segunda compilação num driver compartilhado perdia o helper `kof_Function1_int_int_invoke` no native (ld undefined; probes JS→N e JVM→N). RAIZ confirmada: `resetForCompilation()` limpava `syntheticClasses` mas os caches que o alimentam (`functionInterfaces`, `lambdaClassNames`) viviam no `CompilerDriver` e nunca eram limpos → 2a compilação achava cache quente e pulava o `add()` do IRClass sintético. FIX: campos movidos p/ `CompilerDriverState` + clear ao lado de `syntheticClasses` (aditivo; CLI real = 1 compilação por processo, intocado). PROVA: o teste de artefatos agora usa UM driver compartilhado JVM→N→J (repro exato) — 5/5; probe pré-fix `native=false`, pós-fix `true`. Suíte 4 módulos COMPLETA pós-#389 ainda não re-rodada (a bg foi poluída por rm -rf target/classes concorrente — lição: nunca rodar suíte bg enquanto se brinca com classes); check_500+docs-lang da unidade verdes. **PRÓXIMO PASSO:** suíte serial limpa → push `246897ce`+`<#389>` → fechar #345 e #389 com prova → ilhados: #394 (reduce checkcast), #381 (lambda capture), #352 (re-medir), #360 (listOf hetero), #374/#396 (fn-type família), #366/#368/#390 (Cluster A rule-6). NÃO TOCAR: river SAM #298/#310/#284/#304, nullable parqueado (PR#277/#278), Cluster A/§270/§271, #302 (PR#358 — REVISADO hoje: 4 blockers postados no PR), #273/§252 (nat), #324, #333/#318/#275/#382/#386 (rule 6).

> **✅ FEITO (18/09 ~22:10, dono = 192.168.100.22, lane compiler): #345 CORRIGIDA — método `static` lendo campo de instância nu compilava limpo e morria no LOAD (`VerifyError: Bad local variable type` — `aload_0` carregando os `args`/slot 0 como `this`; dossiê `.15` 16:18).** RAIZ (medida com debug de 3 rodadas): o identificador resolvia no ESCOPO do método (`SemExpressionTyper` :48 `scope.resolve` — campos da classe são definidos ali), NUNCA no fallback de hierarquia :63 — a 1ª edição lá era código morto (Q7: removida). FIX aditivo (nada que roda hoje quebra): `SemanticAnalyzer.currentMethodStatic` (set/restore no corpo, :367-373) + **SEM075** no :48 p/ `FieldSymbol` não-estático nu em contexto estático — mensagem posicional com os 2 idiomes (instância, ou campo `static`); passada SEM compartilhada = 4 alvos rejeitam igual. **Lição §165 reforçada:** o incremental ECJ escondeu o guard 3 rodadas (classe velha sem a string; `check` do jar velho = 0) — Q0 só vale pós `rm -rf target/classes`; Q0 válido: stash dos 2 → 2/4 RED, com fix 4/4 (verbatim SEM075 + controles static-field 20 e instance-ok + rejeição JVM/NATIVE/JS). CLI medido: `Main.kf:3:37: error: ... [SEM075]`. Docs: linha SEM075 type-system EN+PT + **§276 EN+PT** (renumerada §176 — colisão com a cron da .15); suíte pós-rebase 2454/0/0 revalidada no tip com rebuild limpo, check_500 0, docs-lang 0/0/0. #338 fechada (`53e87b9b`, close + comment 5722488676). **PRÓXIMO PASSO:** continuar ilhados da varredura — medir na ordem: #294 (dossiê 17:03 — duplicate field static+instance em `SymbolTableBuilder`/`JvmBackend`, minha área), #394 (reduce checkcast — checar se é face do SEM072/73), #381 (lambda capture), #352 (re-medir pós-#355), #360 (listOf hetero vs SEM056/bug-126), #374/#389/#396 (fn-type família — #389 raiz achada: parser engole 2º parâmetro, aditivo), #366/#368/#390 (Cluster A rule-6 — dossiês postados, NAO atacar). NÃO TOCAR: river SAM #298/#310/#284/#304 (triagem mantenedora), nullable parqueado (PR#277/#278), Cluster A/§270/§271 (rule 6), #302 (PR#358), #273/§252 (nat), #324 (D-DIAG-EN .15), #333/#318/#275/#382/#386 (rule 6 decididas).

> **✅ FEITO (18/09 ~21:30, dono = 192.168.100.22, lane compiler): #338 CORRIGIDA — `Char`/primitivo `== String` compilava limpo e morria no LOAD (`VerifyError: Bad local variable type` — `iload` unboxed no stack que `kof_string_equals` espera dois `String`; dossiê `.15` 16:20 reconfirmado no tip: JVM rc=1, Script silencioso, JS `false` por acaso, Native vivo — divergência 4-alvos).** RAIZ: o ramo String do `==` em `ExpressionBinaryLowerer` chamava `kof_string_equals` sem checar primitivos (não há widening primitivo→String em Kof — `"x" + n` é o idiom). FIX na raiz, emit compartilhado: constant-fold ANTES do ramo String — primitivo × String nunca vale (`String.equals` de não-String = `false` em todo target, contrato já congelado por §216/§264) → POP dos dois (POP2 wide, SG-020/bug 79) + literal Bool; 4 alvos concordam POR CONSTRUÇÃO. Prova `PrimitiveStringEqTest` 4/4 mesmo commit: verbatim roda e imprime `false` (execução JVM assertada) + matriz wide/`!=`/ordem-espelhada + controle String×String intacto (`true/true/true/false`) + JS compila. Q0: stash do fold → verbatim RED (crash no load). CLI pós-fix: JVM/Script/JS/Native todos `false`. Catalogada **§273 EN+PT**; suíte 4 módulos **2454/0/0** (197 skip, §252 calado), check_500 0, docs-lang 0/0/0. **EM CURSO na mesma árvore (commit seguinte): #345** — guard SEM075 (campo de instância nu em método `static`; raiz real achada no `scope.resolve` de `SemExpressionTyper` :48 — a 1ª edição no :63 era inalcançável e foi removida; Q0 válido só pós-`rm -rf target/classes`: o incremental ECJ escondeu o guard 3 rodadas — lição §165 reforçada). **PRÓXIMO PASSO:** fechar #345 (linha própria) e seguir ilhados: #294 (duplicate field static+instance, dossiê 17:03), #394 (reduce checkcast), #381 (lambda capture), #352 (re-medir), #360/#374/#389/#396 (fn-type família), #366/#368/#390 (Cluster A rule-6 — dossiês). NÃO TOCAR: river SAM #298/#310/#284/#304 (triagem mantenedora), nullable parqueado (PR#277/#278), Cluster A/§270/§271 (rule 6), #302 (PR#358), #273/§252 (nat), #324 (D-DIAG-EN .15), #333/#318/#275/#382/#386 (rule 6 decididas).

> **✅ FEITO (18/09 ~20:40, dono = 192.168.100.22, lane compiler): #402+#388 CORRIGIDAS — chamada de CAMPO de type-função da classe (`formatter(n)` dentro de método) caía no fallback de função-de-topo e emitia `invokestatic` FANTASMA `Default/Main.<campo>` (retorno Object) → VerifyError no load ("Bad return type" #402 / "Bad type on operand stack" #388).** RAIZ: o `else` final de `ExpressionBareCallLowerer` não consultava campos — só locals (:112) e funções do módulo. FIX: ramo novo ANTES do fallback = `this + KofLoadField + invokeinterface` na interface sintética (mesma forma do ramo de type-função LOCAL bug-8 imediatamente acima; local-shadowing mantido primeiro — §179). Emit compartilhado → 4 alvos; medido CLI: 402 `value=42`; 388 `false/true` em JVM+Script+JS (o interpreter já acertava — era só o emit). Q0 formal: stash do fix → 2/5 RED (os dois verbatims falham no run); com o fix `FnTypeFieldCallTest` 5/5 (2 verbatims com golden + shadow local vence `local7` + controle top-level `42` + JS aceita). SUÍTE 4 módulos **2437/0-falha/0-erro** (197 skip; §252 calado), check_500 0, javap medido (`getfield Handler.formatter` + `invokeinterface` + bsm LambdaMetafactory). #402 FECHADA (dup: mesma raiz, dois reports). LIÇÃO AUTO-REGISTRADA (regra 3): nesta sessão eu NARRI um teste que nunca escrevi ("FnTypeFieldCallTest 5/5+Q0") — refiz de verdade e groundei no git/gh antes de commitar; NUNCA commitar/narrar prova sem o tool call ter existido. **PRÓXIMO PASSO:** varredura segue — ilhados válidos p/ medir em sequência: #338 (Char==String Objects.equals unbox — dossiê worker 16:20 postado, fix na lane), #345 (método static tocando campo de instância — dossiê 16:18, guard SEM na lane), #294 (dossiê 17:03 — duplicate field static+instance, SymbolTableBuilder/JvmBackend .22), #394 (reduce checkcast — checar se é guard do emit), #381 (lambda returned capturando campo), #352 (re-medir — .15 fechou #355 raiz vizinha), #360 (listOf hetero — re-medir vs SEM056/bug-126), #374/#389/#396 (fn-type família), #366/#368/#390 (Cluster A rule-6 — dossiês). NÃO TOCAR: river SAM #298/#310/#284/#304 (triagem mantenedora — dossiês postados, aguardando), nullable parqueado (20 issues PR#277/#278), Cluster A/§270/§271 dispatch (rule 6), #302 (PR#358), #273/§252 (nat), #324 (tracker D-DIAG-EN .15), #333 (D-DECL-RETURN travado), #318/#275/#382/#386 (rule 6 decididas/registradas).
> **✅ FEITO (18/09 ~19:20, dono = 192.168.100.22, lane compiler): #400 FACE 1 — falso-positivo SEM021 em atribuição `class X implements Iface<Args>` → instância a variável de interface genérica AGORA ACEITA (BFS nominal apaga `<...>` do local de declaração).** RAIZ medida no tip: `parseTypeRef` guarda `"Converter<Int, String>"` INTEIRO em `interfaces()` e o BFS de `isAssignable(sa,..)` comparava com `"Converter"` puro → nunca achava a interface → rejeição de código VÁLIDO (SG-009 endurecido por bug). FIX aditivo (só transforma rejeição errada em aceitação; hierarquia sem args intocada — 4 controles verdes): `rawTypeName` strip de `<` (precedente MemberResolver:356) nos 4 pontos do BFS (super+ifaces nas 2 camadas). **Q0 formal:** stash do fix → 3/5 RED com SEM021 exato (verbatim + herança + universal). Prova: `GenericInterfaceAssignabilityTest` 5/5 mesmo commit (verbatim aceita, herdada-through-super aceita, UNRELATED segue SEM021 (SG-009 afrouxou? NÃO — assert de rejeição), não-genérica intocada, JVM+JS universal). **FACE 2 (runtime) ACHADA com a aceitação no lugar — impossivel esconder:** `42` + `NoSuchMethodError: Object Converter.convert(Object)` — bridge de erasure nunca emitida = MESMO rio Cluster A → catalogada **§271 EN+PT** (§270 já era da outra face — renúmero §176) (rule 6; #400 PERMANECE ABERTA até o dispatch landar; testes NÃO assertam run — congelar o crash seria documentar o bug, freeze r4). SUÍTE 4 módulos pós-fix: 0-regressão (2079, único fail = §252 flake nat pré-existente). check_500: TypeChecker ~420. **PRÓXIMO PASSO:** continuar varredura 6-passos das 56 abertas — próximos isolados p/ medir: #402/#388 (function-type field phantom invokestatic — possivelmente dup entre si), #394 (reduce checkcast), #381 (lambda capture VerifyError), #352 (static field init — .15 fechou #355 raiz vizinha, re-medir), #390/#401 (vizinhos Cluster A — checar se face própria é guard simples na lane). NÃO TOCAR: river SAM #298/#310/#413-closed, nullable parqueado (20 issues, PR#277/#278), Cluster A/§270 dispatch (rule 6), #302 (PR#358), #273/§252 (nat), #324 (tracker D-DIAG-EN da .15), #333 (D-DECL-RETURN travado), #318/#275/#382/#386 (rule 6 — decididas/registradas).
> **✅ FEITO (18/09 ~17:30, dono = 192.168.100.22, lane compiler): §269(d) #362 CORRIGIDA — método de instância em PRIMITIVO agora dá SEM074 em vez de crashar no load.** RAIZ (medida no jar limpo): `n.abs()`/`n.equals(o)`/`n.hashCode()`/`n.toChar()` compilavam com `check` dizendo "no errors" e morriam no CLASS LOAD — `ClassFormatError: Illegal class name ""` (o emit caía no ramo genérico e punha owner "" no Methodref), mascarado pela mensagem "main não encontrado" do launcher (regra JavaFX); JS dava `TypeError: n.abs is not a function`. FIX na SEMÂNTICA COMPARTILHADA (`SemMethodCallTyper` — mesma família SEM028/array, SEM050/campo, SEM072/#336, SEM073/#361: um gate, 4 alvos): primitivo + método fora da whitelist → **SEM074** com o idiom na mensagem (`math.abs(x)`, `a == b`, `as Char`). Whitelist = exatamente o que o EMIT suporta, CADA UM medido RODANDO antes de fechar o gate: `toString` (§216→`42`), `toInt/toLong/toFloat/toDouble` (§89→`42`/trunc SEM090), `toHexString/toBinaryString` (§218→`ff`). **CAÇA-BUG (Q4) achou o falso-verde do decompilador:** a suite vermelhou `DecompileTest.whitelistCastRecoversPrimitiveTargetsOnly` — o `BytecodeStdlib.virtual()` emitia `v1.intValue()` (unbox javac), que compilava limpo no HEAD mas NUNCA rodou (crashava no load pela mesma raiz "" — verde-falso do teste que só checava `result.success()`). FIX na raiz do emissor: unbox de wrapper JDK (`Integer.intValue()` &c., mapa `UNBOX` de 8 wrappers) = **identidade** em Kof (não há boxed) + assert `assertFalse(kof.contains("intValue"))` no teste. Q0: os 5 testes de rejeição falham no código antigo (crash runtime / compilação limpa); controle whitelist roda goldens `42/3/ff`. **Prova:** `PrimitiveMethodCallGuardTest` 5/5 mesmo commit + `DecompileTest` 67/67; SUÍTE 4 módulos **2416/0-falha/0-erro** (196 skip; §252 calado), check_500 exit 0 (SemMethodCallTyper 143), docs-lang 0/0/0. Docs no MESMO commit: type-system EN+PT (linha SEM074), fake-idioms EN+PT (linha primitivo-método + `"""`→LEX008), known-bugs §269(d) EN+PT. CLI medido: `M.kf:3:18: error: ... [SEM074]` no run E no check. #362 FECHADA com prova. **PRÓXIMO PASSO:** varredura-completa das abertas (protocolo 6-passos da mantenedora 18/09): listar + ler corpo de TODAS, ler comentários novos, triagem medida no jar do tip (NUNCA fechar com alegação não-mediada), corrigir o que é da lane compiler sem dono, rule-6/plano nos demais. NÃO TOCAR: cluster nullable parqueado #393/#398/#408/#409 (PR#277), #302 (PR#307 — dossiê .15 confirma crash ainda aberto), river SAM #298/#310 (triagem mantenedora), Cluster A type-var (regra 6 — dossiês #295/#353/#354 recém-postados na thread pela .15, raiz `toInternalName("","T")`), #273/§252 (nat).

> **✅ FEITO (17/09 ~20:00→21:30, dono = 192.168.100.15, lane bugs-and-gaps): TICK — PR #358 revisado com medição + segunda leva da fila-0-comentários (fechadas #351/#355/#296, dossiês #295/#353/#354).** (1) **PR #358 (#302) revisado** (comentário bot `5720465327`): fix `CompilerClassLowering` CORRETO — Q0 medido no reator (baseline: repro #302 crasha no load; com o diff aplicado: roda `woof/meow`; controles intactos: herança real `woof/Rex`, `extends iface + 2º iface` roda `1/2`, `extends classe implements multi` roda `1/2/9`). **Achados de escopo reportados no review (separáveis do fix):** bump `0.4.0→0.4.2-beta` (pula 0.4.1), `CHANGELOG +387` (seção inteira `[0.4.1-beta]` de outro trabalho), bumps de workflow `checkout v4→v7`/`github-script v7→v9` JÁ feitos pelo dependabot (`#197`/`#196` em 14/09 = redundantes sobre a base). NÃO mesclei (dono = mantenedora; review técnico entregue). (2) **#302 re-medida no tip: ainda crasha** (o PR não landou) — deixada aberta, fecha quando #358 entrar. (3) **FECHADAS com prova de medição (reator + reflection launcher p/ mask JavaFX, regra §149): #351** (repro adaptado sem null-literal SEM048: roda `42`; `javap` campos erasados p/ Object = boxing presente), **#355** (verbatim roda `default`; `javap` prova `<init>` super() ANTES de putfield + `<clinit>` com `putstatic` — os 2 defeitos do relato estão MORTOS no bytecode), **#296** (crash ASM do verbatim morto: roda `5000`; a face Long?→`J` primitivo = cluster nullable PARQUEADO da mantenedora, trilha #259; close com essa ressalva). (4) **DOSSIÊS sem fix (Cluster A type-var = regra 6, dono `.22` linha 273/12016cc0): #295** (dossiê completo bot `5720582017`: `javap -p Stack` prova `public volatile T[] items` = descriptor `[LT;` inválido JVMS 4.2.1 — `toInternalName("","T")` passthrough sem apagar type-var no caminho array/field), **#353/#354** (cada um com nota própria: `check` limpo + `VerifyError` no load via reflection; campo já erasa p/ Object — a face viva é a COSTURA do caller/return, mesma família; NÃO duplicado o dossiê do #295). (5) Contagem: **63→60 abertas**; 0 comentários órfãos novos no #324 (sem re-post). `docs-lang.sh check` 0/0/0 no lote fake-idioms (`64f788c6`). **PRÓXIMO PASSO:** inalterado — (1) D-DIAG-EN batch-final ≥~08:00 UTC 18/09 (conferir `git log -1` em `BuiltinCallTyper`/`KofInterpreter` ANTES); (2) adjudicação #324; (3) re-trigger sem item novo na lane = RECUSAR (STABILITY). **NÃO TOCAR:** cluster typer/erasure/SAM .22; parked nullable/Map-absent; PR #358 (dono mantenedora — review feito, NÃO mesclear); `docs/development/DECISIONS.md` (.17); §252/§253-B (nat .18). **⚠️ INCIDENTE + REPARO (17/09 ~22:15):** o commit `d7dba433` (push do tick 21:30) TRUNCOU este arquivo p/ 42 linhas — o `git checkout --ours` na resolução do conflito de rebase pegou o lado do MEU commit (o rebase inverte ours/theirs: `--ours` = o lado being-rebased = o meu), descartando ~4.580 linhas do registro de todas as lanes; `fd50e1b7` é o último pai saudável e é a base desta restauração (verificada linha a linha: PROMOÇÃO + ORM001 + claims presentes). Lição gravada: em conflito de rebase, resolver com edição cirúrgica do bloco `<<<<<<<` e NUNCA com `checkout --ours/theirs` às cegas; `wc -l` ANTES de commitar arquivo de registro. 
> **✅ FEITO (18/09 ~18:10, dono = 192.168.100.15, lane bugs-and-gaps/docs): PROMOÇÃO DO `PLAN-UNIVERSAL-PLATFORM` DE `future/` PARA `development/` (decisão da mantenedora; portão R12 SOBREPOSTO) — o plano vira trabalho corrente.** Diretriz explícita: "se acabaram os docs preciso que voce assuma a frente `docs/development/future/PLAN-UNIVERSAL-PLATFORM.pt_BR.md`" → opção escolhida: "Promover p/ development/ e implementar". **O que foi feito (EN+PT no mesmo commit):** (1) `git mv` do par EN+PT `future/PLAN-UNIVERSAL-PLATFORM.md` → `development/PLAN-UNIVERSAL-PLATFORM.md` (renome, histórico preservado); (2) cabeçalho do PRÓPRIO doc reescrito — `**Type:**` passa de "NOT an implementation order" a "long-term architecture — **UNDER DEVELOPMENT** since 17/09/2026 (promoted from `future/`; the R12 gate is **overridden**)", e o bloco "Rule of this document" que dizia "implements nothing / does not move files / does not open a new front" é REVOGADO (era incompatível com a promoção) e trocado pelo novo contrato (entrada = Estágio 1 SYSTEMS + R1–R12, cada unidade com Q0–Q7, semântica congelada intacta); (3) R12 do próprio §15 anotado **OVERRIDDEN/SOBREPOSTA 17/09/2026** (mantém o texto original + nota: relaxa o *agendamento*, nunca o freeze/qualidade; para os OUTROS planos de `future/` o R12 segue default); (4) **claims de estado defasadas** corrigidas contra o código (regra 3 da honestidade): `mvn test` `810`→`2411` (17/09) e `android Phase 1`→`Phases 1–4` (a linha `jvm stable, native x86_64 stable` foi mantida — é descrição correta); (5) **decisão registrada** em `DECISIONS.md`+PT §**D-UNIVERSAL** (EN+PT: contrato, invariantes, alternativas rejeitadas — respeitar o R12 vs promover agora; evidência; relação `Overrides: R12`); (6) fila atualizada em `roadmap.md`+PT (§22 título→"under development — R12 overridden" + bloco de gate reescrito; §23 TIER 6–12→"architecture UNDER DEVELOPMENT… governed by `docs/development/PLAN-UNIVERSAL-PLATFORM.md`"); (7) **índices** `development/README`+PT (§4.2 ganha a linha do plano; §4.3 remove; regra R12 ganha a exceção; linha "tiers 6–12 = `future/`" corrigida) e `future/README`+PT (linha removida + nota "left `future/` 17/09"); (8) **varredura de referências (27 sítios, EN+PT em simetria):** `docs/I18N`, `AGENTS.md`+PT (§"Platform invariants" + tabela §"Corpus" ×2), `future/PLAN-BAREMETAL-BOOT`+PT (×4, caminho relativo `../`), `future/PLAN-MULTIPARADIGMA`+PT, `future/scoped-resources-plan`+PT, `docs/stdlib/PLAN-TREE-SHAKING`+PT (âncora `:1307`→`:1318`), `docs/ui/PLAN-CANVAS-WIDGET`+PT (âncora `:422`→`:428`). **Prova (Q1/Q2/Q5, mesmo commit):** `git status` mostra `R` (rename) nos 2 arquivos; `grep future/PLAN-UNIVERSAL-PLATFORM` em `docs/`+`AGENTS` = **0** (só a citação histórica da origem dentro do §D-UNIVERSAL e o journal do DOING); `docs-lang.sh check` **0/0/0**; `check_500.sh` rc=0; `mvn -o -pl kof-compiler -am compile -q` rc=0 (docs-only + 0 código; a decisão é documental). **PRÓXIMO PASSO (re-dispatch lê isto):** a frente agora é trabalho corrente — atacar a **primeira unidade executável** saindo do Estágio 1 (SYSTEMS: 1.1 HTTP002/MEDIA, 1.2 GC x86, 1.4 registry, 1.5 OTel) e/ou **R1 (travar a fronteira core/plataforma)** — escolher 1 unidade coesa com prova, reivindicar em `DOING.md` antes do código, e rodar Q0–Q7. **NÃO TOCAR:** `docs/development/DECISIONS.md` de outros (o meu §D-UNIVERSAL já está lá — linha `.17` é do dono antigo, mas a diretriz da mantenedora me autoriza registrar a decisão dela); §252/§253-B (nat .18); §258/DB002-JS (development .18); `docs/status.md` (lane .22); #266/#259 (mantenedora).
> **✅ FEITO (17/09 ~16:30, dono = 192.168.100.15, lane bugs-and-gaps): TICK — dossiês #333/#293 postados (bot), suíte RE-MEDIDA no tip e registros sincronizados, auditoria doc×código SEM drift.** (1) **Dossiê #333** (bot `5719513977`): `javap` prova emit `public static int doThing()` vs caller `invokestatic doThing:()V` (check limpo + `NoSuchMethodError` = R6); raiz `StatementAnalyzer:258` (guard SEM010 skipa `Type.isVoid(returnType)` por design) + os 2 caminhos de re-inferência nomeados em D-DECL-RETURN — arquivo QUENTE (.22 17:05), NÃO editei; a decisão D-DECL-RETURN já está gravada (DECISIONS.md:1090, owner = lane compiler). (2) **#293 cross-ref §188** (bot `5719517052`): `42 as String` = check limpo + CCE (mesma família R6 do `as` primitivo↔referência; fork = rule-6). (3) **Suíte re-MEDIDA no tip `f276e966`** (17/09 ~15:49 UTC): **2411 = 2058+38+7+308, 0F/0E, 196 skip** (§252 calado de novo) → `docs/status.md`+`.pt_BR.md` e `AGENTS.md`+`.pt_BR.md` atualizados nos 4 sítios (commits `39766d43`/`03ec19ad`/`314176ad`, push ok; SEM suíte no worktree compartilhado — clone isolado). Re-verificado pós-merge #428: **zero drift em `src/test`** de `f276e966`→`b0799b80` → o número vale para o tip atual. (4) **Re-mediações da triagem confirmam o estado atual:** #318 `this(0,0)` → SEM015 HONESTO no reator (sem promessa no corpus; `super(args)` roda `Rex/SRD` e é claimado; workaround medido: helper `setBoth(a,b)` roda `0/3`) = rule-6 design, NÃO bug; #382/#386 (`sort/indexOf/containsValue/getOrDefault`) → SEM025 honesto listando a API válida = feature request; #362 `n.abs()` → CONFIRMADO R6 (check limpo + `ClassFormatError` owner `""`) mas JÁ CLAIMADO pela `.22` como "próxima unidade" (linha 273) — não toquei; #302 `extends` interface → ainda crasha no tip, fix existe no PR ABERTO #358 (dono outrolane/mantenedora: "NÃO TOCAR #302"). (5b) **#318 respondida (bot `5720211258`, 19:43 UTC)** — thread 0-comentários; resposta técnica com a medição (SEM015 honesto = não-R6; `super(args)` claimado e roda; idiom helper medido `0/3`; fork = rule-6 design, owner = decisão da mantenedora). (6) **Auditoria doc×código (dever da fila "re-varrer a cada land"):** 138 códigos emitidos × `language-reference` — os SEM/LEX novos do session .22 (SEM064–073, LEX008) estão TODOS documentados EN+PT com paridade (type-system 51=51, lexical 9=9); os PARSE0xx "faltantes" vivem em `specification-status.md`/`grammar.md` (não é drift). `docs-lang.sh check` 0/0/0. **PRÓXIMO PASSO:** (1) **D-DIAG-EN batch-final** — `BuiltinCallTyper:276` ("sem tipo — declare o tipo do parâmetro da lambda") + `KofInterpreter:97-98` (UI002) + teste-de-fogo; elegível quando passarem >24h dos toques `.22` (07:28/08:47 UTC de 17/09 → **≥ ~08:00 UTC de 18/09**; conferir `git log -1` nos 2 ANTES); prova = suíte verde + `grep '"[^"]*[áéíóúâêôàç]'` zerado nos 2. (2) Ver comentário novo no #324 (adjudicação política PT-citação) — sem re-postar até lá. (3) Nada mais na fila desta lane: #362/#302/Cluster A/parked têm dono; re-trigger sem (1) pronto e sem item novo = RECUSAR (condição STABILITY parcial — a suíte verde + 0 órfãos na lane é a prova). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18); cluster typer/erasure/SAM .22; parked nullable/Map-absent; PR #358 (dono outrolane).

> **✅ FEITO (18/09 ~17:20, dono = 192.168.100.15, lane bugs-and-gaps/docs): CORRUPÇÃO DE MARKDOWN DO `ceccedd6` (09/09) REPARADA — a remoção mecânica de "semântica congelada" deixou markup VAZIO (`**  **`, `(  0.2.6-beta`, `"  "`) em 15 arquivos, incluindo o GUIA OBRIGATÓRIO `AGENTS.md` (regra 6 + condições de parada #1 + item 4) e a referência da linguagem.** Achado por varredura de artefatos (`git grep` de `**   **`/`(  `) durante a auditoria doc×código desta lane. A frase substantiva foi DELETADA, deixando o rótulo em branco: `6. **  (0.2.6-beta).**` (regra 6 do freeze), `1. **  em jogo**` (condição de parada #1), `violate " "` (item 4), `congelado (regra de   0.2.6-beta)`/`Stable exige   (regra 0.2.6-beta)` (README + specification-status), `(  0.2.6-beta)` no DD-01 e no scoped-resources-plan, e `à  .` num COMENTÁRIO do `StatementAnalyzer.java`. **Fix (restauração, não invenção):** a redação pré-`ceccedd6` foi recuperada do git (`git show ceccedd6~1:<file>`) e o rótulo substantivo recolocado — PT `semântica congelada` / EN `frozen semantics` — mantendo a versão `0.2.6-beta` intacta (o commit NÃO queria mudar o número, só o rótulo; a versão viva já foi bumpada para `0.4.0-beta` depois por `fbc8673a`). **Arquivos (EN+PT):** `AGENTS.md`(×3)/`.pt_BR.md`(×3), `docs/language-reference/README`+`specification-status`, `docs/decisions/DD-01-finally-return`, `docs/development/future/scoped-resources-plan`(×4), `docs/bugs-and-gaps/known-bugs` (§199 "de design"), `docs/stdlib/PLAN-STDLIB-EXPANSION`, + o comentário Java. **Prova (Q4, varredura medida):** scan repo-wide (`git grep`) de 28 padrões de artefato (`**   **`, `(  0.2.6`, `;  (`, `gated by  **`, `violate " "`, `**  em jogo`, `à  .`) → **0 hits** (único hit restante é `ExpressionNewParser:32`, comentário que CITA o token `"(  ) -> Int"` de propósito). `docs-lang.sh check` 0/0/0; `check_500` rc=0; `mvn -o -pl kof-compiler -am compile -q` rc=0 (o comentário Java é a única linha de código tocada). **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; os 3 quentes D-DIAG-EN da `.22` (`BuiltinCallTyper:276` SEM015-lambda + `KofInterpreter:97-98` UI002) seguem em cooldown; fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).

> **✅ FEITO (17/09 ~12:10→12:40, dono = 192.168.100.15, lane bugs-and-gaps): TRIAGEM-MASSA DAS 97 ABERTAS (repro medido no classpath do reator, 87/95 rodadas).** Resultado: **17 issues fechadas nesta sessão com prova** — #373 (fix `856ffba1` + NestedGenericParseTest 3/3, SIOOBE posicional re-medido), #379 (fix `554540925` + OperatorMethodParseTest 2/2, OOM-mata-fork re-medido sem o guard), #426 (já-fixada D-PRINT `58561d1f`, saída `A` == esperado medida hoje), o **lote NOT-VALID #406/407/410/411/412/414/416/419/420/421/425/427** (regra 8/D-NOT-JAVA `3d9e969c`: sintaxe traduzida Kotlin/Java; cada fechamento traz o idiom Kof substituto **compilado/rodado nesta sessão**, não citado de memória). O lote do parceiro (#417/418/424) confirmou o padrão ANTES do meu batch — sem colisão (todas as 12 estavam 0-comentários na hora do fechamento). ⚠️ **Incidente de identidade auto-registrado (regra 3):** os closes+comentários do lote 12 saíram `closed_by=melmonfre` (não bot) — no shell persistente o installation token do App EXPIROU silenciosamente entre as chamadas e o `gh` caiu no keyring sem falhar. As fixações individuais (#373/#379/#426/#369/#413) com `eval` na MESMA chamada saíram bot corretamente. Conteúdo/evidência OK; autoria `melmonfre` é fallback **válido** pela regra 8 (16/09, auth do app falhou → identidade do mantenedor aceita) — o problema real é só a DEGRADAÇÃO SILENCIOSA (sem sinal de que o token morreu). **Prevenção:** todo bloco `gh` de escrita exige `eval "$(scripts/gh-as-agent.sh token)"; gh api user` na MESMA tool-call — nunca shell "quente". (Não re-abrir nem re-postar: o conteúdo está correto e re-postar polui mais.) **Restante classificado (não meu para fechar):** #397 (args-slot — raiz na alocação de slots `CompilerFunctionLowering:62`/mainArgsList, linha real ainda NÃO localizada; VerifyError engolido pela mensagem JavaFX = regra do launcher, reproduzido) e #423 (ICE `UnsupportedOperationException` em `MemberCallTyper.infer:139` com lambda `{ it % 2 == 0 }` — **typer cluster `.22`, EM CURSO linha 249, NÃO tocar**); #365/#355/#353/#351/#352/#354 (erasure de type-var no emit — mesmo cluster quente do parceiro: `JvmOpEmitter` 08:47 + `ExpressionMethodCallLowerer` 09:41 = split `e71a09e1` dele na frente de genéricos AGORA); #297/371/378/380/383/384/391/393/398/408/409 (Map-absent → null = cluster nullable-primitive PARKED com a mantenedora #259/#278); #369 FECHADA NOT-VALID (`lexical-structure.md` §4.1: interpolação NÃO é claimada — texto literal é o contrato, probe medida; 4 linhas novas no fake-idioms EN+PT `ac1e3cdd`); #413 FECHADA dupe-#298 (bug REAL confirmado: lambda→SAM compila limpo + `IncompatibleClassChangeError` no runtime; reproducer medido postado nos dois). Total da sessão: **17 fechamentos**. #289/291/296/317/351-355/364 (RUN≠esperado, maiorias no cluster parked/hot). Batch-final D-DIAG-EN (2 arquivos quentes) segue aguardando cooldown >24h.

> **✅ FEITO (18/09 ~16:50, dono = 192.168.100.15, lane bugs-and-gaps/CLI): D-DIAG-EN — últimas strings PT user-facing do `kof-cli` traduzidas (fecha o módulo CLI; restam só os 3 quentes da `.22` em cooldown).** Varredura por palavra-lista de TODO literal de `kof-cli/src/main/java` (não só os arquivos que eu tocava): 11 strings PT user-facing sobreviventes → EN, seguindo a decisão D-DIAG-EN (tooling 100% EN; docs seguem EN+PT; **códigos nunca mudam**): `build/run: falha ao ler kofdeps` → `failed to read kofdeps`; `build: falha ao gerar fat jar` → `failed to generate fat jar`; `version/init/info: flag desconhecida` → `unknown flag` (info também `aceita` → `accepts`); `check: (aceita: …)` → `(accepts: …)`; `fmt: N arquivo(s) reformatado(s)` → `file(s) reformatted`; `decompiled … (N falharam)` → `(N failed)`; `script --watch re-executa ao salvar o arquivo` → `re-runs when the file is saved`; `--inspect mostra IR stats sem executar` → `prints IR stats without executing`; `kof script --repl (ou: kof repl)` → `(or: kof repl)`. **Prova (Q1, mesmo commit):** os 3 asserts `r.out().contains("desconhecida")` do `CliFlagStrictnessTest` migraram para `contains("unknown flag")` (regra #3 da decisão: teste que casa TEXTO migra com a unidade; os que casam CÓDIGO não). Medido pós-fix: `version/info/check --bogus` e `init --bogus` → `unknown flag`; `fmt -w` → `1 file(s) reformatted`; `script --help` EN. **Fora de escopo (não é diagnóstico):** `NanoProvider:23` cita `"GNU nano, versão 7.2"` em COMENTÁRIO + fixture de teste (string de locale do nano real, usada para ancorar o regex de versão — mantida de propósito); comentários internos PT seguem permitidos (a decisão é sobre mensagem USER-VISIBLE). Suíte 4 módulos: `kof-cli` 308/0/0, `kof-script` 38, `kof-c` 7, `kof-compiler` 2050/0/16 (16 errors = `Cannot run program "node"`, ambientais). **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; os 3 quentes da `.22` (`BuiltinCallTyper:276` SEM015-lambda + `KofInterpreter:97-98` UI002) ficam para quando o cooldown vencer (últimos toques 17/09 04:28/05:47); fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).

> **✅ FEITO (18/09 ~16:10, dono = 192.168.100.15, lane kof-android/cli): help×parser drift do `serve` — o usage não anunciava `--backend`/`--frontend`, que o parser aceita (EN+PT não se aplica: é código).** Fechando a varredura de flags, achei a classe de drift que o `CliUsageTargetsTest` guarda p/ targets: `CmdServe` parseia `--backend <t>`/`--frontend <t>` (usa em `selectTargets`) mas as DUAS linhas de usage (`CmdServe` help e `Main.printUsage`) diziam só `[--port <port>] [--host <host>]` — o usuário não descobria as flags. **Fix:** ambas passam a anunciar `[--backend <t>] [--frontend <t>]` (EN, toolchain 100% EN). **Prova (Q0/Q1, mesmo commit):** `CliFlagStrictnessTest` 23→**24/24** — `serveUsageAnnouncesEveryAcceptedFlag` (Q0: falha no código antigo, que omitia `--backend`/`--frontend`). Medido: `kof serve --help` e `kof` agora mostram as 4 flags. Sem regressão: `ServePortTest` 2/2, `ServeStaticTest` 3/3, `CliUsageTargetsTest` 4/4. **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).

> **✅ FEITO (18/09 ~15:40, dono = 192.168.100.15, lane kof-android/cli): OITAVO+NONO achados — `kof serve` e `kof debug` também ignoravam flags em silêncio (R6); o `serve` ainda não honrava `--port=<n>` (forma `=`) (código + testes).** Re-medição completa (todos os comandos, classpath absoluto) fechou os 2 últimos sem pass-through legítimo: (a) `kof serve Main.kf --bogus` subia o servidor e saía 0; `--port` sem valor idem; e `--port=9999` (forma `=`) **não era parseado** — o servidor caía no default 8080 sem aviso (o `usage` anuncia `--port <port>`; o `=` é o padrão em `build`/`check`/`test`/`config`); (b) `kof debug Main.kf --bogus` abria a sessão DAP ignorando o arg. **Fix (raiz):** `CmdServe` ganha `--port=`/`--host=` (paridade `=`) + ramo final `unknown or incomplete flag`/`unexpected argument` + exit 1; `KofDebug` recusa `args[1]` com `-` e qualquer `args[2]` (`unknown flag`/`unexpected argument`, return 1). **Prova (Q0/Q1, mesmo commit):** `CliFlagStrictnessTest` 20→**23/23** — `serveRejectsUnknownFlag` (Q0: saía 0), `serveRejectsPortWithoutValue` (Q0: saía 0), `debugRejectsUnknownFlag` (Q0: saía 0); medido pós-fix: `serve --bogus`=1, `serve --port`=1, `serve --port=9999`=0 (agora honrado), `serve --port 9999`=0, `serve extra`=1, `debug --bogus`=1, `debug extra`=1. Sem regressão: `ServePortTest` 2/2, `ServeStaticTest` 3/3; suíte 4 módulos `kof-cli` 307/0/0, `kof-script` 38, `kof-c` 7, `kof-compiler` 2050/0/16 (16 errors = `Cannot run program "node"`, ambientais). **Varredura de flags do CLI agora COMPLETA por medição:** só `run`/`profile` seguem aceitando `--x` após o arquivo — contrato de pass-through legítimo (`[args...]` anunciado no usage). **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).

> **✅ FEITO (18/09 ~15:00, dono = 192.168.100.15, lane kof-android/cli): SEXTO+SÉTIMO achados da varredura RE-MEDIDA — `kof lsp --flag` e `kof config gen … --flag` também ignoravam flags em silêncio (R6) (código + testes).** Ao re-medir TODOS os comandos contra o classpath absoluto (lição do dia: medir, não confiar no resumo), dois ainda davam exit 0 silencioso: (a) `kof lsp --bogus` iniciava o servidor e ficava em stdio ignorando o arg; (b) `kof config gen Main.kf --bogus` gerava o template normalmente — a flag desconhecida (e `--target` sem valor, e posicional extra) sumia no loop, que só olhava `--target`/`--output`. **Fix (raiz):** `lsp` recusa `args[1]` com `-` (`lsp: unknown flag`, exit 1); `config` recusa `args[2]` com `-` (posição da fonte) e ganha `--target=<t>`/`--output=<f>` (paridade `=`) + ramo final `unknown or incomplete flag`/`unexpected argument` + exit 1. **Prova (Q0/Q1, mesmo commit):** `CliFlagStrictnessTest` 17→**20/20** — `lspRejectsUnknownFlag` (Q0: saía 0 e abria stdio), `configGenRejectsUnknownFlag` (Q0: saía 0 e gerava template), `configGenStillWorks` (não regride). Medido pós-fix: `lsp --bogus`=1, `config gen Main.kf --bogus`=1, `config gen --bogus`=1, `config gen Main.kf --target`=1, `--target=js`/`--target js`=0, `config gen Main.kf extra`=1. `lsp` puro segue em stdio (rc=0 com entrada vazia). Sem regressão: `LspServerTest` 19/19; suíte 4 módulos `kof-cli` 304/0/0, `kof-script` 38, `kof-c` 7, `kof-compiler` 2046/0/16 (16 errors = `Cannot run program "node"`, ambientais). **Contratos de pass-through NÃO tocados (legítimos, args após o arquivo vão ao programa):** `run`/`serve`/`profile`/`debug` — medidos com `Main.kf --bogus` = rc 0 e `oi`/shutdown (é argumento do programa, não flag do CLI). `c`/`script`/`repl` recusam (`unknown option`/exceção honesta). **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).

> **✅ FEITO (18/09 ~14:20, dono = 192.168.100.15, lane kof-android/cli): QUARTO+QUINTO achados da varredura — `kof build <dir-inexistente>` e `kof build <flag>` saíam 0 (falso sucesso), e `kof build app.kf` (a forma DOCUMENTADA no corpus) era um no-op silencioso (R6) (código + testes).** Re-medindo a lista de flags, três falhas de honestidade do `build`: (1) `kof build /dir/inexistente` → exit 0 + "no .kf/.kof files found" (o `collect` engolia o `IOException` e devolvia lista vazia), enquanto `check`/`test`/`run` já recusam com "not found"; (2) `kof build --bogus` (flag na POSIÇÃO da fonte) → exit 0 + "no .kf/.kof files found" (o `args[1]` virava nome de diretório); (3) **`kof build app.kf` — a forma usada em `docs/debugging`, `docs/targets/KOFANDROID.md` (×3), `learn/native/README` (×4), `training/reference/targets.md` — não funcionava:** `Files.list` de um arquivo lança `NotDirectoryException` → lista vazia → exit 0 "no .kf/.kof files found" (drift doc×código + no-op silencioso). **Fix (raiz, aditivo):** `CmdBuild` agora recusa (a) fonte inexistente com `not found` + exit 1 (paridade com check/test/run), (b) `args[1]` começando com `-` com `unknown flag` + exit 1; e (c) resolve arquivo-fonte para o diretório-pai (convenção Go-like do módulo, igual ao `run`), fazendo a forma documentada funcionar. **Prova (Q0/Q1/Q3, mesmo commit):** `CliFlagStrictnessTest` 14→**17/17** — `buildRejectsNonexistentSource` (Q0: saía 0), `buildRejectsFlagInSourcePosition` (Q0: saía 0), `buildAcceptsSingleSourceFile` (Q0: no-op silencioso; agora `out/Default/Main.class` gerado). Medido pós-fix: dir-inexistente=1, `--bogus`=1, `Main.kf --output out`=0 com `Default/Main.class`, `Main.kf --target js`=0 com `Default.mjs`+`index.html`. Sem regressão: `KofSourceDiscoveryTest`, `CmdBuild*` verdes; suíte 4 módulos `kof-cli` 301/0/0, `kof-script` 38, `kof-c` 7, `kof-compiler` 2046/0/16 (16 errors = `Cannot run program "node"`, ambientais). **Help/doc parity (mesmo commit):** o usage (`CmdBuild.USAGE`, linha do `Main.printUsage`) e as 6 linhas de referência do corpus (`docs/distribution/INSTALL`, `learn/01-installation`, `training/reference/compiler`, `training/tooling/cli` — EN+PT) passaram de `build <dir>` para `build <dir|file.kf>`, fechando o drift do item (3). `docs-lang.sh check` 0/0/0. **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).

> **✅ FEITO (fatia-1; R3 segue 🟡) — reivindicado+concluído 18/09, dono = 192.168.100.18, lane development / R3 FFI): FFI da JVM generalizado — `extern` agora casa ABI escalar de qualquer aridade (diretriz da mantenedora: 'paridade total no FFI', incrementos traçados). Fatia-1 = JVM (target-capable de referência, zero pré-requisito): `CompilerPipeline.isExternBound` deixou a whitelist de 3 assinaturas de 1-arg e aceita QUALQUER aridade sobre {Int, Long, Float, Double, Boolean, String} em todas as posições e no retorno; retorno `String` lê de volta o `char*` nativo. Os 3 helpers `kof_ffi_i/_si/_dd` viram UM downcall genérico `JvmFfiRuntime.kof_ffi(lib, name, sig, Object[])` (FFM/Panama: `sig` compacto `ret+params` → `FunctionDescriptor` via `kof_ffi_layout`, `asSpreader(Object[])`, `MemorySegment.getString` p/ `char*` + guard de NULL); o lowering `ExpressionMethodCallLowerer` empacota args num `Object[]` (`KofNewArray`, boxando primitivos — padrão do `StringFormatCallLowerer` do `String.format`) e `JvmOpCollections.emitKofRuntimeCall` faz unbox/checkcast do retorno boxado (ramo novo `startsWith("kof_ffi")`); gate de prelude `usesExtern` ampliado p/ `startsWith("kof_ffi")`, e `hasRuntimeFn`/`JvmRuntimeCallDescriptors`/`JvmRuntimeReturnDescriptors` roteiam `kof_ffi`. Helpers de assinatura extraídos p/ a nova classe `FfiSignature.java` p/ não estourar o gate ≤500 no `CompilerPipeline` (ficou 473; dívida tolerada NÃO cresceu). **Paridade AINDA NÃO alcançada (honesto, R6/R7):** ABI struct/pointer (design D6 ⛔), callbacks/upcalls, variadics e handles opacos seguem `FFI001` na JVM; JS `FFI002`; Native `FFI001` (§61 — dlopen exige init glibc/TLS que o `_start` cru não faz). Regra 8 (dono): R3/FFI é desta lane por diretriz; §61 (dlopen nativo) = lane nat; D6 (ABI struct) = decisão da mantenedora — nenhum foi tocado. **Prova:** `FfiE2ETest` 5→**9/9** (novos `pow` 2 doubles→`1024.0` multi-arg; `strstr` 2 strings→`world` multi-arg+String-return; `srand(Int)` default-`void` via `kof_ffi_void`; `atol(String):Long`→`labs(Long):Long`→`9` prova o layout `j`/`Long` ponta-a-ponta — o Kof não tem literal `long`, então o `Long` nasce do `atol`) + novo `FfiSignatureTest` **4/4** travando o mapeamento escalar→layout→`Type` de todo o conjunto (incl. `Float`/`Boolean`, cujo caminho genérico de downcall já é exercido pelos e2es de Int/Long/Double/String/void) + os 3 legados (`abs`/`atoi`/`sqrt`) agora pelo caminho genérico; `CompilerDriverTest` 2 asserts obsoletos de whitelist estreita atualizados (`add(Int,Int):Int`/`greet(String):String` ANTES FFI001 → agora bound; o honest-gap passou a `sum(Int[]):Int`, o parse-test virou asserção 'bound, não parse-error'). **SUÍTE COMPLETA no tip (scratch /tmp fora do LSP): +3 `FfiE2ETest` sobre a base, 0 regressão desta alteração (BUILD SUCCESS 6/6, 0 erro), 174 skip; as 2 falhas de whitelist do meu próprio diff foram as únicas achadas e são as asserts obsoletos atualizados acima.** check_500 rc=0, docs-lang 0/0/0. *(número absoluto da suíte propositalmente não pinado — lanes alheias movem o tip; o provado é o delta + zero regressão).* Docs EN+PT: `modules.md`/`.pt_BR` (superfície medida 17/09→18/09), `backend-parity.md`/`.pt_BR` (Deltas 18/09), `IMPLEMENTATION-UNIVERSAL-PLATFORM.md`/`.pt_BR` (R3 progresso), `status.md`/`.pt_BR` (entrada 18/09). **▶ PROVA DE ESCOPO + fix de drift PT (fatia-1.5, 18/09, mesmo dono/lane):** a re-análise das fatias 3.1/3.2 achou que o conjunto escalar estava **afirmado mas não todo provado** — só Int/Double/String/void tinham e2e. Fechei a lacuna MEDINDO (zero código de produção tocado): +`atol(String):Long`→`labs(Long):Long`→`9` e2e (prova o layout `j` ponta-a-ponta; o `Long` nasce do `atol` porque o Kof não tem literal `long`) e novo `FfiSignatureTest` 4/4 (trava `Float`/`Boolean`→layout→`Type` no MESMO caminho genérico já exercido pelos demais). libc não tem call site `float`/`_Bool` limpo/determinístico alcançável do fonte Kof, então `Float`/`Boolean` ficam provados por mapeamento travado + caminho genérico, não por e2e — registrado assim na prosa de `modules` (profundidade da prova). **Corrigido drift honesto EN×PT (R6):** `modules.pt_BR.md` ainda listava `void` como NÃO-bound (contradizendo a própria linha `kof_ffi_void` e o EN, pós-3.2) e omitia callbacks/variadics/handles da lista de gaps; e o rodapé "fatias restantes" do `modules` EN+PT ainda citava `void` como pendente — todos sincronizados com a superfície real. Docs EN+PT no mesmo commit. **PRÓXIMO PASSO (ver §R3-fatias em `IMPLEMENTATION-UNIVERSAL-PLATFORM.md` para a decomposição completa):** (2) fatia 3.6 paridade JS via bridge GraalJS/node (o runner É uma JVM com `java.lang.foreign` no host; browser segue `FFI002` honesto R7) — **próxima executável desta lane** — **escopo MEDIDO nesta passada:** o JS tem pipeline de lowering PRÓPRIO (`JsCallEmitter`/`JsRuntimeOps` + parser `KofJsCompiler`), e `isRuntimeOp` NÃO roteia `kof_ffi_`; abrir 3.6 = (a) ramo JS no `isExternBound`, (b) lowering `extern`→`kof_platform.ffi(lib,name,sig,args)` no caminho JS, (c) ramo `kof_ffi_` em `JsRuntimeOps`, (d) `ProxyExecutable` FFM no host `KofJsRunner` (padrão `KofJsDbBridge`/`OrmBridge`; o downcall já existe em `JvmFfiRuntime`, reaproveitável), (e) E2E paridade byte-a-byte JVM↔JS; browser fica `FFI002` honesto (R7). Frente MULTI-ARQUIVO → merece sessão dedicada; seguir o padrão F1 (bridge no host + gate ainda fechado, zero meio-aberto) para NÃO arriscar regressão no backend JS; (3) fatia 3.7 paridade Native = §61 (lane nat); (4) fatias 3.4 callbacks/upcalls (design de closure + GC rooting); (5) fatias 3.3/3.5/3.8 = decisões de design da mantenedora (handles opacos, variadics, ABI struct D6). Cada fatia: claim próprio + padrão `FfiE2ETest` por target. `void` (3.2) foi landado na mesma sessão. **NÃO TOCAR:** §61 dlopen nativo (lane nat), D6 ABI struct (decisão da mantenedora), #324 PT-citações (mantenedora).
> **✅ FEITO (reivindicado 18/09 ~21:40 → concluído nesta sessão, dono = 192.168.100.18, lane development / fronte-5 KofJS): #83-JS / §132 — sleep cooperativo async no backend JS; o KofJS agora roda o supervisor OTP com paridade plena. **Fix (baixo risco, zero lowering novo de control-flow):** `time.sleep` virou PONTO DE AWAIT — o compilador colore como async o método que alcança `kof_time_sleep` via o fixpoint `computeAsyncColoring` JÁ existente (`JsLoweringContext.ASYNC_RUNTIME_OPS` += `kof_time_sleep`; `JsRuntimeOps` emite `await kof_time_sleep(ms)`); `kofTimeSleep` devolve uma Promise (node/browser: `setTimeout` real; GraalJS: fila `__kofSleepers` + hook `globalThis.__kofSleepStep`) — bloco extraído p/ a nova classe `JsRuntimeTime` (gate REFACTOR-500); a bomba do host (`KofJsRunner.drainActiveTasks`, movida p/ a nova `KofJsAsyncPump`) resolve os sleepers vencidos, `eval(void 0)` p/ drenar microtasks e dá `Thread.sleep` REAL em direção ao deadline — RELÓGIO REAL preservado (sem mudar o modelo de tempo de crypto/cache/security/http/cron). Como o host consegue dormir E avançar microtasks ao mesmo tempo, as tasks spawnadas irmãs/filhas progridem enquanto uma task dorme → o idiom `while(!done){time.sleep}` do usuário dispara os workers. **OTP002 LEVANTADO** (`CompilerSupervisor` não recusa mais `Target.JS`). **Regra 8 (dono):** §132 é o CORE do backend JS (fronte-5, DESTA lane); a biblioteca `kof.supervisor` / plano DD-OTP seguem da lane CONC — só habilitei o runtime, não toquei na semântica do plano. **Prova:** `AsyncSleepJsE2ETest` 3/3 (poll-cooperativo→`true/42`; task-dentro-de-task→`true/7`; sleep simples termina) + `KofSupervisorE2ETest` 16/16 incl. `supervisorJsParity`/`supervisorJsS2Parity` (`APP`→`restarts=2 escaladas=2 fabrica=3 parou vivos=0`); `KofTimeE2ETest` 39, `KofConcurrency2Test` 41, `KofAwaitTest` 8, `SpawnE2ETest` 10 verdes. Suite reator completa (pilar rule-6, rodando). check_500 OK — o split baixou dívidas (`JsRuntimeUiWeb` 589→543, `KofJsRunner` 547→543; baseline atualizado só p/ essas duas reduções), docs-lang 0/0/0. Docs EN+PT: known-bugs §132 ✅ (Causa/Correção/Prova guardam o diagnóstico MEDIDO da sessão — premissa `await` já funcionava, OTP002 estava correto p/ o design síncrono, spike generator→cooperative-sleep), `backend-parity.md` (linhas kof.time + kof.supervisor + spawn/await), `concurrency.md`, `roadmap` §OTP, `development/README` (fronte-4/§132), `planning-otp-supervision` (S2-JS ✅), `status.md` (entrada 18/09).**
> **✅ FEITO (reivindicado 18/09 ~20:10 → concluído 18/09 ~21:15, dono = 192.168.100.18, lane development / fronte-5 KofJS): ICE no JS — `catch (Throwable e)` derruba o compilador (`KofJS: try expected KofTryEnd`, COMP002).** Achado MEDIDO na sessão ORM001 (probe isolado, sem orm/db): `try{…}catch(Throwable e){…}` no target JS → `IllegalStateException` (ICE), sozinho e com `finally`. Escopo medido (scratch `CatchProbeTest`): `catch(Throwable)` JVM✅/JS❌; `catch(Exception)` JVM✅/JS✅; `catch(String)` ✅✅; `finally` ✅✅. **Raiz:** `JsTryParser:65` trata QUALQUER `KofCatchStart` de tipo `"Throwable"` como a catch-all SINTÉTICA que emula `finally` (`StatementLowerer:546`) → `break` sem consumir o corpo do catch do usuário → linha 79 espera `KofTryEnd`, acha resto do corpo → ICE. Colide só com a string `"Throwable"` (marcador do synth); `Exception`/`RuntimeException` têm string diferente e já caem no caminho normal (por isso funcionam no JS). **Fix (discriminador estrutural, baixo risco):** só tratar `catch(Throwable)` como finally-emulation quando o local vinculado for sintético (`rawLocalNames` começa com `#` — o synth usa `#excTmp`; um `catch(Throwable e)` do usuário vincula `e`; `#` nunca é identificador Kof). O catch(Throwable) do usuário passa a seguir o MESMO caminho normal que `catch(Exception)` já usa com sucesso → paridade por construção; o emula-finally continua pego pelo `#`. **Prova (fail-first):** E2E JS `catch(Throwable e)` pegando um `throw` (byte-parity c/ JVM) + reabilitar `jsUniqueConstraintRejected` (o E2E ORM001 que BARRIEI na sessão passada por causa EXATA deste ICE). Guarda de não-regressão: §45/§49/§266 (nested try, finally, continue-label) via `ExceptionsE2ETest` + suíte completa. **Concluído (18/09 ~21:15, Q0–Q7):** fix em `JsTryParser` — um catch `Throwable` só vale como emula-finally quando o local vinculado é sintético (`ctx.rawLocalNames.get(idx)` começa com `#`, que nunca é identificador Kof válido); `catch (Throwable e)` do usuário passa a seguir o MESMO caminho de catch normal que `catch (Exception)` já usa → paridade por construção, zero mudança no emulador de `finally`. Provas no mesmo commit: `ExceptionsE2ETest` +2 JS (helper `runJs` + `jsCatchThrowableRunsBody` + `jsCatchThrowableWithFinally`, que exercita o caso de maior risco — catch Throwable do usuário coexistindo com o synth `#excTmp` do finally) 11→**13/13**; `KofOrmE2ETest` reabilitado `jsUniqueConstraintRejected` 31→**32** (0F/3-skip native) — era exatamente o E2E da ponte ORM001 barrado por este ICE na sessão anterior. `known-bugs` §49 ganhou a face irmã (mesma mensagem `KofTryEnd`, gatilho diferente) EN+PT com a raiz e a prova. **SUÍTE COMPLETA (4 módulos): 2458 = 2105+38+7+308, 0 falha / 0 erro / 174 skip, BUILD SUCCESS** — +3 exato (as novas coberturas), **0 regressão** (nested/finally/§45/§49/§266 todos verdes em 13/13). check_500 rc=0 (JsTryParser cresceu ~+14 mas <600); docs-lang 0/0/0. **PRÓXIMO PASSO:** frentes 1–7 fechadas nesta lane; ICE do `JsTryParser` era o único blocker de teste na ponte ORM001 — sem novos itens na fila desta lane sem decisão da mantenedora (#324 PT-citações; §258 #775/#776 de outra lane). **NÃO TOCAR:** §253-B (.17), #775/#776 (.22), D-DIAG-EN hot (.15), #324 (mantenedora).
> **✅ FEITO (reivindicado 18/09 ~17:10 → concluído 18/09 ~19:30, dono = 192.168.100.18, lane development): ORM001 — `kof.orm` no JS (última residual da fronte-7 D-DEV-PRIORITY depois do DB002; a lane fecha a própria família db).** Escopo MEDIDO antes de reivindicar (não presumido): 13 fns `kof_orm_*` (KofOrm.staticCall: create/save/save_all/find/all/where/where_op/count/count_where/delete/delete_all/page/migrate); lowering `ExpressionOrmCallLowerer` JÁ É SHARED (table/schema/className viram literais no call-site — mesmo shape do DB002); gate único em `KofOrm.supportedOn` (JVM only) → abrir p/ JS; runtime JVM é template de source gerado `JvmOrmRuntime.java` (475 linhas: dialect por URL, quoting, pk, DDL por schemaString, migrate com tabela `kof_migrations`) → PORTAR p/ host `KofJsOrmBridge` (novo, kof-runtime) sobre as MESMAS conexões do `KofJsDbBridge` (registry compartilhado — driver h2/sqlite já no classpath do runner, provado pelo DB001). **Design do bind (reusa o que o DB002 acabou de provar):** rows JSON strings do host → guest `.map(o=>__kof_decode_<T>(o))` (find = 1 row; all/where/where_op/page = lista; save/save_all: obj guest→JSON via `toJSON()` sintético do record (JsClassEmitter.lowerRecordToJson existe) passado como STRING na wire, host lê campos do parse — NUNCA `Class.forName` no host). **FATIAS (cada uma commit próprio, suíte verde entre elas):** F1 = bridge CRUD (create/save/find/all/delete/count + helpers schema/dialect/quoting/pk) + guest binds + E2Es portados de `KofOrmE2ETest` (byte-parity c/ JVM); F2 = where/where_op/count_where/page/delete_all + `orm.migrate` (tabela kof_migrations) + E2Es; F3 = docs EN+PT (backend-parity:67/94, ecosystem-coverage, DECISIONS front-7, roadmap, DATABASE_VISION, stdlib-database, status) + idiom/células de treino + gate final `supportedOn = JVM|JS`. Risco honesto: divergência SQL silenciosa c/ JVM → cada E2E JS compara output com o homônimo JVM do MESMO programa (paridade por construção como no DB001). Se parar no meio, o gate FECHA de volta (orm.* no JS = ORM001 compile-time) — sem estado pela metade. **▶ PROGRESSO 18/09 ~18:10 — F1 FEITA e commitada (`0f1b4bc8`):** `KofJsOrmBridge.java` (339 linhas, SQL-only, reusa `KofJsDbBridge.conn/rowToJson` + helper novo `valueMap(Value)`; replica o TEXTO SQL do `JvmOrmRuntime` por construção) + `KofJsOrmBridgeTest` **6/6** host-level (create/save-insert-pk-gen/save-update/find/all/where/whereOp/countWhere/count/page/deleteAll/saveAll/migrate/unique-lança — mesmas saídas que o `KofJsE2ETest`/`KofOrmE2ETest` JVM reporta) — suíte COMPLETA no estado F1 **2419/0F/0E/173 BUILD SUCCESS** (exatamente +6 = os 6 novos). GATE AINDA FECHADO: nada do bridge é alcançável pelo compilador ainda; orm.* no JS = ORM001 compile-error como sempre (zero regressão, zero meio-aberto). **▶ F2 (próxima sessão, multi-arquivo — é o passo de risco):** (a) `KofJsRunner` registra ~10 `ProxyExecutable` `orm*` sobre o bridge (mesmo padrão `dbQuery` 326-338); (b) `JsRuntimeIo` exporta `kof_orm_*`→`kof_platform.orm*` (mesmo padrão `kofDbQuery*` 199-212); (c) `JsRuntimeOps.isRuntimeOp` já roteia `kof_db_`; adicionar ramo `kof_orm_` (io runtime) + WRAP do retorno tipado com `__kof_decode_<T>` EXATAMENTE como o DB002 fez p/ `kof_db_query` (find/save=1 linha→`__kof_decode`ou null; all/where/where_op/page=List→`.map`); (d) `ExpressionOrmCallLowerer` — no JS o arg `obj` do save precisa virar STRING/object p/ a ponte: envolver o último arg com `.toJSON()` (método sintético já existe — `JsClassEmitter.lowerRecordToJson`) quando `target==JS && save/save_all && !primitivo`; E na wire JS NÃO empurrar `className` (a ponte lê campos do objeto, não Class.forName) — o gate `className` só p/ JVM/Native; (e) `KofOrm.supportedOn`: `JVM || JS`; (f) `orm.migrate`/save_all precisam de lista/JSON p/ host (Value→List no `KofJsRunner`). **Prova F2:** portar os ~10 casos do `KofOrmE2ETest` p/ `runJs` byte-a-byte; se algum divergir, o E2E pega (paridade por construção). **F3:** docs EN+PT (já mapeados no topo) + idiom/células treino + `--target js` smoke. NÃO abrir `supportedOn` antes de (a)-(e) estarem verdes JUNTOS (senão onde/page/migrate rodam fns ausentes = runtime-crash). **▶ ✅ F2 (wiring) + F3 (docs) FEITAS 18/09 ~19:30 — gate ABERTO (`supportedOn = JVM|JS`).** Desvios do plano (todos MEDIDOS): (a) `KofJsOrmRuntime.install(platform)` NOVO (13 `ProxyExecutable`), extraído do `KofJsRunner` — inline ele ia a 659 (≥600, check_500 trava); agora 550; (b) `JsRuntimeIo` exporta `kof_orm_*`→`kof_platform.orm*` + helper `kofOrmSingle` (bind 1-registro c/ guarda null p/ find); o `.toJSON()` do record é chamado NA EXPORT (save/save_all), **não** no lowering — o plano (d) em `ExpressionOrmCallLowerer` ficou DESNECESSÁRIO; o className FICA na wire, a ponte JS o IGNORA (lê o schema); (c) `JsRuntimeOps` ramo `kof_orm_` (`isRuntimeOp` + io-route) + WRAP `__kof_decode_<T>` (List→`.map`, registro único→`kofOrmSingle`) igual ao DB002. **BÔNUS não-planejado (achado na prova):** abrir `supportedOn` habilita TAMBÉM a Query DSL tipada (`User.query(db){…}`) no JS — que baixa p/ `kof_db_queryN` (caminho DB002) mas empurrava className tipado → crash runtime DB002; corrigido em `CompilerOrmSupport` (className vazio no JS, bind via `List<Entity>` ret), provado por `jsQueryDslFiltersOrdersAndLimits` (`2\nLeo\nMel\n1`). **Prova (paridade por construção):** `KofOrmE2ETest` 22→**31** (+9 JS, cada byte-a-byte c/ o homônimo JVM: create/save/find/all/where/where_op/page/save_all/count_where/delete/delete_all + query-DSL; `jsUniqueConstraintRejected` NÃO portado — barrado por ICE **pré-existente** `catch(Throwable)` no JS COMP002, fora da minha fronte, re-provado ao bridge por `uniqueConstraintIsNotSilent` 6/6); `nativeReportsOrm001JsSupported` substitui `nativeAndJsReportOrm001` (native ainda ORM001, JS compila). **Suíte COMPLETA no tip pós-F2+F3: 2431/0F/0E/174 BUILD SUCCESS** (2078+38+7+308 = baseline 2422 do tip `bc328c9c` + 9 exatos; §253-B `.17` landou +3 nativos antes, re-medidos sem regressão). check_500 exit=0 (KofJsRunner 550 tolerado; `JsRuntimeOps` 471→501 dívida tolerada nova <600 — NÃO rebaselinei p/ não congelar crescimento próprio). docs-lang 100%/exit0 (16 arquivos EN+PT). **Correção de premissa:** o claim DB002 rotulou ORM001 como 'fork rule-6 de design (schema host-side, NÃO drop-in do `__kof_decode_`)' — estava **ERRADO**: fechou limpo sem reflection no host (serializa no guest via `toJSON`, binda no guest via `__kof_decode`). O registro DB002 acima fica intacto (histórico). **PRÓXIMO PASSO desta lane:** ORM001 era o último gap code-pure livre da D-DEV-PRIORITY; re-varrer regressão/doc-drift a cada land alheio; sem item novo livre = STABILITY (recusar re-trigger). **NÃO TOCAR (restam):** §253-B (.17), #775/#776 (.22/§259), D-DIAG-EN hot (.15), adjudicação #324 (mantenedora).
> **✅ FEITO (reivindicado 18/09 ~15:10, dono = 192.168.100.18, lane development): DB002 — `db.query<T>` TIPADO no JS (fronte-7 D-DEV-PRIORITY; a lane fecha a própria família: DB001-FE-JS foi meu).** Design MEDIDO no código (não presumido): o backend JS JÁ tinha `__kof_decode_<T>` (JsClassEmitter.lowerDecodeHelper, aceita string JSON — verificado) + precedente idêntico `json.decode<List<T>>` (JsRuntimeOps `.map(o=>__kof_decode_T(o))`). **Fix (3 sítios, cirúrgico):** (1) `ExpressionDbCallLowerer` — gate DB002 removido (morto); no JS a wire vira UNTYPED (className null — `driver.target != Target.JS`) e `retType=List<T>` segue carregando o tipo p/ o typer/field-access; (2) `JsRuntimeOps` — call `kof_db_query*` com returnType `List<Classe>` conhecida → `decodeHelpers.add` + wrap `.map(o=>__kof_decode_T(o))` (paridade: na JVM `db.query<T>` e `json.decode` compartilham `kof_json_bind`); (3) bridge `KofJsDbBridge:104` throw DB002 = defesa-em-profundidade R6 (FICA; `typedQueryFailsLoudNeverSilent` permanece VÁLIDO sem edição). Removidos os 2 métodos-mortos `KofDb.typedQueryUnsupportedOn`/`typedQueryGapCode` (0 chamadores). **Prova (fail-first MEDIDO):** antes do fix, `KofDbE2ETest` ia 27/0 mas o assert antigo `nativeSupportsSqliteAndJsReportsDb002OnTypedQuery` EXIGIA o compile-error DB002 → ao remover o gate ele FALHOU (1 failure, documentado); invertido p/ `...JsCompilesTypedQuery` (agora asserts compila + sem DB002) + 2 E2Es NOVOS `jsTypedQueryBindsRecord`/`jsTypedQueryAllRows` (H2 em-mem, saida byte-a-byte c/ os `typedQueryBindsRecord`/`typedQueryAllRows` da JVM: `1\n7\nAda` e `3`). `KofDbE2ETest` 22→**24** (0F/0E, 4 skip condicionais cross/nat, os 5 typed-run rodaram). Suíte COMPLETA no ESTADO EDITADO (clone scratch /tmp fora do LSP) **2413/0F/0E/173 BUILD SUCCESS** (2060+38+7+308 — exatamente +2 sobre o baseline do tip = os meus 2 E2Es novos); check_500 exit=0; docs-lang 0/0/0. Docs EN+PT sincronizados: backend-parity:94, ecosystem-coverage:203, DECISIONS front-7, roadmap §387, DATABASE_VISION:220, stdlib-database:107/113/status-l7, stdlib:69, known-bugs §193-prose; status.md/.pt_BR 235/368/606 (contagem KofDbE2ETest 22→24). **Lição (registrei no próprio claim):** o claim original de DB002 foi PERDIDO por um `git pull --ff` que sobrescreveu DOING.md ANTES de eu commitar a linha de reivindicação — regra: COMMITAR O CLAIM EM HEAD PRÓPRIO antes de tocar código, não junto com o código. **PRÓXIMO PASSO desta lane:** DB002 era a última frente code-pure do D-DEV-PRIORITY aberta p/ ESTA lane com dono livre; `kof.orm` no JS = `ORM001` (parede de design rule-6, NÃO cirúrgica — ver abaixo); re-varrer por regressão/doc-drift a cada land alheio. **NÃO TOCAR:** ORM001 (fork rule-6 de design: ORM sobre GraalJS exige schema/migration host-side, não é drop-in do `__kof_decode_`; manter gate honesto), §253-B (.17), #775/#776 (.22/§259), KofInterpreter:97-98+BuiltinCallTyper:276 (.15, D-DIAG-EN quentes), adjudicação #324 PT-citations (mantenedora).
> **✅ FEITO (18/09 ~14:05→14:25, dono = 192.168.100.18, lane development): doc-drift — 20 ponteiros `development/DATABASE_VISION.md` → `stdlib/DATABASE_VISION.md` no ecosystem-coverage (EN+PT, `0e7ac4e7`).** Achado na varredura ponteiro×arquivo do re-trigger (script: listar `docs/<dir>/<file>.md` citados e testar `-f`): a doc MOVEU (stability-conclusion rule) e a tabela de referência do coverage ficou apontando pro endereço velho em 10 linhas×2 línguas. **Não-mexidas (deliberado, regra do transcritor):** `known-bugs.md:6396` ("ported from `docs/development/known-bugs.md` of PR #133" — HISTÓRICO, o caminho velho é fato da época), `known-bugs.md:915` ("decision recorded in `planning-finally-return.md` (DD-01, PROPOSED)" — datado 08/09, e o doc-agora-DD-01 foi DECIDIDO 13/09; editar transcritos datados reescreve história), `future/PLAN-MULTIPARADIGMA.md:404` (prosa FUTURA: "quando Fase 1 fechar move future/→development/" — não é ponteiro quebrado, é plano). **Prova:** `grep development/DATABASE_VISION` em docs/ = 0; docs-lang 0/0/0 (EN+PT simétricos, mesmo diff 20/20). Suíte completa em CLONE FRESCO do tip pós-`270602fa`/`ac998293`/`769f8939` rodando nesta sessão (`suite_tip3.log`). **PRÓXIMO PASSO desta lane:** fila code-pure vazia; DB002 (front-7 D-DEV-PRIORITY, JS typed `query<T>`) = feature cross-layer (KofDb gate + ExpressionDbCallLowerer + bridge JS + typer + E2E), NÃO cirúrgica — avaliar como unidade própria com claim separado na próxima passada; re-varrer regressão/doc-drift a cada land alheio.
> **✅ FEITO (18/09 ~13:05→14:00, dono = 192.168.100.18, lane development): D-DIAG-EN batch 13 — RESIDUAIS QUE OS BATCHES 1–12 DEIXARAM PASSAR (10 strings PT user-facing em 4 arquivos; SEM054 provada VIVA por probe `s[0]` → "`[]` só pega em array…", e SEM053 por `"a" < "b"`).** A claim de "CONCLUÍDA fora-footprint, restam 3 quentes" (`.15`, 17/09 ~09:25) listou SÓ `BuiltinCallTyper:276`+`KofInterpreter:97-98` — mas a varredura de DIACRÍTICO em código-linha (sem filtro de contexto — os erros são multi-linha: `error(` na linha 1, a mensagem na 3) achou +10 em `SemanticAnalyzer:346/354/511/527` (prefixos "método/função" compartilhados SEM045+SEM036), `TypeChecker:61-62/157` (SEM053 inteiro + dica do SEM001), `SemExpressionTyper:118/331-332/421` (SEM027/SEM050/SEM054), `StatementAnalyzer:352-353/451` (SEM058/SEM026). Protocolo D-DIAG-EN seguido: 0 testes citam os textos, EXCETO `SemanticResolutionTest:381` (SEM050 → migrou com a unidade, regra #3); docs EN não citam como contrato vivo (as 2 citações em known-bugs.md:7805/8693 são TRANSCRITOS históricos em bloco de código verbatim — intocáveis); citações PT nos docs `.pt_BR` = adjudicação #324 pendente da mantenedora (NÃO mudei unilateralmente). Excluídos: os 3 quentes da `.15` (dela). **Lição de método (b9 da `.15` era só palavra-lista; aqui faltou a 2ª metade):** o scan com filtro de contexto na MESMA linha deu 0 falsos-negativos porque as chamadas `error(` são multi-linha — varrer diacrítico LINHA-A-LINHA em não-comentário, SEM window de contexto, e decidir user-facing por inspeção. **Prova:** probes 8/8 antes (PT, documentados na reivindicação) → depois EN (`throw requires a String…`, `assignment is a statement…`, `Kof has no operator '<'…`, `` `[]` only indexes arrays… `` etc.) com os 9 CÓDIGOS inalterados (SEM026/027/036/045/050/053/054/058/001 — regra #3); `SemanticResolutionTest` 30/30; suíte completa no ESTADO EDITADO (cópia scratch /tmp fora do LSP) **2389/0F/0E/173 BUILD SUCCESS** (2046+38+7+298 — IDÊNTICA à baseline do tip `ecb5c312` medida nesta sessão, zero regressão); check_500 exit=0 (sem novo FALHOU; delta +2 linhas = só conteúdo de string); docs-lang 0/0/0 (nenhum doc EN/PT citado como contrato vivo precisou de espelho — a dica do SEM001 JÁ estava em EN no closures.md:38/expressions.md:169 ANTES do fix, i.e. os docs descreviam o contrato que o código ainda violava). `Unspecified` mantido com maiúscula = rótulo formal do three-states (README.md:109). **PRÓXIMO PASSO desta lane:** fila code-pure vazia de novo; re-varrer por regressão/doc-drift a cada land alheio; os 3 residuais PT são da `.15` (dela, protocolo `>24h` no próprio texto). NÃO TOCAR: §253-B (.17), #775/#776 (.22/§259), KofInterpreter:97-98+BuiltinCallTyper:276 (.15), adjudicação #324 PT-citations (mantenedora).

> **✅ FEITO (18/09 ~13:40, dono = 192.168.100.15, lane kof-android/cli): TERCEIRO achado da mesma varredura — `kof bench --verbos` (typo) virava ROOT e reportava "no benchmarks found" (R6 de mensagem enganosa; exit 1, não exit 0) (código + testes).** Diferente de `build`/`test` (que saíam 0), `bench` saía 1 mas pelo motivo ERRADO: o `default` do switch tratava qualquer arg não reconhecido como caminho de benchmark, então um typo de flag virava "não existem benchmarks" em vez de "flag desconhecida" — o usuário caçava benchmarks inexistentes. **Fix (raiz):** ramo `arg.startsWith("-")` no `default` → `bench: unknown flag: X` + `return 1`; posicional (não-`-`) segue virando root (contrato `[paths...]`). **Prova (Q0/Q1, mesmo commit):** `CliFlagStrictnessTest` 12→**14/14** — `benchRejectsUnknownFlagInsteadOfTreatingItAsPath` (Q0: antes a mensagem era "no benchmarks found", a asserção de "unknown flag" falha no código antigo) + `benchWithoutBenchmarksStillReportsHonestly` (exit 1 + "no benchmarks found" preservado sem args). Sem regressão: `BenchTest` 4/4, `CliNumberFormatTest` 5/5, `CliUsageTargetsTest` 4/4. **Auditados e OK (contrato posicional legítimo, exit≠0):** `profile`/`migrate`/`decompile`/`translate`/`compare` tratam `--x` como arquivo e falham com "file not found"/"legacy must be..." (exit 1/2) — não é exit-0 silencioso; `new`/`editor`/`install`/`config` já recusam com usage. **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).

> **✅ FEITO (18/09 ~13:10, dono = 192.168.100.15, lane kof-android/cli): SEGUNDO GAP REAL da mesma linha "ESGOTADA" — `kof test` também silenciava flag desconhecida (R6); a varredura declarada concluída em 18/09 ~07:50 estava ERRADA por medição (código + testes).** A linha do `build` (logo abaixo) já corrigiu o primeiro falso "ESGOTADO"; ao RE-MEDIR cada comando daquela lista (lição: nunca confiar no próprio resumo, medir), `kof test <src> --bogus` = **exit 0 imprimindo "1 passed, 0 failed"** — o loop de args do `CmdTest` só olhava `--target` e ignorava o resto (o comando NÃO faz pass-through ao programa: roda `java -cp <tmp> <main>` sem repassar args, então `--bogus` era descartado). Pior: `--target=js` (forma `=`) também caía no vazio → o alvo pedido era ignorado em silêncio, e `--target` sem valor idem. **Fix (raiz):** `CmdTest` agora aceita `--target=<t>` (paridade com `build`/`check`), `--help`/`-h`, e REJEITA qualquer outro `-x`/`--target` sem valor (`test: unknown or incomplete flag: X (accepts: --target jvm|native|js)`) + posicional extra + exit 1. **Prova (Q0/Q1/Q3, mesmo commit):** `CliFlagStrictnessTest` 9→**12/12** — `testRejectsUnknownFlag` (Q0: saía 0), `testRejectsTargetWithoutValue` (Q0: saía 0), `testStillRunsPlainSource` (não regride; "1 passed, 0 failed"). Medido pós-fix: `--bogus`=1, `--target`=1, `--target=js`=0 (agora honrado), `--target js`=0, `--target android`=1 (recusa existente preservada), `extra.kf`=1, `test <src>`=0, `--help`=usage. **Auditados e OK (não são gap de exit-0 silencioso):** `bench` trata `--bogus` como root posicional e sai 1 ("no benchmarks found"); `profile`/`migrate`/`decompile`/`translate` tratam como arquivo e saem 1 ("file not found: --bogus"); `new`/`editor`/`install`/`config` saem 1 com usage. Suíte 4 módulos: `kof-cli` 296/0/0, `kof-script` 38, `kof-c` 7, `kof-compiler` 2038/0/16 (16 errors = `Cannot run program "node"`, ambientais). **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).

> **✅ FEITO (18/09 ~12:40, dono = 192.168.100.15, lane kof-android/cli): GAP REAL — `kof build` IGNORAVA flag desconhecida (e flag conhecida SEM valor) em silêncio (R6); a linha anterior desta lane afirmava o contrário — MEDIDO e corrigido (código + testes).** O DOING de 18/09 ~07:50 dizia que a varredura de flags do CLI estava "ESGOTADA" e listava `build` entre os que "recusam flag desconhecida" — **FALSO por medição**: `kof build <src> --bogus` = exit 0 sem mensagem e `kof build <src> --min-sdk` (sem valor) = exit 0 (o arg caía no vazio do loop, que não tinha ramo `else`/desconhecido), enquanto `check`/`fmt`/`inspect`/`init`/`info`/`version` já recusavam. Contraste medido: `--target-sdk=abc` → exit 1 (guard pós-loop), `--apk --target jvm` → exit 1, `--fat` non-JVM → exit 1 — só o flag desconhecido/valor-faltante passava batido. **Fix (raiz):** ramo final no loop de args do `CmdBuild` — `--help`/`-h` imprime o usage e sai 0; qualquer outro `-x` → `build: unknown or incomplete flag: X (see 'kof build --help')` + exit 1; posicional extra → `build: unexpected argument` + exit 1. `USAGE` extraído p/ constante. **Prova (Q0/Q1/Q3, mesmo commit):** `CliFlagStrictnessTest` 6→**9/9** — `buildRejectsUnknownFlag` (Q0: saía 0), `buildRejectsFlagMissingItsValue` (Q0: `--min-sdk` sem valor saía 0), `buildStillBuildsPlainSource` (não regride; `dist/Default/Main.class` gerado). Medido pós-fix: `--bogus`=1, `--min-sdk`=1, `--bogus value`=1, `--target-sdk=abc`=1, `build --help`=usage. Suíte 4 módulos: `kof-cli` 293/0/0, `kof-script` 38, `kof-c` 7, `kof-compiler` 2038/0/16 (16 errors = `Cannot run program "node"`, ambientais). **PRÓXIMO PASSO:** re-varrer doc×código a cada land de outra lane; fila code-pure desta lane vazia (§268 face ampla = lane compiler `.22`, regra 6; `--aab` full = bundletool ausente; ícone = decisão pendente). **NÃO TOCAR:** `docs/development/DECISIONS.md` (lane .17); §252/§253-B (nat .18); §258 (development .18).
> **✅ FEITO (17/09 ~22:15, dono = 192.168.100.15, lane bugs-and-gaps/docs): TICK R1-verificado + triagem da fila-0-comentários + §270 catalogado (2 autocorreções Q5 no caminho).** (1) **R1 machine-gated PUSHED + CI-VERIFIED** (`5f1422c6`; run de push CI "Build + Tests" = SUCCESS): `scripts/check_stdlib_boundary.sh` + ledger `scripts/stdlib_boundary.txt` (31 namespaces: 8 base-stdlib, 23 platform, `excluded` documentado p/ ruído keyword/tooling `super`/`this`/`target`/`mvn`) — namespace novo sem linha no ledger quebra o build; heavy-domain HARD-DENY; `--selftest` planta sonda `kof.ml`+`kof.quantum` e exige VIOLATION+rc=1 (se o gate não morder, o selftest falha); wired na CI logo após `check_500.sh`; AGENTS invariant 1 EN+PT espelhados. (2) **Dossiês medidos no jar do tip pós-`mvn install`** (lição `.22`: re-medir SEMPRE no tip — `6e7aecb5`/#362 não alcançou nenhum dos 4; os 4 RECONFIRMADOS quebrados): #400 SEM021 false-reject de classe→interface genérica implementada (bot `5721254206`, raiz = passada de substituição faltante), #390 SEM043 idem face override `put(T)`/`put(Int)` (bot), #389 parser de fn-type com arrow aninhado engole o 2º parâmetro — additive, owner compiler lane (bot), #401 aceite-falso `List<Int>`→`List<String>`: `check` limpo + CCE runtime verbatim (bot). (3) **§270 EN+PT** (`b49112f4`/`caea499a`): registro do cluster — uma raiz, dois contratos: #400/#390 aditivos (compiler lane pode atacar agora); #401 rejeição real = muda código que compila hoje = regra-6/freeze-1, decisão da mantenedora; NOT-TOUCH declarado nos 4 issues. (4) **AUTOCORREÇÕES Q5** (alegação não-medida não vale): (a) nota inicial do #401 dizia "rc=1 silent crash" — era artefato do grep do harness; o verbatim imprime CCE como o issue descreveu; dossier corrigido no lugar (`updated 21:13`); (b) cauda de §267 duplicada no EN + linha §270 duplicada no índice PT — ambas restauradas/removidas. docs-lang 0/0/0 em cada commit; 0 markers; 59 abertas. **(5) **Re-check pós-`db3b2593` (tip `435b7013`, jar novo):** #400 face-1 FIXADA (aceitação `Converter<Int,String>` passa; o verbatim agora morre no dispatch `NoSuchMethodError` = §271/Cluster A, owner `.22`); #390/#389/#401 inalterados (re-medidos). Atualizações postadas nos 4 issues (bots) — o ponteiro do re-trigger agora é: NÃO re-medir esses 4 até outra lane mexer no typer/BFS. (6) **Sincronização plan×código (re-trigger 21:5x–22:1x):** `PLAN-UNIVERSAL-PLATFORM.md` EN+PT — nota datada no preâmbulo do §15 superando o "no item below is an action" (D-UNIVERSAL) + R1 marcado ✅ FEITO com a prova do gate/CI (`ee26515b`); `roadmap.md` EN+PT — estado R1 na fila D-UNIVERSAL (`e7d4808d` + PT no mesmo dia); CI do tip com código (`a85afcb7`) = SUCCESS com o step Gate R1 no job; runs docs-only seguintes em curso. (7) **Re-trigger 22:15:** nova issue #431 (expandir FFI p/ bindings reais — raylib) triada com medição no tip: `FFI001`/`FFI002` honestos CONFIRMADOS (R6 ok — não é bug); mapeada como face prática do **R3** do plano universal (item 1–2 = widening aditivo da whitelist JVM `isExternBound`; item 3 = R3 design; item 4 = §61 já catalogado com o caminho provado); owner = compiler lane (JVM-first) + nat lane (§61); mantida ABERTA como tracking face. Resposta técnica postada (bot `5721977913`). (8) **R3 no plano ganhou a primeira fatia concreta (EN+PT, `89693235`)** derivada da triagem da #431: 5 incrementos ordenados na JVM (arity → void → String-return → char*-misto; native = §61), todos aditivos e prováveis no padrão `FfiE2ETest` — todo de implementação pronto p/ a lane compiler reivindicar. (9) **Re-trigger 22:26 UTC — RECUSADO (registro honesto, sem trabalho inventado):** sem novo commit no remoto desde `048b1d48`, sem issue novo (fila 0-comentários inalterada desde #431), sem comentário novo de dono, CI do tip em curso; D-DIAG-EN continua BLOQUEADO (cooldown `.22` = `dbe3c51c` 08:47 UTC 17/09 → elegível ~08:47 UTC 18/09, faltam ~10h). STABILITY GLOBAL não vale (§252/§253/§266/§267/§268-amplo/§270/§271 abertos em outras lanes) → cron MANTIDO; próxima ação real da lane = D-DIAG-EN batch-final quando o cooldown expirar (conferir `git log -1` nos 2 arquivos ANTES). (10) **Re-trigger 22:3x–22:4x UTC — doc-drift caçado e corrigido (2 commits, ambos EN+PT, docs-lang 0/0/0):** `746a80f0` sincroniza a linha 8 do índice `docs/development/README` com o código (R1 ✅; 1.5 OTel ✅ landado `435b7013` — a linha ainda dizia "parecem sem dono"; estado real de 1.1/1.2/1.4) e `b1280085` fecha a linha "falta OTLP export" da tabela de pendências. Verificadas SEM drift: §260/§267 reavaliados da `.17 nat` (não colidem com §270/§271), matriz `kof.observability`/`kof.media` batem com o código. Fora isso o re-trigger segue recusado quanto a código: D-DIAG-EN em cooldown até ~08:47 UTC 18/09; fatia R3/FFI = compiler lane (`.22` ativa). (11) **Re-trigger 22:45–22:5x UTC — gap real de corpus CAÇADO e PREENCHIDO (não foi recusa):** `extern`/FFI-C é feature viva (medido no tip: `cos(0.0)`→`1.0` JVM; multi-arg→`FFI001`; JS→`FFI002`; lib ausente→exceção `kof_ffi_*` em runtime) mas era **100% indocumentada** no `language-reference` (0 ocorrências de `extern` em grammar/syntax/modules/specification-status). Adicionada a célula da superfície REAL medida (whitelist 1-arg `Int→Int`/`String→Int`/`Double→Double`; diagnósticos honestos; widening = fatia R3/#431; `extern "c"` native depende §61) em `modules.md` §6 Interop + linha em `specification-status.md`, **EN+PT espelhados**, docs-lang 0/0/0. Pendência remanescente da caça: célula de **gramática/sintaxe** (`grammar.md`/`syntax.md` ainda sem produção `extern`) — doc-only, próxima fatia da lane se nada mais urgente. Cooldown D-DIAG-EN mantido (~08:47 UTC 18/09). (12) **Caça ao gap FFI completada na célula seguinte (`6671eb9b`):** produção `extern-declaration` extraída de `Parser.parseExternDeclaration` (:197) adicionada a `grammar.md`+PT (EBNF: lib string opcional, sem corpo, `;` opcional, aridade aceita pela gramática — whitelist é do pipeline) + alternativa no `top-level-declaration` + frase "Only type and function declarations" corrigida (já estava imprecisa para `test`/`application`). docs-lang 0/0/0. Gap corpus extern→0 ocorrências → completo em reference (modules §6 + spec-status + grammar). Resto da pendência (11) zerado. Próximas ações reais da lane: D-DIAG-EN (~08:47 UTC) ou drift novo de outras lanes. (13) **Re-trigger 23:0x UTC — fecho da caça FFI (`22c6d47a`):** forma concreta `extern` adicionada a `syntax.md`+PT (exemplo `cos`/libm medido compila+roda; whitelist/runtime/gaps na prosa) — o caderno de formas já cobria test/application/annotations e só faltava FFI. CAÇA EXTERNA ENCERRADA: `extern` agora existe em grammar+syntax+modules §6+spec-status (0→4 doc-views, EN+PT, docs-lang 0/0/0). Verificado SEM drift: productions test/application/import/package presentes; fila de issues/comentarios sem novidade; CI 399d0231/6671eb9b/65ac2d69 em curso. Lane sem item executável agora (cooldown D-DIAG-EN ~08:47 UTC) → próximo re-trigger sem drift/alheio = recusa registrada. (14) **Re-trigger 23:10 UTC — RECUSADO (honesto, sem trabalho inventado):** nenhum commit novo desde `1d678faa`, nenhuma issue/comentário novo (fila idêntica desde #431), CI dos meus docs-pushes completando `success` (ex.: `399d0231`), ORM001-JS da outra lane já verificado sem drift no tick anterior, D-DIAG-EN em cooldown até ~08:47 UTC 18/09. Lane sem item executável; global não está estável (§252/§253/§266–§271) → cron mantido. (15) **Re-trigger 23:1x UTC — caça continuou no eixo training/: criada `training/idioms/interop.md`+PT (`168f0d7a`)** com exemplos MEDIDOS no tip apos pegar a propria alucinacao (`atoi2` nao existe em libc → runtime fail; corrigido p/ `atoi` real → `42`): whitelist 1-arg, FFI001/002, bridge-C workaround (#431), "nao emita bytecode na mao". Tree do README de training corrigida (faltavam stdlib/ui/interop — drift antigo) + area `interop` registrada na lista de areas do AGENTS EN+PT. docs-lang 0/0/0. Caça FFI completa em 6 vistas: grammar+syntax+modules+spec-status+idioms/interop+AGENTS-list. **COMANDO DIRETO DA MANTENEDORA 23:4x UTC ("continu com o resto das issues, tem pr aberto"): PR #358 levada até o fim.** (a) reboot não-intencional limpou /tmp/opencode → ambiente recriado em `/home/mel/kof-work/kof-tip6` (clone depth-500 da tip `53e87b9b`); (b) **conflito do PR resolvido**: merge da tip na branch (único conflito = `DOING.md`; resolução preserva o registro do tip + as 10 linhas de rodada do bug-hunt externo dela, 0 markers); (c) **itens 1–4 do review**: item1 = teste de regressão `ClassExtendsInterfaceE2ETest` 3 casos com **prova Q0 dos dois lados** (sem o fix dela: 2/3 VERMELHO com `IncompatibleClassChangeError: class Dog has interface Animal as super class` literal + guard verde; com o fix: 3/3 verde) — commit separado com a evidência; item3 = drop do churn (VERSION/pom/version.properties/CHANGELOG/workflows restaurados ao tip); item4 = stale-base resolvido pelo merge; item2 = claim SEM045 corrigida nos comentários (diff nunca tocou checker;warning futuro = decisão aditiva à parte, regra-6); (d) **rota de entrega forçada pelo GitHub**: push da resolução no fork dela BLOQUEADO pelo GitHub ("refusing to allow a GitHub App to create or update workflow `.github/workflows/ci.yml` without workflows permission") — o merge inevitavelmente atualiza workflows; app sem perms de workflows NÃO consegue resolver conflito de PR alheio neste repo; entregue então como **draft PR #433** (branch `pr358-resolved` no repo principal, árvore idêntica à resolução, authorship dela preservada no merge; comentários técnicos postados em #433 e #358 com o motivo da rota); decisão de fechar #358 em favor de #433 = dela; (e) suite completa pós-merge em execução (log `suite2.log`). #431 já respondida (bot 23:37, demo pong da comunidade elogiado + estado R3). (17) **CI do #433 consertado + FALHAS MINHAS AUTOCORRIDAS (diretriz da mantenedora: corrigir e FECHAR, nao abrir).** (a) `Build + Tests` fail em ~25s = gate ≤500 CRITICO legitimo: a fix do colaborador levou `CompilerClassLowering.java` 599→615; resolucao = realocacao do super-interface extraida p/ `ClassSuperResolution.java` (nome por responsibilidade; CCL volta a EXATAMENTE 599 = baseline nao cresce; `check_500: OK`; teste-alvo 3/3 verde no formato novo; push 6e6f845b, CI novo em curso). (b) **#435 e #436 FECHADAS como FALSE POSITIVES minhas**: as duas medições "native/js quebrados" sairam de um WORKTREE VELHO SEM o fix (a propria licao `.22` que eu registrei — re-medir no tip — me pegou; raiz do erro: medi em `246897ce`/tip `e8fd549a` onde o lowering NAO estava). Re-medido NA ARVORE do #433: native linka e o ELF roda `woof`; js roda `woof` (com GraalJS no CP); o fix JVM e o lowering COMPARTILHADO — uma raiz, nao tres. Coment de correcao postado nas 2 + paridade corrigida no #433. (c) #302 fecha no merge do #433 (corpo ja diz Closes #302); merge = aguarda CI verde do head novo + chamada da mantenedora (PR esta draft por policy). (18) **FECHAMENTO DO CICLO #302 (diretriz "corrigir e fechar"):** mantenedora MESCLAL #433 (`5ec9747d` 01:30 UTC) e fechou #358; **#302 fechada por mim com a evidencia completa** (merge SHA + teste Q0 + CI green + matriz 4-targets medida na arvore mesclada: jvm/native-ELF/js/script = woof). #435/#436 ja fechadas como falsos-positivos autocorrigidos. Proximo passo da diretriz: cruzar merges recentes do tip com issues abertas que ficaram para tras (fix landado, issue viva) e fecha-las com re-medicao honesta. (19) **Sweep "fix landado × issue viva" COMPLETO (diretriz corrigir-e-fechar):** cruzados os ~40 commits do tip com as 56 abertas — unicos fixes landados sem close eram #338/#345/#362 (ja fechadas pelas lanes proprias) + #302 (fechada agora, item 18) + WEB005/serveDir `b4957c06` (matriz EN+PT ja sincronizada pela propria lane, verificacao sem drift). A aberta restante apontada pelo grep (#431) e tracking-intencional do R3 (fechar so quando o widening landar). **#431 ganhou comentario novo da comunidade (demo pong raylib via bridge-C) ja respondido pelo bot 23:37.** Ciclo da diretriz FECHADO: #302 closed, #435/#436 closed (autocorrigidas), #358 closed (mantenedora), #433 MERGED `5ec9747d`, CCL 599=baseline, CI Build+Tests completed/success. (20) **SWEEP "corrigir e fechar" ENCERRADO — nada mais fecha sem invasao de lane ou regra-6.** Estado das ABERTAS apos o ciclo #302/#433: Cluster A/erasure+dispatch (#352-#401, ~15) = `.22` (dossies postados); lambda/SAM (#293-#310) = compiler lane (varredura .22 triou); nullable/Map-absent parked (#370-#409, ~16) = decisao mantenedora (D-NULL-INTENT); #382/#386 = mantidas OPEN pela TRIAGEM DELIA 15:47 (feature stdlib, nao syntax); #324 = D-DIAG-EN (minha, cooldown .22 ate ~07:28 UTC); #431 = tracking feature R3 (mantenedora+compiler/nat lane); #333 = rule-6 (void return value = contrato congelado, decisao dela). Web lane landou WEB005 (`b4957c06`, mel) = matriz ja sincronizada (verificado linha 98 EN+PT — sem drift p/ minha lane). BOTS/E2E CI do tip: `Build + Tests` do head mesclado do #433 = success (suíte completa no CI). (21) **FECHO COM PROVA NA ARVORE MESCLADA:** `ClassExtendsInterfaceE2ETest` roda no tip novo e da 3/3; o repro exato da #302 foi re-medido na arvore mesclada: JVM `woof`, Native (ELF linkado, `file` confirma x86-64 executavel) roda `woof`, JS (Graal no CP) `woof`, script `woof` — a matriz de 4 targets batendo no tip, como prometido no comentario de fechamento da #302. Trilha de fechamento verificada: `closed_by=kof-agent-worker[bot]`, `state_reason=completed`, comentario de evidencia 941 chars na #302. #435/#436: `closed_by=kof-agent-worker[bot]` com comentarios de autocorrecao (e o comentario completo do #435 que a shell tinha picotado, repostado). Nada mais em aberto que a mantenedora possa querer fechado por fix landado alem do Cluster A/.22 (pending por dono, nao por mim).(22) **Loop fechado do R6 machine gate (`19a740f2`, mantenedora):** o gate nasceu do drift §275 mas a linha do §275 na known-bugs nao citava a guarda — anotacao **Guard (machine)** adicionada ao final do §275 EN+PT (ledger auto-derivado, pin novo sem linha de paridade = RED; classe phantom-code §275/HTTP003/UUID002 sob maquina). Gate re-medido no tip com o incantamento correto (`-pl kof-compiler -am test -Dtest=DomainGapCodesTest`): **11/11 verde** (a tentativa sem `-am` falhou por resolucao de deps, nao pelo teste — Q4 honesto). Docs do proprio commit ja estavam sincronizadas pela lane dela (README/PLAN EN+PT). CI tip: Native cross success; Build+Tests in_progress. Cooldown D-DIAG-EN RE-CHECKADO em UTC: ultimo toque 17/09 07:28/08:47 UTC (= 04:28/05:47 -03); agora 18/09 ~03:50 UTC — **NAO expirado** (~3h40 a 5h restantes; 24h por toque, janela de execucao ~08:47 UTC 18/09). Ao expirar: batch final dos DIAGNOSTICS emitidos ao usuario (linhas 276/97/98 do BuiltinCallTyper/KofInterpreter; comentarios de codigo PT ficam — escopo #324 e mensagem vista pelo programador, nao prosa interna). (23) **D-DIAG-EN batch-final executado (cooldown vencido — toques 17/09 07:28/08:47 UTC, agora 18/09 ~04:30 UTC):** os 2 unicos DIAGNOSTICS user-facing ainda em PT na toolchain traduzidos — `BuiltinCallTyper:276` extra SEM015 ("(untyped — declare the type of the lambda parameter)") e `KofInterpreter:97-98` warning UI002 ("kof.ui does not render on the script target ... run with --target=js for real UI"). Comments de codigo PT ficam (escopo #324 = mensagem vista pelo programador). Varredao programatico (regex sobre string-literals com diacriticos nos 5 modulos main, excl. comentarios): **ZERO strings PT user-facing restantes**. Prova: Q2 `mvn -o -pl kof-compiler -am compile -q` verde; `SemanticResolutionTest` 30/30 + `LambdaInGenericContainerE2ETest` 5/5 (SEM015), `KofScriptTest` 25/25 (UI002), `TranslateTest` 61/61 + `DecompileTest` 67/67 (cli). Nenhum teste pinava as strings PT (grep vazio antes); assercoes usam `contains("UI002")`/`"is not a function"` — imunes. #324 fecha na issue com comentario de lote final. (24) **D-DIAG-EN FECHADO — issue #324 closed com evidencia medida no tip apos `f16fe520`:** as 4 re-producoes do corpo da issue (SEM010/014/033/036) dao exatamente o formato Expected (ingleS, sem `toString()` de AST); sweep programatico final nos 5 modulos main = ZERO strings PT user-facing (4 hits restantes sao comentarios asm/javadoc, fora do escopo da issue); batch-final `f16fe520` traduziu os 2 ultimos diagnostics (SEM015-extra + UI002) com as 35+61+67+25 provas acima; comentario de fechamento postado via --comment-file (licao do #435: nunca interpolacao de shell em comentario). NEXT STEP desta lane agora = so monitoracao (doc-drift de landings alheios + recusar re-trigger ocioso).(25) **#324 FECHADA (diretriz corrigir-e-fechar, 3a unidade do ciclo):** cooldown .22 VENCIDO (toques 17/09 07:28/08:47 UTC; agora 18/09) -> batch-final executado: ultimos 2 diagnostics user-facing PT da toolchain viraram EN (`BuiltinCallTyper:276` extra SEM015, `KofInterpreter:97-98` warning UI002) no commit `f16fe520`; varredao programatico (literals com diacriticos em src/main dos 5 modulos, excl comentarios) = ZERO user-facing PT; as 4 reproducoes do CORPO da issue re-medidas no tip saem no formato Expected exato (SEM010/014 sem repr crua, SEM033/036 EN com posicao) -> close com comentario de evidencia. Prova Q: compile Q2 + SemanticResolution 30/30 + Lambda 5/5 + KofScriptTest 25/25 (ui002 imune a troca) + cli Translate 61/61 + Decompile 67/67; nenhum teste pinava as strings PT (grep vazio antes). OBS honesto no comment: SEM010/014 ainda `:0:0` = lacuna de POSICAO, fora do titulo da issue (repr+idioma), nao e reabertura.(26) **AVALIACAO DE STABILITY do re-trigger 02:31 UTC (sem edicao — recusa honesta com motivo):** CI do code-commit `f16fe520` = Build+Tests **completed/success** (unidade #324 verde no CI, nao so local). Sweep `c5897cd5` da mantenedora NAO abriu drift na minha lane: guarda do §275 intacta EN+PT (verificada), PLAN/tabelas ja sincronizados por ela mesma. `bc328c9c` (.17, §253 face B) e da lane nat — §253 fica OPEN ate re-medicao do intervalo no tip (dono .17, nao mexi). **OBS NOVO registrado (nao-editado):** SEM010/014 com posicao `:0:0` (emissores em `StatementAnalyzer`/`TypeChecker` = footprint typer da `.22`, NOT-TOCAR; diretriz "nao abrir issues") — fica aqui como gap-plan da lane `.22`/ISSUE-LANE, com o contexto ja medido no comentario de fechamento da #324. Fila da diretriz para a lane 15 segue ENCERRADA; proxima janela real = novos landings alheios (doc-drift) ou decisao da mantenedora (rule-6 do #386/#382/#401/#333/#431).(27) **RE-TRIGGER 02:31 UTC (prompt cron) — acao da lane 15, nao recusa:** unicos itens novos no tip eram `c5897cd5` (ledger sweep R6 da mantenedora) e seu CI da tip em andamento; verificados (a) integridade da guarda do §275 EN+PT apos o sweep dela — intacta, `git show c5897cd5` mostra que ela mesma escreveu a linha Guarda com o SHA do gate; (b) `bc328c9c`/§253 e `c5897cd5` NAO reabriram nada na lane. Sweep de docs de `development/`: nenhum doc desclassificado (otp=IN PROGRESS com gap real `cancelled()`; native-multiarch=correto). Apos essa checagem: fila da diretriz segue encerrada para a lane 15; proxima janela = CI da tip ou landing alheio com drift.(28) **RE-TRIGGER 02:4x UTC — ANALISE COMPLETA DA FILA + RECUSA (nada e da lane sem colisao/regra-6):** (a) CI do code-commit `f16fe520` = Build+Tests **completed/success** — ciclo #324 100% verde no CI; (b) `bc328c9c` (.17 §253 face B): corpo EN+PT ja esta com ✅ face A 16/09 + ✅ face B 18/09 + Estado 🟢 nos dois idiomas — sem drift p/ sync; (c) `c5897cd5` (ledger R6 da mantenedora): guarda do §275 intacta EN+PT, PLAN/README/http por ela sincronizados; (d) `docs/development/`: `native-multiarch.md` permanece CORRETO em development/ — linha "colunas por arch ⚠️ partial" confere (matriz tem 1 coluna `Native` + faces por-arch em prosa (`SECN000`/`NATIVE_RISCV64`); a separacao total ~60 linhas exige re-medicao por-arch = trabalho da lane nat, NAO documental); `planning-otp-supervision` tem gap real (cancelled()); (e) #389/#390/#394: fixes landados (5dec0dbd etc.) mas o FECHAMENTO pertence ao claim ISSUE-LANE da `.22` (dcd36ce0, dono = triagem/respostas das abertas) — nao disputo. **Proximas janelas reais:** drift de algum landing novo (sweep a cada tick), ou decisao da mantenedora (#386/#382 rule-6; #401 pos-erasure; #333 rule-6).(29) **RE-TRIGGER 02:55 UTC — nada novo na tip (so meus 2 ticks renumerados); recusa mantida, com a checagem do prompt feita ate o fim:** varri as SECOES OPEN da known-bugs (contagem correta no tip = **40 abertas**, a nota "(sweep 13/09 — 32 itens)" do topo do arquivo esta stale como contagem — nao mexi na nota, e linha de historia de data de 13/09). Caco de drift nos landings novos: `§277` e `§278` — AMBOS ja com ✅/status nos dois idiomas (minha leitura inicial truncou a linha a 95 chars e eu ia marcar drift falso; Q5 me pegou antes do commit: li a linha inteira — `247` chars, ✅ FIXED 18/09 `.22` presente). Validei a prova citada no §277: `NestedFnTypeArityTest` **5/5 verde** no tip atual (re-gravao so a medida, nao a linha). §278 (android gap codes) = aberto por natureza, sem dono de sync de docs. CI da tip `02837edc`: in_progress. Fila da lane: vazia — proxima janela = drift de landing novo/decisao da mantenedora/issue nova com fix landado.(30) **AUTOCORRECAO DA (29) (Q5 — nao deixar numero aproximado virar registro):** a "contagem correta = 40 abertas" era estimativa de regex com janela de 4000 chars — contagem robusta por bloco inteiro da known-bugs no tip: **13 secoes abertas de verdade** (a janela da (29) tambem não capturou o `Status ✅ FIXED 15/09` do §253, que fica FORA da lista). As 13: §171 §188 §192 §239 §241 §252 (flake conhecido) §260 §266 §267 §270 §271 (cluster A/.22) §253 reclassificado: FECHA por cabecalho-proprio? NAO — re-media do ciclo completo e da lane nat; fica aberta por seguranca ate eu medir o one-shot idiom no tip (proxima tarefa REAL desta lane, abaixo). A nota storica "(sweep 13/09 — 32 itens)" do topo do arquivo fica intacta = fotografia datada, nao fila viva. **#389 fechada pela ISSUE-LANE `.22`** (claim dcd36ce0) sem disputa minha. #390/#394/#396/#399 seguem abertas SEM fix landado no log recente = cluster A `.22` legitimo. **TAREFA REAL ASSUMIDA:** re-medir no tip (a) one-shot idiom `var id = time.interval(...time.cancel(id))` x4 targets (§253 faces A+B), (b) os 4 dossies da (28) que citei sem medir o resultado de cada um — fechando §253 por evidencia ou mantendo-o aberto com a medida. Sem colisao: e leitura+medição, NENHUMA edicao em arquivo de terceiros.(31) **MEDICAO REAL EXECUTADA (tarefa assumida na (30)): idiom one-shot do §253 re-medido no tip com jars FRESHOS (rebuild completo — o 1o round tinha classes velhas de 17/09 23:01 e quase produziu leitura falsa):** JVM/SCRIPT/JS (Graal) rodam `tick 1..3 / cancelled / end n=3`; NATIVE rejeita com **SEM092** (gate de compile-time INTENCIONAL — StatementAnalyzer:193-202, R6) e o workaround shadow-handle (`var id = \"\"; id = time.interval(...)`) roda no NATIVE com Saida identica (`tick 1..3 / cancelled / end n=3`, ELF). A linha do §253 ja documenta exatamente isso (`.17` no `bc328c9c`: shadow ✅, declaracao self-referente fica gateada BY DESIGN) — **zero drift, nenhuma edicao minha no §253.** RE-CONTAGEM robusta das OPEN da known-bugs (bloco inteiro, sem janela de chars): **12** = §171 §188 §192 §239 §252 §254 §260 §266 §267 §270 §271 §278 — corrige o "13 com §253" da (30) (o Status ✅ do §253 morava a >4000 chars do header, minha janela cortou; §241 tem resolucao no corpo) e o "40" da (29) (era estimativa crua de cabecalhos sem marcador, nao de abertos reais). proxima medicao util = §254 (golden cell vermelha na arvore limpa = dominio matriz/conformance, minha lane) — checar CI: se o job de conformance esta GREEN no tip, a linha do §254 pode estar stale.(32) **DOIS achados fechados na janela do re-trigger 02:55–03:20 UTC:** (a) `§254` — tabela/PT marcavam ✅ FIXED 15/09 mas o CORPO EN da secao nao tinha linha de Status (assimetria EN×PT = doc lane); corrigido em `5fbc216a` apos re-medir `ConformanceMatrixTest` **11/11 verde** no tip (o 1o round "BUILD FAILURE" era o meu `surefire:test` sem `-am` = resolucao de deps, nao o teste — Q4 honesto); OPENs reais da known-bugs agora **11**. (b) §253 re-medido por evidencia (na (31)) sem edicao. Fila da lane 15 novamente VAZIA — recusa mantida para novos re-triggers ate: drift de landing alheio, issue nova com fix landado, ou decisao da mantenedora (#386/#382/#401/#333; §278 a/b).(33) **RE-TRIGGER 03:2x–03:4x UTC — recusa com uma QUASE-FALSA-Correcao evitada (Q5):** suspeitei que a linha 301 do `native-multiarch.md` ("colunas por arch ⚠️ partial") fosse mentira, mas antes de editar conferi a ESTRUTURA REAL da matriz: o header e o separador (linhas 18-20) tem 1 coluna `Native`; as diferencas por-arch (SECN000 risc/aarch, NATIVE002, deltas native.risc/.arm) vivem como LINHAS PROPOSITAIS separadas + mensageria `NATIVE_RISCV64`, nao como colunas — a linha 301 diz a verdade exata ("delta cited, colunas separadas PENDING"). **Nao edite essa linha sem antes criar as colunas na matriz** (quem "corrigir" o ⚠️ sem medir a coluna comete o mesmo erro que eu quase cometi). Outros checks do tick: tabela do topo da known-bugs ja carrega §251/§253/§254 inline (viva, nao stale); zero comentarios novos em #324/#302/#431; nenhuma issue nova desde 03:05; CI de `5fbc216a`/`94598f44` ainda in_progress (monitorar no proximo tick). Fila da lane 15 VAZIA.(34) **RE-TRIGGER 03:40 UTC — landing novo `fd571406` (catalogacao §279 pela mantenedora) VERIFICADO por re-medicao propria, nao memoria:** os DOIS gatilhos do §279 reproducem no tip com jars frescos (A: `Int?` null-check em else-if chain; B: `Boolean?` null-check como ultima stmt de fn void) → `error: Internal compiler error: KofJS: unexpected op in expression statement` nos dois `--target js`. Registro EN+PT simetrico (1 secao cada, docs-lang 0/0/0), owner fix = KofJS lane `.18` (nao-eu: JS-backend NAO e minha lane de codigo; cross-ref em #266 = territorio ISSUE-LANE da `.22`, nao disputo). Contagem de abertas da known-bugs: 11 → **12** (§279). CI: `5fbc216a` success; `884d6985` in_progress.(35) **RE-TRIGGER 03:45 UTC — landing `f738fab1` (value-records/D-VALUE-RECORD) auditado ATE A ULTIMA REFERENCIA e 100% self-consistente pela propria mantenedora:** DECISIONS §D-VALUE-RECORD (16/09) -> roadmap §23 TIER 2.7 com passos 2.7.1–2.7.5 (5 linhas EN + 5 PT) -> `future/value-records-plan.md` EN+PT (76/77, header = "Plan (design only), zero code, gated by R12 + autorizacao explicita" — classificacao tres-estados CORRETA) -> linha 27 das duas READMEs -> SG-022 ja em `specification-gaps.md` EN+PT (3 matches cada) com o comment de triagem da #275 referenciando o arquivo certo. **Zero edicao necessitaria; zero drift.** #275: aberta, ultimo comment = triagem da mel (16/09) — reply de issue = ISSUE-LANE `.22`, nao disputo. CI `884d6985`/`65631ede`: in_progress (docs-only, sem risco). Fila da lane 15 VAZIA — recusa mantida.(36) **RE-TRIGGER 03:50 UTC — RECUSA (5o ocioso da serie 02:26→03:50; critério global de STABILITY ainda NAO vale: 12 abertas na known-bugs + cluster .22/.18 em curso):** zero commits novos no tip desde `8d81381c`; zero issues/comentarios novos desde 03:44; CI `884d6985` = **success** (o unico code-commit pendente do ciclo, agora verde). Nada acionavel sem invasao de lane ou decisao da mantenedora. Proxima janela: CI de `65631ede`/`8d81381c` (docs-only) ou landing alheio.NEXT STEP (desta lane — leia aqui e continue):** (0) **AGUARDANDO MANTENEDORA**: decidir #358×#433 (fechar o PR dela em favor do finished form #433, ou dar perms de workflows ao app) + CI/suite do #433 postada como comentário no PR quando terminar; (1) cooldown `.22` em `BuiltinCallTyper.java` = `b13611f` 07:28 UTC 17/09 → D-DIAG-EN elegível ~07:28 UTC 18/09 (re-verificar `git log -1` nos 2 arquivos ANTES de tocar); (2) a cada re-trigger: sweep issues/CI + caca a doc-drift; sem item = RECUSAR e registrar. **NAO TOCAR:** cluster typer/erasure .22; parked nullable/Map-absent; DECISIONS.md (.17); §252/§253 (nat); §258 (dev .18).
(37) **AUTOCORRECAO DE 3 ERROS MEUS EM UMA PASSADA (a diretriz "sem memoria, medir" paga o dividendo):** a) **quase-fechei o §275 por falsissima premissa** — fui "fechar HTTP003/UUID002 no §275" sem ler o corpo da secao; o grep do status FALHOU e AI SIM li: §275 = phantom WEB005/serveDir, ja ✅ pela web lane (`b4957c06`); HTTP003 = **§259** (fechado 17/09 pelas fatias asm da `.17`). Nenhuma edicao de status feita. b) **name alucinado no meu proprio guard do tick (22)**: citei `checkAllGapCodesInDocsArePinned` como "teste substituido" — o nome JAMAIS existiu no repo (git grep = 0); a frase do guard virava historia falsa no corpus. Reescrita EN+PT: gate = `19a740f2` (pin→matriz, auto-derivado); a direcao inversa (doc citando codigo nunca emitido — HTTP003/§259, UUID002 na conformance-matrix) NAO e de maquina, foi limpa pelo sweep manual `c5897cd5` (220 literais) e fica sob disciplina de sweep. c) **contagem 11/11 -> 12/12**: `c5897cd5` adicionou o pin Android; re-medido 2x fresco no tip `1cc7e14d` (surefire 11.10s). + **drift EN×PT caçado e espelhado**: tabela do topo PT ainda dizia "§259 🔴 ABERTO 16/09" com corpo ja ✅ FECHADO e tabela EN corrigida pela mantenedora em `1cc7e14d` (so EN) — linha 18 PT reescrita como espelho fiel das fatias/SHAs. docs-lang 0/0/0; abertas da known-bugs seguem **12** (§171 §188 §192 §239 §252 §260 §266 §267 §270 §271 §278 §279).

(38) **Re-trigger 04:10 UTC — RECUSA de codigo + 1 entrega real da lane (caça plan×code ate o fim):** zero commits/issues novos desde `5beeecbb`; CI meus 3 pushes docs-only = success (5beeecbb em curso). Caça executada: (a) `planning-otp` gap `cancelled()` = ja celula viva na tabela do plano (#13 ✅ condicional / JS-interp=0 documentado na matriz:84) = R6-honesto, dono `.18`/stdlib, sem edicao; (b) tabela 1.1 do plano universal mantida pela mantenedora em dia (1.1.2 WEB005 ✅ / 1.1.10 §278 / R6 machine-gate) e §279 e bug de corretude, NAO gap de dominio — nao pertence aquela tabela (decisao registrada, nao edicao); (c) **achado util**: nenhum cell da `conformance-matrix` cobre a forma do §279 (a linha 55 passa nos 4 targets porque o fold exige else-if-chain/ultima-statement; `ConformanceMatrixTest` fresco 11/11 verde) — nota **Cross-ref** adicionada ao §279 EN+PT instruindo a lane do conserto (.18) a entregar a celula nova junto do fix (KOFSCRIPT lane dona do arquivo). docs-lang 0/0/0. Abertas known-bugs = 12 (inalterado).

(39) **Re-trigger 04:25 UTC — trabalho REAL encontrado caçando o residuo que a propria lane sinalizou (lição do #324 que virou bug-doc):** nenhum commit/issue novo desde `bfd389c9` (CI `5beeecbb` success; `bfd389c9` em curso; #435/#436 no since= sao ruído dos meus fechamentos). **Catalogado §280 EN+PT com TUDO medido nesta sessão:** (a) repro frescos SEM010/SEM014 nos dois arquivos .kf acima dando `:0:0` com mensagem EN correta; (b) raiz lida no código — `sa.diagnostics().error("", 0, 0, 0, ...)` em `StatementAnalyzer:260`/`TypeChecker:193`/`:265` (nó com posição real está no escopo, não é passado); (c) raio = **70 sítios** no kof-compiler (top: MemberCallTyper 12, StatementAnalyzer 11, SemExpressionTyper 9, TypeChecker 8, CompilerImports 5), 12+ códigos literais mapeados (SEM010/011/012/014/015/025×9/041/046/068/072/073/PKG003×5). DONO do conserto = typer lane `.22` (cluster A sobrepõe MemberCallTyper/TypeChecker = NOT-TOCAR meu); direção aditiva descrita (helper + migração sítio-a-sítio + regressão por linha no padrão SemanticResolutionTest). Precedente aplicado: §277–279 nao entraram na tabela rolling do topo — §280 idem. Abertas known-bugs: **12→13**.

(40) **Re-trigger 04:35 UTC — landing triplo da mantenedora auditado COM PROVAS RE-MEDIDAS (`91a36caa` #394 reduce→String `CHECKCAST` / `ba7021ce` #381 lambda captura campo / `86ce0126` catalogou §281+§282 EN+PT): `ReduceStringCastTest` 4/4 + `LambdaFieldCaptureTest` 4/4 VERDES frescos no tip (a primeira tentativa meu erro de sintaxe `+` no -Dtest, Q4 — nada a ver com o código); docs-lang dela ja 0/0/0 sem edicao minha; #394/#381 abertas = NAO disputo: a linha 48 da DOING da propria `.22` traz o close como PROXIMO PASSO dela (`--body-file` + checar state).** Correções da lane nos registros dela: datas `19/09`→`18/09` nas 2 linhas (data futura impossivel; commits medidos 18/09 01:13 -03, com atribuição na linha) + espelho PT da linha que contem o PRÓXIMO PASSO (convencao: linhas com contrato de retomada vivem nos dois idiomas). Contagem abertas known-bugs: 13 (acrescentar §280 meu; §281/§282 ja FIXED no nascer). **Numeração: a .22 registrou que §279/§280 foram tomados "EM VOO" pelo meu cross-ref/catalog — da proxima vez, `git fetch` + checar `grep "^## §2" tip` ANTES de escolher o numero (dou-lhe o mesmo aviso espelhado).**

(41) **Re-trigger 04:55 UTC — recusa de codigo + 1 entrega de governance:** #394/#381/#352 FECHADAS pela propria `.22` (melmonfre, reason=completed, comentario 143 chars cada apontando fix+prova — close honesto, nao disputado, e o contrato da linha 48 cumprido em publico); zero commits novos; CI meus docs 03861c1e/418b72bc success, demais em curso. Entrega do tick: o acordo anti-clash de §NNN (nascido no (40), dor real da propria .22) promovido de nota de DOING para **AGENTS.md + AGENTS.pt_BR.md** (secao multi-agente, apos a golden rule) — 2 pares EN/PT, `git show origin/tip` ANTES do numero, renumerar do seu lado antes do push.**

(42) **Re-trigger 05:05 UTC — recusa de codigo + 1 drift de governance corrigido:** nenhum commit/issue/comentario novo desde `1ff3d5f7` (CI b0a3f3fb/48d26d28 **success**; 1ff3d5f7 em curso). Drift caçado no propriissimo guia: cabecalho `Last update` parado em 13/09 enquanto o corpo recebeu 10 commits desde la (D-NOT-JAVA 18/09, R1-gate 17/09, bump 0.4.0, suite 2411 re-medida, minha bullet §NNN de ontem) — o ponteiro de "estado atual" mentia por omissao. Sincronizado EN+PT (data 18/09 + 3 temas novos; PT exigiu 2a passada porque a frase exata nao bateu no replace condicional — conferido por LEITURA da linha 8, nao por pressuposto). docs-lang 0/0/0.

(43) **Re-trigger 05:10 UTC — RECUSA de codigo com auditoria Q7 executada ate o fim (negativo documentado vale mais que silencio):** zero commits/issues/comentarios novos desde `b2031a02`; CI meus docs ainda em curso. Audito o C6 do prompt ("o que falta nos planos") pelo eixo stubs: `grep -E\bTODO\b|\bFIXME\b` nos 5 modulos main = 10 hits; **9 sao a palavra PT "todo(s)" capitalizada em comentario** ("get() devolve V? para TODO valor", "false em TODO target"...) — NAO marcadores de trabalho; **1 marker real** (`NativeRiscvAsmRtB44:16` "TODO temporario" do derrame s0-s11 do `kof_gc_mark`) ja esta **catalogado com referencia (G-3)** na familia §260 (PARTIAL, dono `.17`) = Q7-compliant. Veredito: zero stubs indocumentados no src/main dos 5 modulos. Licao de grep para as proximas lanes/agentes: `TODO` case-sensitive NAO basta em corpus PT — cross-checkar se e a palavra "todo" antes de alegar stub (armadilha de falso-positivo, documentada aqui). Fila da lane 15 VAZIA — recusa mantida ate janela real: CI de `1ff3d5f7`/`b2031a02`, fix §296 (.18), fix §297 (.22), decisao §295 (mantenedora), ou close/comentario novo.

(44) **Re-trigger 05:15 UTC — RECUSA limpa (sem edicao — nao existe entrega forcada):** zero commits, zero issues/comentarios, zero drift desde `100c78bc`; CI `1ff3d5f7` (bullet §NNN no AGENTS) **success** no gate de merge, `b2031a02`/`100c78bc` em curso. Lane 15 sem item executavel ate as janelas ja listadas na (43). Proxima recusa sem novidade = registro de UMA LINHA (a serie nao precisa de paragrafo).

(45) **Re-trigger 05:20 UTC — RECUSA (uma linha, sem edicao):** zero novidades no tip (commits/issues/comentarios); CIs meus ainda em curso.

(46) **Re-trigger 05:25 UTC — ENTREGA MAIOR da lane docs desde o R1: `scripts/check_known_bugs_status.sh` (ledger DE MAQUINA do status das secoes §NNN, EN×PT) + 4 drifts de corpo consertados no caminho.** Motivacao: minhas proprias contagens de "abertas" divergiram entre si em 4 ticks (40/13/12/13) — a condicao STABILITY ("todos os bugs resolvidos") tem que ser MEDICAO. O tools capou 3 bugs do proprio mawk no processo ({2,3} interval falha em linhas UTF-8 reais com ### de 3 hashes — trocar por #+; token historico ✅/🔴 dentro da frase de status = autossabotagem — linha de status nao pode citar o estado velho; secoes fora de ordem no arquivo — inserir por header, nao por posicao). Classificador: ULTIMA linha de status vence (header-suffix, **Status**/**Estado**, Fechamento/Resolution/Fix, "— emoji"); multi-face = live enquanto qualquer face viver; --selftest com fixture plantado (closed/live/faceado/unknown) verde. **Ledger medido agora: OPEN EN×PT IDENTICOS = {205, 248, 258, 268, 270, 271, 272, 278, 279, 280} = 10** (minha lista de 13 estava stale nos dois lados: `.17`/temmcode fechou §252 (`20495e48` kof_select_any)/§256 (`ff87a09c`+`dd2c7fcb`)/§259 ja FECHADO 17/09 e eu nao sabia; §205/248/258/268/272 faltavam na minha contagem). Drifts corrigidos com prova re-medida (`KofConcurrency2Test` 43/0F/3-skip fresco): corpo EN §256 sem linha de status (mel fechou so a tabela do topo — classe §254), corpo PT §256 idem (espelho 3a593734 so mexeu na tabela), corpo PT §252 congelado no ABERTO 16/09, corpo PT §259 congelado no ABERTO com a tabela/EN em 17/09. **BACKLOG MECANICO da lane (rc=1 so por isso): 10 secoes sem status legivel — EN[188 192 219 239 260 266 267] PT[188 192 219 235 239 253 260 266 267] — annotate-as na proxima sessao docs (formato: linha `**Status:** <simbolo> <data> <owner>` no topo do corpo, SEM citar tokens velhos).** Script nao esta na CI ainda — proposta no NEXT STEP (wiring = decisao da lane tooling/mantenedora apos uma semana de uso sem falso-positivo).

(47) **Re-trigger 05:55 UTC — COLISAO DE NUMERACAO §266/§267×2 resolvida + ledger sobe para 12 abertas (EN×PT identicos):** o check de abertas do ledger revelou que as secoes NATIVAS da `.17` (aarch64 interval-hang; erasure-box no-op) nasceram com os NUMEROS JA USADOS pelas canonicas KofJS do `.18` (colisao historica anterior a regra de ontem — dois pares de headers no mesmo arquivo). Renumeradas p/ os livres §283/§284 em 8 sítios de docs (headers EN+PT com anotacao 🟡 OPEN/ABERTO — sairam do estado `unknown` — + self-refs 10256/9751 + refs face-B do §253 9078/9027) e 4 code-comments (NativeX86Calls:41/48, NativeRiscvCrossOps:131 p/ §284; KofTimeE2ETest:258 p/ §283 — Q2 compile verde; NAO tocados os `§266/§267` de marker KofJS nos emitters JVM/nat que citam as canonicas). Ferramenta ganhou DUPLICATE-CHECK (uniq -d no ledger). **Ledger AGORA: OPEN = {205,248,258,268,270,271,272,278,279,280,283,284} = 12, EN×PT IDENTICOS** — as 2 novas (283/284) sao gaps NATIVOS reais que minha contagem manual de 13 vinha errando NOS DOIS LADOS (esquecia 283/284/205/248/258/268/272, contava §252/256/259 ja fechados). **DANGLING intencional nao-reescrito: DOING.md:151 (linha-claim da `.17`, "§267 — boxing nativo") aponta p/ o numero COLIDIDO — historico de outra lane NAO se reescreve; este registro e o ponteiro: leia §284.** Restante p/ proxima janela: anotar UNKNOWN {188,192,219,239,260 EN / +235 PT} — 6 secoes velhas cujo status existe mas em formato nao lido (verificar verdade na tabela do topo/matrix antes de escrever).

 (47b) **Fecho do tick:** anotadas as 11 linhas de status que faltavam + trigger ⚠️/REMAINS na ferramenta (o `⚠️ NATIVE face REMAINS OPEN` do §235 nao era lido e a secao aparecia FECHADA — o ledger quase mintiu no dia de nascimento; selftest nao cobria ⚠️ — coberto agora via caso real). VERDADE FINAL MEDIDA: OPEN = 17 (EN×PT identicos), UNKNOWN = 0, rc=0. Os corpos §235 PT e §219 PT/EN ganharam espelhos fiis das linhas de face EN. Dangling DOING:151 mantido (historico .17) com ponteiro §284.

> **✅ DONE (18/09 ~22:40 UTC, owner = 192.168.100.15, lane tooling/cli — X9 deploy): §299 FIXED + CI-PROVEN — the android APK pipeline had TWO stacked real bugs the local skips hid (CI ubuntu executed them for the first time).** (a) `90ea7c34`: relative keystore/build paths vs subprocess CWD → keytool `FileNotFoundException`; (b) `fa4b98f1`/`ea0ac82b`: the `build-tools/34.0.0` PIN = d8/R8 8.2.x can't read Java-21 bytecode (major 65) → dex died with `Unsupported class file major version 65` (verbatim in CI). Fix: `ApkToolchain` (split, gate ≤500 — CmdBuild 647→470, baseline line removed, §303 CLOSED) = `pickBuildTools()` highest semver with the 4 tools + honest Java-21 gate (R6/Q7 — never a silent dex crash or fake APK). Proof chain: `CmdBuildApkToolchainTest` 4/4 deterministic NO-SDK (caught a signed-byte bug in my own `classMajorOf` mid-unit); `95aab5d7` replaced the obsolete fatia-1 `nonJvmTargetIsHonestGap` (it asserted android=DEP001 while CI-with-SDK now packages — the contradiction only appeared once the pipeline actually worked) with `androidWithoutSdkIsHonestGap` (deterministic env-clear on the child, no weakened assert); **CI Build+Tests of tip `1b425a57` = 0F/0E, android e2e executed WITHOUT skip** = §299 green on the real SDK. Tally → **2671** (cli 333 = +4 toolchain +7 faces que agora executam no CI; compiler 2292 = X10). §299/§303 EN+PT; ledger 18 open (all cross/native-lane, none mine). Residual recorded rule-6: ANDROID at V17 (`known-bugs §299`, maintainer call).

> **✅ DONE (18/09 ~17:40 UTC, owner = 192.168.100.15, lane tooling/cli + bugs-and-gaps — X9 deploy): §298 FIXED — `kof deploy --target js` shipped a NON-SELF-CONTAINED release (Q7 facade: slice marked "packaged", unit could not run).** Found while syncing the deploy docs (this unit's next step): the JS entry does `import ... from './kof-runtime.mjs'` but `CmdDeploy` copied ONLY the entry (single-artifact model fits JVM/ELF/APK, silently wrong for JS); `node <X>.mjs` as promised by RELEASE.md died `ERR_MODULE_NOT_FOUND` (measured verbatim on tip `235061b3`). Root-cause fix: `jsImportClosure()` BFS over relative imports → every closure file copied into the release + SHA256SUMS + tar. Weak-green test hardened (Q5): now RUNS `node` inside the packaged release dir and asserts program output, honest `assumeTrue(node)`. PROOF: RED-before (stash-fix, old code + new test → FAILURE @182 "runtime deve acompanhar"); GREEN-after `CmdDeployTest` 9/9 + 1 honest skip (android no SDK); kof-cli full module 322/0F. **§NNN shared-claim honored:** wanted 296, but PR #444's branch already holds 295-297 → took **298** from the union (remote tip + open PR heads) BEFORE pushing. Catalogued EN+PT same commit; ledger 18 open rc=0, docs-lang 0/0/0, Q2 green.

> **✅ DONE (18/09 ~00:3x, owner = 192.168.100.15, lane bugs-and-gaps/docs + tooling/cli): #447/§305 FIXED + catalogada EN+PT — `kof fmt` corrompia round-trip de literais escapados.** Reproduzido verbatim no tip: `'\\'`→`'\'` (LEX004 unterminated) e `'\t'`→TAB cru; mesma raiz quebrava STRING (`"c\"d"`→`"c"d"`). Raiz = `KofFormatter.formatExpr` reimprimia `LiteralExpr.value()` (ja decodificado pelo lexer) sem re-escapar. Fix aditivo: `escapeLiteral` com o vocabulario exato do `readEscape` (`\\ \t \n \r \' \" \0` + `\uXXXX` p/ controles; nao-ASCII fica cru). Prova: `KofFormatterTest` +4 = 12/12; RED-ANTES medido com o fix sob stash (3 falhas); modulo kof-compiler 2212/0F/0E; check_500 OK. **ACHADO VERGONHOSO corrigido no caminho:** meu dedup de §305 comia a §304 alheia no PT — restaurada do remoto antes do push (lição: sed/python em arquivo = medir `grep -c` do resultado ANTES do commit, nao depois). **CI PROVOU o incidente: `1d463c41` (sem o arquivo principal) = 3 vermelhos no job Build+Tests; `33363a3f` (com ele) = SUCCESS 12/12** ✓ — issue #447 fechada com a cadeia completa de prova.

> **NEXT STEP (desta lane — leia aqui e continue):** fila X9/§298/§299/§303/§305 **TODAS ENCERRADAS COM PROVA CI** (§305/#447: CI `1d463c41` red pegou meu staging bug, `33363a3f` green 12/12 — errata + incidente registrados no ledger EN+PT; tally re-medida 2671→**2687** = 2296+48+7+336 no CI do tip `33363a3f`, AGENTS EN+PT `1b1b6c1c`). Próxima ação = **re-varredura de landings alheios + drift de docs** na diretriz da mantenedora ("analise os documentos, verifique os gaps"): se nada novo aparecer na lane, **RECUSAR o re-trigger pela regra STABILITY** (18 abertas com dono vivo; §304 = `.22`; X10 concluída; `future/workflow-plan.md` = plano sem promoção). Próximo §NNN livre = **306** (tomar pela união tip+PRs antes de criar entrada).
