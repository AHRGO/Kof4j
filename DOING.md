[English](DOING.md) | [Português](DOING.pt_BR.md)

# DOING.md — coordenação multi-agente (quem faz o quê)

> **Regra obrigatória para agentes (IA ou humano):**
> 1. **Antes** de começar qualquer trabalho de feature/gap: leia este arquivo.
> 2. Se o item que você quer atacar já tem **dono + estado `EM CURSO`**, não toque —
>    escolha outro ou pergunte. Nunca dois agentes no mesmo gap.
> 3. Ao **reivindicar** um item: edite este arquivo **no mesmo commit** que começa
>    o trabalho (dono, branch, arquivos que vai tocar).
> 4. A **cada commit**, atualize sua linha (estado, progresso, o que falta).
> 5. Ao **concluir**: mude para `FEITO` com data + commit + teste que prova, e
>    marque o gap no docs (`status.md`/`backend-parity.md`).
> 6. Itens abertos ficam em `EM CURSO` por no máx. uma sessão; ao abandonar,
>    volte para `ABERTO` com nota do que já funciona e o que falta.
> 7. **Modo autônomo:** se o turno vai acabar, a ÚLTIMA coisa escrita aqui é
>    a linha **"PRÓXIMO PASSO"** abaixo (tarefa exata + arquivo + prova).
> 8. **Nunca descartar trabalho de outro agente (13/09, diretriz da
>    mantenedora):** os agentes trabalham **em conjunto, não um contra o
>    outro**. Working tree sujo de outro agente = trabalho dele: **commita
>    tudo** (valide antes: sintaxe + run/compile da área), nunca `checkout --`
>    nem `stash drop`. Conflito de rebase = resolve preservando os dois lados
>    (fatos idênticos → escolhe uma redação; fatos diferentes → mantém ambos).
>    Reversão de commit alheio só com causa raiz provada + registro no DOING.
> 9. **Identificação por IP local (13/09, diretriz da mantenedora):** cada
>    agente se identifica pelo **IPv4 local da máquina** (`hostname -I`).
>    Toda reivindicação `EM CURSO`/`FEITO` leva `dono = <IPv4>` (ex. `dono =
>    192.168.100.22` = `mel-optiplex`). **O cluster compartilha o storage**
>    (mesmo repo/branch): cada IPv4 = **um agente = uma máquina**; nunca
>    assumir o trabalho/dono de outro IP, e a linha `Esta sessão = ...` vale
>    só para quem a escreveu. Esta sessão = **192.168.100.15**
>    (lane **bugs-and-gaps** — manter `docs/bugs-and-gaps/` (known-bugs,
>    conformance-matrix, ecosystem-coverage, specification-gaps, KOFUI-AUDIT)
>    sincronizados com o código/suíte; §106 fechado pela lane .18/9094; §89
>    evidência corrigida 13/09; contagem da suíte sync 13/09). Quem voltar
>    (humano/cron/outra instância) retoma em ≤1 leitura.

Estados: `ABERTO` · `EM CURSO` · `FEITO` · `BLOQUEADO`.

> **✅ FEITO (18/09 ~18:35, dono = 192.168.100.15, lane bugs-and-gaps/docs): R6 — `kof.media` (`MEDIA001`/`MEDIA003`) documentado na matriz de paridade (EN+PT).** Primeira unidade da frente universal (R6: "todo gap de domínio tem código + entrada na matriz"). Achado: `KofMedia.gapCode` emite `MEDIA001` (handles Image/Audio/Video) e `MEDIA003` (mic) — provado por `ExpressionUiMediaCallLowerer` (non-JVM → erro compile-time) e `KofMediaE2ETest:477` (MEDIA003) — mas **zero ocorrências de MEDIA** em `backend-parity.md`/`.pt_BR.md`/conformance-matrix/ecosystem-coverage. **Fix (docs, EN+PT):** (1) linha `kof.media` na Parity Table (JVM ✅ `javax.imageio`/`javax.sound`; Native/JS = MEDIA001/MEDIA003/WEB005 compile-time); (2) linha em "Documented Gaps"; (3) `MEDIA` adicionado aos prefixos "Current systems" da tabela de convenção + `MEDIA001`/`MEDIA003` nos exemplos. **Prova:** `grep MEDIA backend-parity` EN/PT 0→3 cada; contagem de linhas de tabela +2 em cada (108→110 EN, 109→111 PT — assimetria 110×111 é PRÉ-EXISTENTE, verificada contra HEAD); `docs-lang.sh check` 0/0/0. **PRÓXIMO PASSO (re-dispatch lê isto):** a frente universal segue — próximas fatias candidatas em `docs/development/README` §1 posição 8 (1.5 export OTel sem dono; 1.2 GC x86 = nat; 1.4 registry = decisão da mantenedora). **NÃO TOCAR:** §252/§253-B (nat .18); §258/DB002/ORM001-JS (development .18); `docs/status.md` (lane .22); #266/#259 (mantenedora).

> **✅ FEITO (18/09 ~18:10, dono = 192.168.100.15, lane bugs-and-gaps/docs): PROMOÇÃO DO `PLAN-UNIVERSAL-PLATFORM` DE `future/` PARA `development/` (decisão da mantenedora; portão R12 SOBREPOSTO) — o plano vira trabalho corrente.** Diretriz explícita: "se acabaram os docs preciso que voce assuma a frente `docs/development/future/PLAN-UNIVERSAL-PLATFORM.pt_BR.md`" → opção escolhida: "Promover p/ development/ e implementar". **O que foi feito (EN+PT no mesmo commit):** (1) `git mv` do par EN+PT `future/PLAN-UNIVERSAL-PLATFORM.md` → `development/PLAN-UNIVERSAL-PLATFORM.md` (renome, histórico preservado); (2) cabeçalho do PRÓPRIO doc reescrito — `**Type:**` passa de "NOT an implementation order" a "long-term architecture — **UNDER DEVELOPMENT** since 17/09/2026 (promoted from `future/`; the R12 gate is **overridden**)", e o bloco "Rule of this document" que dizia "implements nothing / does not move files / does not open a new front" é REVOGADO (era incompatível com a promoção) e trocado pelo novo contrato (entrada = Estágio 1 SYSTEMS + R1–R12, cada unidade com Q0–Q7, semântica congelada intacta); (3) R12 do próprio §15 anotado **OVERRIDDEN/SOBREPOSTA 17/09/2026** (mantém o texto original + nota: relaxa o *agendamento*, nunca o freeze/qualidade; para os OUTROS planos de `future/` o R12 segue default); (4) **claims de estado defasadas** corrigidas contra o código (regra 3 da honestidade): `mvn test` `810`→`2411` (17/09) e `android Phase 1`→`Phases 1–4` (a linha `jvm stable, native x86_64 stable` foi mantida — é descrição correta); (5) **decisão registrada** em `DECISIONS.md`+PT §**D-UNIVERSAL** (EN+PT: contrato, invariantes, alternativas rejeitadas — respeitar o R12 vs promover agora; evidência; relação `Overrides: R12`); (6) fila atualizada em `roadmap.md`+PT (§22 título→"under development — R12 overridden" + bloco de gate reescrito; §23 TIER 6–12→"architecture UNDER DEVELOPMENT… governed by `docs/development/PLAN-UNIVERSAL-PLATFORM.md`"); (7) **índices** `development/README`+PT (§4.2 ganha a linha do plano; §4.3 remove; regra R12 ganha a exceção; linha "tiers 6–12 = `future/`" corrigida) e `future/README`+PT (linha removida + nota "left `future/` 17/09"); (8) **varredura de referências (27 sítios, EN+PT em simetria):** `docs/I18N`, `AGENTS.md`+PT (§"Platform invariants" + tabela §"Corpus" ×2), `future/PLAN-BAREMETAL-BOOT`+PT (×4, caminho relativo `../`), `future/PLAN-MULTIPARADIGMA`+PT, `future/scoped-resources-plan`+PT, `docs/stdlib/PLAN-TREE-SHAKING`+PT (âncora `:1307`→`:1318`), `docs/ui/PLAN-CANVAS-WIDGET`+PT (âncora `:422`→`:428`). **Prova (Q1/Q2/Q5, mesmo commit):** `git status` mostra `R` (rename) nos 2 arquivos; `grep future/PLAN-UNIVERSAL-PLATFORM` em `docs/`+`AGENTS` = **0** (só a citação histórica da origem dentro do §D-UNIVERSAL e o journal do DOING); `docs-lang.sh check` **0/0/0**; `check_500.sh` rc=0; `mvn -o -pl kof-compiler -am compile -q` rc=0 (docs-only + 0 código; a decisão é documental). **PRÓXIMO PASSO (re-dispatch lê isto):** a frente agora é trabalho corrente — atacar a **primeira unidade executável** saindo do Estágio 1 (SYSTEMS: 1.1 HTTP002/MEDIA, 1.2 GC x86, 1.4 registry, 1.5 OTel) e/ou **R1 (travar a fronteira core/plataforma)** — escolher 1 unidade coesa com prova, reivindicar em `DOING.md` antes do código, e rodar Q0–Q7. **NÃO TOCAR:** `docs/development/DECISIONS.md` de outros (o meu §D-UNIVERSAL já está lá — linha `.17` é do dono antigo, mas a diretriz da mantenedora me autoriza registrar a decisão dela); §252/§253-B (nat .18); §258/DB002-JS (development .18); `docs/status.md` (lane .22); #266/#259 (mantenedora).
> **✅ FEITO (17/09 ~20:00→21:30, dono = 192.168.100.15, lane bugs-and-gaps): TICK — PR #358 revisado com medição + segunda leva da fila-0-comentários (fechadas #351/#355/#296, dossiês #295/#353/#354).** (1) **PR #358 (#302) revisado** (comentário bot `5720465327`): fix `CompilerClassLowering` CORRETO — Q0 medido no reator (baseline: repro #302 crasha no load; com o diff aplicado: roda `woof/meow`; controles intactos: herança real `woof/Rex`, `extends iface + 2º iface` roda `1/2`, `extends classe implements multi` roda `1/2/9`). **Achados de escopo reportados no review (separáveis do fix):** bump `0.4.0→0.4.2-beta` (pula 0.4.1), `CHANGELOG +387` (seção inteira `[0.4.1-beta]` de outro trabalho), bumps de workflow `checkout v4→v7`/`github-script v7→v9` JÁ feitos pelo dependabot (`#197`/`#196` em 14/09 = redundantes sobre a base). NÃO mesclei (dono = mantenedora; review técnico entregue). (2) **#302 re-medida no tip: ainda crasha** (o PR não landou) — deixada aberta, fecha quando #358 entrar. (3) **FECHADAS com prova de medição (reator + reflection launcher p/ mask JavaFX, regra §149): #351** (repro adaptado sem null-literal SEM048: roda `42`; `javap` campos erasados p/ Object = boxing presente), **#355** (verbatim roda `default`; `javap` prova `<init>` super() ANTES de putfield + `<clinit>` com `putstatic` — os 2 defeitos do relato estão MORTOS no bytecode), **#296** (crash ASM do verbatim morto: roda `5000`; a face Long?→`J` primitivo = cluster nullable PARQUEADO da mantenedora, trilha #259; close com essa ressalva). (4) **DOSSIÊS sem fix (Cluster A type-var = regra 6, dono `.22` linha 273/12016cc0): #295** (dossiê completo bot `5720582017`: `javap -p Stack` prova `public volatile T[] items` = descriptor `[LT;` inválido JVMS 4.2.1 — `toInternalName("","T")` passthrough sem apagar type-var no caminho array/field), **#353/#354** (cada um com nota própria: `check` limpo + `VerifyError` no load via reflection; campo já erasa p/ Object — a face viva é a COSTURA do caller/return, mesma família; NÃO duplicado o dossiê do #295). (5) Contagem: **63→60 abertas**; 0 comentários órfãos novos no #324 (sem re-post). `docs-lang.sh check` 0/0/0 no lote fake-idioms (`64f788c6`). **PRÓXIMO PASSO:** inalterado — (1) D-DIAG-EN batch-final ≥~08:00 UTC 18/09 (conferir `git log -1` em `BuiltinCallTyper`/`KofInterpreter` ANTES); (2) adjudicação #324; (3) re-trigger sem item novo na lane = RECUSAR (STABILITY). **NÃO TOCAR:** cluster typer/erasure/SAM .22; parked nullable/Map-absent; PR #358 (dono mantenedora — review feito, NÃO mesclear); `docs/development/DECISIONS.md` (.17); §252/§253-B (nat .18).

