[English](backend-parity.md) | [Português](backend-parity.pt_BR.md)

# Backend Parity — Kof JVM × Native × KofJS

**Última atualização:** 18 de setembro de 2026
**Versão:** 0.4.0-beta

> Deltas desde 0.1.0: Targets `native.risc` (riscv64) e `native.arm` (aarch64) separados de `native` x86_64 (**core completo 02-03/09** — `NativeRiscv64E2ETest 13/13` + `NativeAarch64E2ETest 13/13` em asm puro + qemu, paridade avançada pendente; ver `docs/development/native-multiarch.md`); Native free-list (`kof_free_head`) + `kof_gc_collect` (mark-sweep implementado 03/09, `kof_gc_collect_now` manual; auto-GC desativado — auto-collect pendente §260); MySQL wire protocol em progresso (`kof_db_mysql_scramble` + `user:pass@`); pattern matching `switch case String s` + record destructuring `Point(x,y)` em JVM/Native/JS; `String?` null safety básica; `KofScript` top-level `let` → `KofScriptGlobals`; `KofCcompiler` (`kof c`) native-only C subset; `List map/filter/reduce` + `Box<T>`; Windows SIGPIPE fix.
> Deltas 30-31/08: `spawn`/`await` real no Native (pthread + trampoline + join + allocator thread-safe futex — CONC001); FP real em XMM (FLT001); JSON objetos/records + arrays FP no Native (JSN001/JSN002/JSN003); WebSocket/SSE no JVM (`app.ws`/`sse.*`, RFC 6455); `kof.http` retry/circuit JVM+JS (30s window, fail-fast); `kof.cache` 3 targets (fix de clobber de registradores); UI Fase 7 Router (JS real, JVM no-op); SQLite nativo via `.so` direto; `kof fmt` + `kof config gen`.
> Deltas 18/09: **FFI da JVM generalizada (R3, `.18`)** — `extern` agora casa qualquer assinatura sobre o conjunto escalar {Int, Long, Float, Double, Boolean, String} em aridade arbitrária (parâmetros e retorno), um retorno `String` lê de volta o `char*` nativo; os downcalls FFM `kof_ffi`/`kof_ffi_void` substituem `kof_ffi_i`/`_si`/`_dd` (`FfiE2ETest` `pow` dois doubles → `1024.0`, `strstr` duas strings → `world`, `srand(Int)` default `void`; `FfiE2ETest` `atol`→`labs` prova `Long` ponta-a-ponta, `FfiSignatureTest` trava o mapeamento escalar→layout completo incl. `Float`/`Boolean`). Gaps honestos preservados: struct/pointer (D6), variadics e handles opacos/out-buffers seguem `FFI001`. **Paridade JS FECHADA 18/09 (3.6, `.18`)**: a ABI escalar binda no target JS via bridge FFM no host `KofJsFfiBridge` (`extern`→`kofFfi`→`ProxyExecutable` `kof_platform.ffi` no runner GraalJS/node), provada byte-a-byte JVM↔JS por `FfiE2ETest` (+7 `assertJvmJsParity`); browser não tem host → degrade honesto em runtime (R7); assinaturas não-escalares seguem `FFI002`. Native `FFI001` (§61) permanece gap honesto por target (R7).
> Deltas 18/09 (2): **paridade FFI-JS FECHADA (R3.6-F2/F3, `d3598c2d`)** — o runner host JS (GraalJS/node) liga a MESMA ABI escalar via `KofJsFfiBridge` (`extern`→`kofFfi`→`kof_platform.ffi`); `FfiE2ETest` 16/16 afirma igualdade byte-for-byte JVM↔JS; browser = erro honesto de runtime (R7, sem host); nao-escalar JS = `FFI002` em compilacao.
> Deltas 18/09 (3): **callbacks/upcalls da FFI bindam na JVM (R3.4-C1/C2)** — um `extern` com parâmetro de tipo-função baixa para `kof_ffi` com um token aninhado `(<ret><params>)` e o runtime monta um ponteiro de função C real via `Linker.upcallStub` sobre o valor de função Kof (interface sintética especializada `int invoke(int,int)`, unreflectida + ponte `.asType`); `JvmFfiCallbackE2ETest` roda `(x,y)->x+y` através de callbacks C em ABIs Int/Long/Double/mistas (`42/42/6.0/7.5`). Contrato síncrono/não-escapante (vida na arena confined); ABI de callback só primitiva (String/struct/pointer-no-callback e callback-como-retorno seguem `FFI001`); paridade JS de callback = fatia 3.4-C3 (`FFI002` no JS), browser = degrade honesto (R7). Pin no host `JvmFfiCallbackTest` trava o mecanismo.
> Deltas 18/09 (4): **callbacks/upcalls da FFI também bindam no host runner JS (R3.4-C3.1→C3.3)** — o `ProxyExecutable` do `KofJsRunner` marshala um callback montando um `Linker.upcallStub` sobre o valor de função Kof; **o valor de função compilado é um objeto `Lambda…` com método `invoke` (não uma arrow JS nativa), então a ponte chama `fn.getMember("invoke").execute(...)`** (o enquadramento `Value.execute`-em-arrow do pin C3.1 foi corrigido); `JvmFfiCallbackE2ETest.jvmAndJsCallbacksMatchByteForByte` prova paridade byte-a-byte JVM↔JS de callback (`42/42/6.0/7.5` Int/Long/Double/mistas); browser = degrade honesto (R7); um callback não-bindável (`String`-in-callback) segue `FFI002` no JS. Paridade de callback JVM+JS FECHADA; gaps R3 restantes inalterados (structs/D6, variadics, handles opacos, Native §61).
> Deltas 01/09: **`spawn` com lambda que captura variável externa** (`SpawnStmt` emitia a lambda com zero capturas → `VerifyError`/valor errado; agora coleta via `collectCaptures`); **`&&`/`||` booleanos short-circuitam no JS** (backend emitia `&`/`|` bitwise que avalia os dois lados; agora `&&`/`||` JS para `bool`, bitwise intacto); **`Channel<T>` como parâmetro de função** (tipo saía com package vazio e o `isChannel` exigia `kof.concurrent` → mapeado como builtin + `JvmTypeMapper` → `LinkedBlockingQueue`); **`println`/`print` antes de `spawn` no Native** e o bug pré-existente **`spawn→await→spawn`** (SIGSEGV no `pthread_create` — stack chegava desalinhada ao C call; alinhado com `andq $-16` preservando `r15` + frame); **KofJS no browser real** (`KofJsBrowserE2ETest` — Chrome headless + HTTP + DOM); **`SECN002` fechado — AES-256-GCM no KofJS** (AES-GCM puro JS: FIPS 197 key expansion + GCM CTR/GHASH, formato `aesgcm$iv$ctTag` idêntico ao JVM/Native; tamper detectado por decode base64 estrito `strict=true` — paridade cross-target JVM↔JS testada); **`OBS002` fechado — histogram/metrics no Native** (store asm 32B name+sum+count + export Prometheus via `kof_string_concat` — paridade de conteúdo com o JVM); **`TIME001` (Native) — `time.interval`/`time.cancel`** (mesmo mecanismo do `scheduler.every`/`cancel`, SCHED001: thread por job; alias `kof_time_interval`/`kof_time_cancel` → `jmp kof_scheduler_every`/`cancel`; mutação por referência da captura validada); **`TIME001` fechado no JS (02/09) — fila cooperativa de timers bombeada por `time.sleep`** (GraalJS não tem `setInterval`; browser/Node usam o nativo — `KofTimeE2ETest.jsIntervalRunsPeriodicallyUntilCancelled`); **`MQ001` fechado — kof.mq no Native** (pub/sub + filas in-process em asm: store `.bss` + nodes 40B, invoke-com-arg `rdi`=fn `rsi`=msg, unsubscribe por identidade do objeto fn — paridade de output JVM↔Native↔JS); **`Set<T>`/`Map<K,V>` como campo/retorno de classe no JVM** (o `JvmTypeMapper` mapeava só `List`→`ArrayList`, então `Set`/`Map` viravam `Lkof/Set;`/`Lkof/Map;` → `NoClassDefFoundError`; agora `HashSet`/`HashMap`; + parse de método de classe com retorno genérico `Set<Int> all(` — `KofMapSetTest.setMapAsFieldAndReturn`, 3 targets); **Query DSL tipada do ORM (nível 3, `ORM001`)** — `User.query(db){ where; orderBy; limit }` baixado para `db.query<T>` (SQL preparada em compile-time, valores como binds; `KofOrmE2ETest` 22); **source map V3 do KofJS** (mappings VLQ reais em nível de linha: cada função gerada → linha Kof via `KofDebugInfo` — antes era stub `"mappings":""`; `KofJsSourceMapTest`).
> Deltas 02/09: `Set<T>` como tipo declarado no JVM (`kof.Set` → HashSet); null-safety narrowing JVM corrigido (`s.length`/`s.substring(...)`/`if_acmp*`); `mapOf` infere o primeiro par; `String? s = null` prefixado parseia; `readText`/`readFile` → `String?` (Native devolve `null`); `size()` lança em vez de `-1`; `Map.get` → `V?` (referências); gaps `STR001`/`STR002` documentados abaixo.
> Deltas 03/09 (main): **aarch64 native core completo** (tradução riscv→aarch64, NATIVE002 13/13); **kof.deps** (package manager MVP, Maven Central); **lifecycle** `application { onStart/onShutdown }`; **observability spans W3C** (3 targets) + metrics removidos; **LOG001 JS**; **TIME001 JS**.
> Linhas "Deltas" registram o histórico por release (0.1.0→0.4.0-beta). Build atual: `mvn test` **2218** (1911 kof-compiler + 38 kof-script + 7 kof-c-compiler + 262 kof-cli), 0 regressões / 0 erros, 192 skip (sem qemu no host → 84 cross skip; guardas de toolchain/DB externo + sysroot §255; `node` presente; o flake §252 disparou 09:44, calou 11:38/15:09/15:54) — 16/09 ~15:54 clone limpo de `9572949f`. A matriz reflete o estado corrente dos 3 targets. DoD em `docs/development/DECISIONS.md` §D-PLAT → `docs/architecture/performance.md` §40–§41.

---

## Tabela de Paridade

| Feature | JVM | Native | KofJS | Notas |
|---------|-----|--------|-------|-------|
| Literals (int, long, float, double, string, bool, char, null, hex) | ✅ | ✅ | ✅ | hex: `0x...` |
| Variáveis, `var`/`val`, inferência | ✅ | ✅ | ✅ | |
| Aritmética, bitwise, unário | ✅ | ✅ | ✅ | |
| if/else | ✅ | ✅ | ✅ | |
| if-expr (`var x = if (c) a else b`) | ✅ | ✅ | ✅ | |
| while, for, do-while, break/continue | ✅ | ✅ | ✅ | |
| for-in (`for (var x in coll)`) | ✅ | ✅ | ✅ | |
| switch (`case N:`) | ✅ | ✅ | ✅ | |
| Funções (todas as formas, sem `fun`) | ✅ | ✅ | ✅ | |
| Records (`record Point(Int x, Int y)`) | ✅ | ✅ | ✅ | toString/equals/hashCode |
| Classes, campos, métodos | ✅ | ✅ | ✅ | |
| `constructor(...)` e primary `class X(...)` | ✅ | ✅ | ✅ | |
| Herança, `super`, override | ✅ | ✅* | ✅ | Native: `super.metodo()` = SUP001 |
| Virtual dispatch | ✅ | ✅ | ✅ | |
| Interfaces | ✅ | ✅ | ✅ | |
| Generics (erasure) — `Box<T>` `T` primitivo | ✅ | ✅ | ✅ | 25/08 `substituteTypeVariable` + `kof_int_to_string` |
| Lambdas `(x: Int) -> expr` | ✅ | ✅ | ✅ | com capturas (box `BoxN`) |
| Exceptions (throw "msg", try/catch/finally) | ✅ | ✅ | ✅ | Native: unwinding próprio |
| `assert(cond[, msg])` | ✅ | ✅ | ✅ | |
| `spawn` stmt / `spawn f()` / `await` / `poll` / `done` / `cancel`+`cancelled` / `selectAny` / `awaitTimeout` / `channel<T>` send/receive / `scheduler.every`+`cancel` (Handle<T>, unbox, exceção limpa; `at(cron)` = cron real 5 campos UTC em JVM/JS, Native CRON001) | ✅ (virtual threads; canal = LinkedBlockingQueue; scheduler = ScheduledExecutor) | ✅ 31/08 (pthread_create + trampoline + pthread_join, allocator futex — CONC001; awaitTimeout = polling 1ms; canal = FIFO futex; **scheduler SCHED001** = thread por job + `usleep` ms→us + flag `active`; **cross riscv64/aarch64: clone 220 + nanosleep 101 + spinlock `amoswap.w`; helpers CONC001 15/09 = fatia `RtB48` (done/poll/cancel/cancelled/selectAny/awaitTimeout; cancel por TID real gettid(178) no ctid do clone; supervisor segue OTP001)) | ✅ 03/09 (CONC003 fechado — async/await/Promise reais; canal = fila de resolvers pendentes; `cancelled()` sempre `0`, limitação conhecida; scheduler = setInterval) | `KofAwaitTest` 8/8 · `KofConcurrency2Test` 41 (pós-§117 + helpers cross 15/09) · `SpawnE2ETest` 8/8 |
| Strings (`+`, `==`, length, charAt, substring, contains, startsWith, endsWith, indexOf, trim, case, replace, split) | ✅ | ✅ | ✅ | `STR001` fechado em UTF-16: x86_64 + JS + riscv64/aarch64 `length`/`charAt` (10–11/09, B33) + `substring`/`indexOf`/`lastIndexOf` riscv/aarch (11/09, B34 — walk de code-units fiel ao x86, trampolins `kof_su_*`). **Resíduo de contrato (x86, não cross):** `"ab".substring(0,0)` → `"ab"` (marker `end=0`="até o fim" colide com `end=0` explícito; JVM: `""`) — decisão da mantenedora |

| Arrays (`new Int[n]`, `arr[i]`, `.length`) | ✅ | ✅ | ✅ | |
| `List<T>`, `listOf`, for-in | ✅ | ✅ | ✅ | |
| `Map<K,V>` + `mapOf` (put/get/remove/contains/size/keys/values/clear/isEmpty) | ✅ HashMap | ✅ asm próprio | ✅ JS Map | `KofMapSetTest` — **campo/retorno de classe 01/09** (mapper `Map`→`HashMap`; parse de método c/ retorno genérico) |
| `Set<T>` + `setOf` (add/contains/remove/size/clear/isEmpty) | ✅ HashSet | ✅ asm sobre List | ✅ JS Set | tag de tipo no Native; **campo/retorno de classe 01/09** (mapper `Set`→`HashSet`) |
| `enum` + values/valueOf/name + switch exaustivo (`SEM031`) | ✅ | ✅ | ✅ | enum→String nos descritores; `KofEnumSwitchTest` |
| JSON encode/decode (primitivos, List) | ✅ | ✅ | ✅ | arrays `JSN003` fechado; `Double[]`/`Float[]` no Native (JSN001, 31/08) |
| JSON objetos/records (JSN002) | ✅ | ✅ 31/08 | ✅ | composição em compile-time no Native |
| `kof.io` (File, Path, Directory) | ✅ | ✅ | ✅ | `readText`/`readFile` → `String?` (`null` p/ ausência, 02/09); `size()` lança em vez de `-1` |
| `readLine` (stdin) | ✅ | ✅ | ✅ | `String?` — `null` no EOF (02/09; Native antes devolvia `""`) |
| kof.io `readText`/`size` | ✅ | ✅ | ✅ | `STR002` histórico fechado 02/09: Native `readText` devolve `null` (antes encerrava); `size()` sem sentinela `-1` |
| kof.time (`now()`, `sleep`, `interval`) | ✅ | ✅ | ✅ | `interval`/`every` JVM (ScheduledExecutor) + **Native (01/09, `time.interval` reusa o scheduler — SCHED001; mutação por referência validada; **cross 05/09** — alias `j kof_scheduler_every`/`cancel` no runtime riscv64/aarch64)** + **JS (TIME001 fechado 02/09; §132 18/09 — `time.sleep` agora é ponto de await: sleep cooperativo async devolvendo Promise, dirigido pela bomba do host `KofJsRunner` para que tasks spawnadas irmãs/filhas progridam enquanto uma task dorme)** — fila de interval disparada por essa mesma bomba do host (`__kofSleepStep`) no GraalJS, `setInterval`/`setTimeout` nativo em browser/Node. Calendário civil + data ISO (`isLeapYear`/`daysInMonth`/`dayOfWeek`/`daysBetween`/`addDays`/`diffDays`): **TIME002 fechado 11/09** — `addDays`/`diffDays` nos 5 targets (x86 `RuntimeTimeIso` + riscv64 fatia B35 + aarch64 traduzida; golden sob qemu) |
| `readLine`, `readFile`, `writeFile` | ✅ | ✅ | ✅ | |
| `kof.validation` (13 preds) | ✅ | ✅ | ✅ | `KofValidationTest` |
| `kof.math` (`clamp/abs/sign/min/max/isEven/...` Int + `sqrt/lerp/percentage/isInteger/isDecimal` Double) | ✅ | ✅ | ✅ | Escalares Int nos 4 alvos; **Double (S1b/S1b.1): JVM/Script/JS/x86 (SSE2 `sqrtsd`/`cvttsd2si`/`ucomisd`, bits crus via rax) + riscv64/aarch64 — MATH001 fechado 11/09** (fatia B36 `fsqrt.d`/`fmul.d`/`fdiv.d` + `fcvtzs`/`fcvt.d.l`; bits crus via a0..aN = modelo "rax cru" do x86; golden 20 vetores idêntico nos 5 targets sob qemu). `Double as Int` aarch64 corrigido junto (tradutor: `fcvt.w/l.{s,d}`→`fcvtzs`, era `scvtf` invertido — §120). **`roundTo(value, decimals)` (S1b.3, DECISIONS §3) FEITO 14/09 nos 5 alvos** — arredonda-meio-para-longe-do-zero por escala decimal determinística, sem libm (`p=10^\|d\|` por multiplicação repetida; `d<0` arredonda p/ dezenas/centenas); contrato aritmético `roundTo(2.675,2)==2.68`; matriz `stdmathround` + `KofMathTest.roundToCrossArch` sob qemu. `pow` = libm (x86 `pow@PLT`; riscv/aarch MATH001). Bug 94: `NaN==NaN` só no interpretador (semântica congelada). |
| `kof.supervisor` (núcleo OTP issue #83: `import kof.supervisor` → `supervisor(name).child(id,fabrica,politica).restartLimit(max).escalate(cb).start()/.stop(deadlineMs)/.stats()`) | ✅ | ✅ (x86) · 🔴 OTP001 riscv/aarch | ✅ 18/09 (§132) | Host puro-Kof injetado como pacote virtual (resource `dev/kof/supervisor-host.kf`, mecanismo idêntico ao `android-host`: só age no `import kof.supervisor` explícito — zero falso-positivo). Observa falha do worker por `try{await}catch` por filho (laço `vigiar` em thread dedicada), reinicia com **fábrica nova** (DD-OTP-06), respeita restartLimit (para + R6 sem escalate), stop cooperativo `cancel`+deadline, stats(started/restarts/dropped/vivos). Gate DD-OTP-11: worker falha 2x → termina na 3ª → restarts=2/fabrica=3. NATIVE x86 ✅ 15/09: §129 fechado (DECISIONS §2 opção B — `kof_exc_chain` agora é TLS por thread e o trampolim do spawn instala handler por worker; `await`/`selectAny` relançam a causa). riscv/aarch seguem OTP001 (clone cru, sem TLS). JS ✅ 18/09 §132 resolvido: `time.sleep` agora é ponto de await (sleep cooperativo async — o compilador colore async o método que o alcança via `computeAsyncColoring`, `kofTimeSleep` devolve Promise, e a bomba do host `KofJsRunner` a drena), então uma task spawnada de dentro de outra roda enquanto o poller dorme — o worker dispara e `OTP002` é levantado. `KofSupervisorE2ETest` 16/16 (JVM+Script+Native x86+JS rodam, limite, gate cross OTP001, sem-import não injeta). Impeditivo §130 corrigido junto (SEM024 falso em corpo de método re-analisado no mesmo escopo). |
| `kof.observability` (health/metrics/requestId + spans) | ✅ | ✅ | ✅ | `KofObservabilityTest` 12/12 (spans JSON x86_64/riscv64/aarch64 == golden JVM — §272 fechado 18/09); **`exportSpans()` (OTel OTLP/JSON) = `OBS003`: JVM/JS ✅, gap em compile-time no Native** |
| `kof.media` (`Image`/`Audio`/`Video`/`Mic` + `app.serveDir`) | ✅ `javax.imageio`/`javax.sound` (`KofMediaE2ETest`) | **MEDIA001** (handles) / **MEDIA003** (mic) / **WEB005** (`serveDir`) — gap em compile-time | **MEDIA001** / **MEDIA003** / **WEB005** — gap em compile-time | só JVM: alvos não-JVM diagnosticam em compile-time (R6), nunca stub silencioso; `MEDIA003` também cobre hardware ausente |
| `kof.gpu` (compute Vulkan: `available`/`failReason`/`dispatchMatmul`/matvec; namespace `gpu.*`) | ✅ FFM Vulkan (real; link-por-uso + `--enable-preview` só em programas GPU) | ✅ stubs asm (`available=false`, dispatch devolve o fallback CPU) | **GPU001** — gap em compile-time | semente FFI do plano universal (§GPU): JS não tem suporte (`KofGpu.supportedOn` falso → `GPU001`); stub Native sinaliza fallback, nunca resultado errado silencioso |
| `kof.http` client | ✅ | Native HTTP/1.1 asm (socket/recv/send parse; https→throw; DNS não-IP→127.0.0.1; retry knob no-op) | ✅ GraalJS síncrono (`Java HttpClient` interop); Node/browser = fetch real **assíncrono** (`spawn http.* + await` → Promise; §133 — a face síncrona em JS puro é registrada como HTTP004, não emitida como código) | `KofHttpE2ETest` 8/8 (JVM+JS+Native real resp; `jsNodeSpawnAwaitHttpResolvesBody` no node); retry/circuit paridade 30/08 |
| `kof.cache` (get/set/ttl/delete/clear) | ✅ | ✅ 30/08 (fix clobber `%rax`/`%rdi`) | ✅ | `KofCacheE2ETest` 5/5 x3 targets |
| `kof.mq` (pub/sub + queue) | ✅ | ✅ (01/09, pub/sub + filas in-process, asm; **05/09 port completo riscv64/aarch64** — queue por handle, pop via `kof_list_remove`, queue_size, unsubscribe por identidade, scan loops sem clobber) | ✅ | `KofMqE2ETest` 5/5 (JVM+Native+JS + **cross riscv64/aarch64 qemu** c/ paridade de output) |
| `kof.config` (tipado) | ✅ | ✅ (asm próprio) | ✅ 16/09 (CONF001 fechado) | precedência total Native (`KOF_CONFIG` > env > profile > `kof.config`); `NativeConfigE2ETest` 8 |
| `kof.log` | ✅ (JSON + correlation ID) | ✅ (asm; UTC, sem JSON) | ✅ 01/09 (console.* + nível) | `KofLogE2ETest` 11 (incl. JS) + `NativeLogE2ETest` 7. **Cross (riscv64/aarch64)**: `[LEVEL] msg` sem timestamp (x86_64 usa `UTC LEVEL msg`) — gap de formato não-determinístico, não falha silenciosa (nível+mensagem presentes) |
| `kof.process` (`process.run`/`process.spawn` — subprocesso com stdin/stdout vivos) | ✅ (ProcessBuilder) | **PROC001** — gap em compile-time | ✅ (host GraalJS) | JVM/JS rodam subprocessos reais; o Native recusa em compile-time (`process.run: not supported on the Native driver.target yet`, medido 17/09 — R6, nunca link quebrado silencioso) |
| `kof.security` (passwords/crypto/jwt/secrets + G9) | ✅ | ✅ x86_64 (asm) · **SECN002** chacha20 / **SECN006** cookies · **SECN000** riscv64/aarch64 (sem runtime `kof_sec_*`) | ✅ (JS puro; **SECN007** resourceServer é JVM-only) | PBKDF2/SHA512/JWT/AES-GCM (asm no Native x86, JS puro no KofJS — face **AES** do `SECN002` fechada 01/09). `KofSecurity.supportedOn` faz gate por função/target; medido 17/09: `crypto.encryptChacha20` no Native → `SECN002`, `security.cookieSet` no Native → `SECN006`, `crypto.sha256` em `native.risc` → `SECN000`. Ver as linhas de Documented Gaps |
| `auth.resourceServer` (OAuth2 resource server, D-SEC camada 16) | ✅ JVM (JWKS RS/ES, iss/aud/exp, sem confusão de algoritmo; `KofOAuthResourceServerTest` 4/4) | **SECN007** | **SECN007** | valida JWT de terceiro; integra com `auth.authenticated`/`app.security` |
| `web.app().security()` (D-SEC C18, middleware composto) | ✅ JVM (ordem fixa rate-limit→CORS→headers→cookies/session→csrf→auth→RBAC; `KofWebE2ETest` 25/25) | **WEB006** | **WEB006** | `app.security([opts])`; opts `headers/cors/rateLimit/csrf/auth/roles`; auth-if-present; aviso em produção |
| `kof.db`/`kof.orm` | ✅ | ✅ (SQLite `.so` direto; **`transaction {}` commit/rollback 01/09** (EH asm + BEGIN/COMMIT/ROLLBACK); **MySQL wire protocol** — handshake+scramble+auth-switch+COM_QUERY+resultset 31/08 + **prepared statements binários 03/09**)/ORM001 | ✅ não-tipado (16/09); `query<T>` = `DB002` FECHADO 18/09; ORM FECHADO 18/09 (`ORM001`) | **Query DSL `User.query(db){ where; orderBy; limit }` (nível 3) 01/09** — baixa p/ `db.query` (JVM E2E H2); MySQL native `nativeMysqlWireProtocol` + `nativeMysqlPreparedBinary` (binário q/ binds); `nativeTransaction{Commits,RollsBack}`. **riscv64/aarch64 SQLite FECHADO 15/09** (link-by-use `libsqlite3` via `NativeCrossLink.needsSqlite` + runtime `kof_db_*` fatias `RtB46/RtB47` — connect/execute/query/bind/close/transaction, `KofDbE2ETest` crossNativeSqliteRoundtrip sob qemu nas duas arches; ORM segue `ORM001`, MySQL só x86). **JS FECHADO 16/09 (DB001)**: `connect`/`connect2`/`close`/`execute`/`query`/`transaction` nao-tipados delegam a `kof_platform.db*` no host GraalJS (mesma JVM/classpath do caminho JDBC — `KofJsRunner`, ponte `KofJsDbBridge`; saida de `KofDbE2ETest.js{ExecuteAndQueryRowsAsJson,TransactionCommits,TransactionRollsBackOnFailure,NestedTransactionDoesNotCommitOuterScope}` byte-parity com JVM). **`db.query<T>` tipado = `DB002` compile-time** (JS nao emite bytecode JVM no classpath do host — `Class.forName` nao tem o que carregar; R6: alto, nunca silencioso). ORM no JS FECHADO 18/09 (`ORM001` — `KofJsOrmBridge`). |
| `web.app()` + TLS `listenSecure` | ✅ | server HTTP/1.1 (accept/route/lambda/body) — **TLS = WEB002 residual** | ✅ 03/09 (GraalJS `HttpServer` real — `bc577aa`) | `KofWebTlsTest` 7/7 (JVM) · `KofWebE2ETest` 25/25 · `KofWebNativeE2ETest` 4/4 |
| `web.app()` WebSocket `app.ws` + SSE `sse.*` | ✅ 30/08 (RFC 6455 + frame codec/máscara) | **WEB004** (ws) / **WEB003** (sse) — não portados p/ Native | **SSE ✅ 16/09 HANDLER-SCOPED** no host GraalJS (`app.sse` + `send/event/close/isOpen` + `sse()`, framing/headers idem JVM; push pós-return/multi-cliente = WEB003 residual, pump single-thread); **ws** = `WEB004` gate em compile 16/09 (antes `kofWebStub` 0 silencioso, R6); context-fns com runtime seguem ✅ | `KofWebWsE2ETest` 11/11 · `KofWebSseE2ETest` 7/7 · `KofWebJsE2ETest` sse 3 · `KofWsFrameTest` 7/7 · `KofWebHardeningTest` 6/6 |
| `status(code, body)` / `headerSet` | ✅ 27/08 | WEB001 (Native só suporta `body()` no contexto web; `status`/`headerSet` emitem WEB001 em compile-time — só `kof_web_body` em `contextNativeSupported`, 16/09) | ✅ 03/09 (JS HttpServer) | `KofWebE2ETest` 9/9 |
| UI Fase 7 Router (`go/replace/back/forward/param/current/depth`) | no-op | — | ✅ 31/08 | `RouterE2ETest` |
| `switch` pattern matching `case String s` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `switch` record destructuring `Point(x,y)` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `switch` como expressão `case X -> v` (SYN001) | ✅ | ✅ (x86_64/riscv64/aarch64) | ✅ (ternários aninhados) | 03/09 — `default` obrigatório ou exaustividade enum (`SEM032`); `KofSwitchExprE2ETest` 19 + riscv64/aarch64 14/14 |
| `String?` null safety básica | ✅ | ✅ | ✅ | 0.2.6-beta (`Type?`) |
| `List map/filter/reduce` | ✅ | ✅ | ✅ | 0.2.6-beta |
| `Box<T>` generic | ✅ | ✅ | ✅ | `substituteTypeVariable` |
| `KofScript` top-level `var`/`val` → `KofScriptGlobals` (execução direta via `KofInterpreter`) | ✅ | ✅ | ✅ | `KofScript` 0.3.0 |
| `KofCcompiler` (`kof c`) C subset | — | ✅ x86_64 native-only | — | `kof_c`, while/if/deref &/* |
| FFI C (`extern "<lib>"`, R3) | **Parcial** — qualquer assinatura sobre {Int, Long, Float, Double, Boolean, String} aridade livre/void/String-retorno + callbacks com argumentos primitivos E String, sincronicos/nao-escapantes (C2→C3.4); argumentos struct/array/ponteiro + callback-retorno-String/D6 → `FFI001` | **FFI001** — gap em compile-time (medido 18/09: `extern` qualquer em `--target native` recusa honestamente) | **Parcial** — o host runner liga a MESMA ABI escalar + callbacks byte-for-byte (`KofJsFfiBridge`/`upcallStub`); nao-escalar → `FFI002`; browser = degrade honesto de runtime (R7, sem host) | `FfiE2ETest` 16/16 JVM↔JS byte-for-byte; `JvmFfiCallbackE2ETest` 42/42/6.0/7.5; `KofJsFfiBridgeTest` 8/8 |
| `native.risc` (riscv64) / `native.arm` (aarch64) | — | **core completo (02-03/09)**: plumbing + codegen/runtimes asm puro + qemu — `NativeRiscv64E2ETest 13/13` + `NativeAarch64E2ETest 13/13` (core: classes/arrays/List/switch/try-catch/pattern/Strings/recursão) — paridade avançada pendente — `docs/development/native-multiarch.md` | — | target separation 0.2.0 |
| `kof fmt` (formatter parser real, idempotente) | ✅ 31/08 | ✅ | ✅ | `KofFormatter` (2c3e794) |
| **KofJS no browser real** (`kof.ui` renderizando DOM via ES Modules) | — | — | ✅ 01/09 (`KofJsBrowserE2ETest` — Chrome headless + HTTP + captura de DOM; pula se Chrome ausente) | ESM via HTTP local (módulos não carregam via `file://`); `KofJsRunner` serve `appDir` em `127.0.0.1` |
| Android (Fase 1: `kof build --target android` → projeto Maven + APK, host Activity em Kof) | ✅ (bytecode JVM) | — | — | gaps `AND00x` em compile-time **mais `DB001`/`SECN00x`/`GPU001`** (medido 17/09 — §278): o alvo com backend JVM ainda exclui `ANDROID` em vários checks de `supportedOn` |

## Gaps documentados (não mascarados)

| Gap | Diagnostic | Status |
|-----|-----------|--------|
| **App model (D-APP)** — matriz `APP001–003` | `APP001` (build backend-only com `web/` presente → frontend não compilado; honesto R6), `APP002` (**residual**: `[server] port` no `kof.toml` é parseado pelo `KofProjectConfig` (`serverPort()`) mas o `kof serve` nunca o consome — só `--port`/8080; os docs só descrevem o fallback sem manifesto), `APP003` (`kof new` — tipo/flag inválido, manifesto existente, dir parcial: recusa com diagnóstico, nunca sobrescreve) | ✅ `CmdNew` 14/09 (D-APP I1, DECISIONS.md §D-APP): `kof new <dir> [--type mono\|backend\|frontend\|full-stack]` — esqueletos nascem compiláveis (prova `CmdNewTest` 8/8, incl. compile JVM real dos esqueletos backend/full-stack); I3 `--fat` ✅ FEITO 14/09 (`kof build --fat` → `kof-app.jar` executável, `CmdBuildFatTest` 4/4, recusa honesta fora do JVM) |
| spawn/await no Native | ✅ 31/08 (CONC001 fechado — pthread_create + trampoline + pthread_join + allocator thread-safe futex; join implícito) | |
| spawn/await no JS | ✅ 03/09 (CONC003 fechado — async/await/Promise real; stmt + spawn-expr + await/poll/cancel/selectAny/channel bloqueante) | `cancelled()` sempre `0` (sem thread-local pra task atual); só task-lambdas podem ficar async (`CONC003-JS-01`) |
| web no Native/JS (server, TLS, ws/sse) | ✅ server base (03/09): Native accept/route/lambda/body (`NativeWebRuntime.java`) + JS GraalJS HttpServer (`bc577aa`). JS: SSE handler-scoped ✅ 16/09. Residual (por feature): TLS `WEB002`, ws `WEB004`, sse `WEB003` (Native; push pós-return/multi-cliente no JS), resto `WEB001` (path params/keepalive) | `WEB002`/`WEB004`/`WEB003`/`WEB001` |
| kof.http no Native | ✅ HTTP/1.1 asm (`NativeHttpRuntime.java`, 03/09) | https + DNS real ficam como gaps; **`timeout`/`retry`/`circuit` são REAIS nos 4 alvos nativos desde 17/09** (§259 FECHADO: connect não-bloqueante + deadline `poll` + `SO_RCVTIMEO`/`SO_SNDTIMEO` → `throw "kof.http: timeout"`; retry em exceção/`>=500`; circuito fail-fast 30s half-open — riscv64 portado, aarch64 herda; `KofHttpNativeResilienceCrossTest` 4/4 sob qemu, mensagens idênticas ao x86/JVM). `HTTP002` é o único código de gap HTTP *definido* — **não é emitido hoje**: `KofHttp.supportedOn` sempre devolve `true`, então o ramo `HTTP002` em `ExpressionHttpCallLowerer` está morto e `KofHttp.gapCode()` não tem chamadores (§259). Reservado para quando um alvo HTTP nativo ficar de fato sem suporte |
| kof.db/orm no JS | **`kof.db` nao-tipado FECHADO 16/09** (ponte no host GraalJS) — `db.query<T>` tipado FECHADO 18/09 (`DB002` — bind no guest via `__kof_decode_<T>`); **`kof.orm` FECHADO 18/09** (`ORM001` — `KofJsOrmRuntime`/`KofJsOrmBridge`, mesmo SQL de `JvmOrmRuntime`, E2E byte-paridade) | feito |
| `kof.media` (Image/Audio/Video/Mic + `app.serveDir`) no Native/JS | `MEDIA001` (handles), `MEDIA003` (mic), `WEB005` (`serveDir`) | só JVM: handles de mídia emitem o gap em `ExpressionUiMediaCallLowerer` (`MEDIA001`/`MEDIA003`); `app.serveDir` emite `WEB005` no gate web (`KofWeb.gapCode` + `ExpressionBuiltinInstanceCalls.lowerWeb`) — compile-time, R6 honesto, nunca stub silencioso. Medido 17/09: antes do fix o catch-all do gate web emitia `WEB001` enquanto as docs prometiam `WEB005` (código fantasma; o único mapeamento vivia no `KofMedia.appServeDir` morto). `KofMediaE2ETest` cobre `MEDIA003` (hardware ausente) + `WEB005` no Native/JS |
| `kof.gpu` no JS | `GPU001` | JS não tem suporte: `KofGpu.supportedOn` é falso para JS e `ExpressionMethodCallLowerer` emite `GPU001` em compile-time (medido 18/09: `gpu.available()` com `--target js` → `gpu.available: not available on the JS driver.target yet (GPU001)`); os stubs asm do Native são honestos (`available=false` + fallback) |
| `kof.observability.exportSpans()` (OTel OTLP/JSON) no Native | `OBS003` | JVM-first (R7): `KofObservability.supportedOn` é falso para os targets Native e `ExpressionMethodCallLowerer` emite `OBS003` em compile-time (medido: `--target native` → `observability.exportSpans: not available on the NATIVE driver.target yet (OBS003)`); o payload JVM/JS é OTLP/JSON `resourceSpans` (golden medido no oráculo JVM + GraalJS) |
| `kof.security` em riscv64/aarch64 | `SECN000` | O runtime riscv64/aarch64 em asm puro **não tem** nenhuma primitiva `kof_sec_*`: `KofSecurity.supportedOn` devolve falso para toda função nesses targets (medido 17/09: `crypto.sha256("x")` com `--target native.risc` → `crypto.sha256: not available on the NATIVE_RISCV64 driver.target yet (SECN000)`). Sem o gate a chamada quebrava no link com undefined reference (R6) |
| `kof.security` chacha20 no Native x86_64 | `SECN002` | `crypto.encryptChacha20`/`decryptChacha20` são JVM/JS apenas (`KofSecurity.supportedOn`); medido 17/09: `--target native` → `crypto.encryptChacha20: not available on the NATIVE driver.target yet (SECN002)`. A face AES-GCM do `SECN002` está fechada no Native (01/09) — esta é a face chacha20 do mesmo código |
| `kof.security` cookies no Native | `SECN006` | `security.cookieSet`/`cookieSetOpts`/`cookieGet` são JVM/JS apenas (D-SEC C11); medido 17/09: `--target native` → `security.cookieSet: not available on the NATIVE driver.target yet (SECN006)` |
| `kof.process` no Native | `PROC001` | `process.run`/`process.spawn` são JVM/JS apenas (`ExpressionProcessCallLowerer`); medido 17/09: `--target native` → `process.run: not supported on the Native driver.target yet (PROC001)`. JVM (`ProcessBuilder`) e JS (host GraalJS) rodam subprocessos reais (controle `echo hi`) |
| `extern` (FFI C) no Native; ABI nao-casavel no JVM/JS | `FFI001` / `FFI002` | O Native nao tem camada FFM nenhuma: todo `extern` recusa em compile-time com `FFI001` (medido 18/09: `extern abs(Int): Int` com `--target native` → `FFI binding not implemented on the NATIVE target yet (FFI001)`). O JVM casa o conjunto escalar; assinatura struct/array/ponteiro (D6) → `FFI001` no JVM, `FFI002` no host JS (medido 18/09). Nunca stub silencioso, nunca link-break (R6). |
| JSON Float/Double | ✅ 31/08 (JSN001 fechado — XMM + parser fração/expoente) | |
| FP→string no riscv64/aarch64 | ✅ 15/09 (FLT001 fechado — fatia `RtB45`) | era `FLT001`: o runtime asm puro não tinha libc (`%g`/`snprintf` ausentes). Agora o port cross do `RuntimeDtoa` (`kof_dtoa_format`/`kof_double_to_string`/`kof_float_to_string`) consome `snprintf`/`strtod` pelo **link dinâmico sob demanda** (`NativeCrossLink`: um programa que imprime FP liga `-lc`, o resto segue estático). Byte-idêntico ao JDK `Double.toString`/`Float.toString` no riscv64 **e** aarch64 (aarch herda via tradutor, que agora mapeia os apelidos ABI `fa0..fa7` → `d0..d7`); `println(3.14)`/`"v=" + 3.14`/`listOf(1.5)` deixam de diagnosticar. Prova `NativeRiscvDtoaTest` (tabela oracle JVM, 2 arches) + `Native{Riscv64,Aarch64}E2ETest#nativeValueOfDoubleFloatMatchesJvmGolden` |
| JSON objetos/records no Native | ✅ 31/08 (JSN002 fechado — composição em compile-time) | |
| GC mark-sweep no Native | ✅ | `kof_gc_sweep` real (flag bit1) + **auto-collect desligado** por exigir safe-points (`status.md` #1) — `KofGcE2ETest` 3/3 |
| MySQL nativo completo | ✅ | **wire protocol** + **prepared statements binário (03/09)** — `NativeDbPrepared.java`, `KofDbE2ETest` 12/12 |
| Native riscv64/aarch64 codegen | `NATIVE002` | **riscv64 core completo (02/09) + aarch64 core completo (03/09)**: `NativeRiscv64E2ETest 20/20` + `NativeAarch64E2ETest 20/20` — runtimes + codegen em **asm puro** (raw syscalls, **sem C**; `as`+`ld` estático com `--no-relax` (gp=0 sem C runtime); aarch64 via `translateRiscvToAarch64`) + qemu: println, var, if/else, aritmética, **classes (virtual dispatch/fields), arrays, List, switch, try/catch/throw, pattern matching (switch String s/instanceof/as), String methods (charAt/substring/contains/startsWith/endsWith/indexOf/concat/toInt + trim/toUpperCase/toLowerCase/replace char+String/lastIndexOf/equalsIgnoreCase/split), recursão, JSON (encode/decode record+listas), HTTP (get/post/put/patch/delete/options/status + headers — asm-generic socket=198/connect=203/write=64/read=63/close=57, mesma tabela nos 2), spawn/await (`clone(220)`+`futex` — qemu-riscv64 8.2.2 não tem clone3; heap compartilhado → `kof_alloc` bump atômico `amoadd.d`/`ldadd`)**. Paridade total x86 (DB/UI/net) pendente nos dois. **Ver `docs/development/native-multiarch.md`** |
| Campo estático no Native | ✅ CORRIGIDO 07/09 (bug 41, lane Native) | Histórico: `NativeBackend:629-630` era stub vazio p/ `KofGetStatic`/`KofPutStatic` (imprimia memória não-inicializada). Fix: `ClassLayout` exclui estáticos do layout de instância + `collectStaticFields`/`emitStaticData` emitem com offset real. Prova: `NativeE2ETest.nativeStaticFields` (verde no gate 12/09, 64/64) |
| `record.hashCode()` no JS/Native | ✅ CORRIGIDO (bug 42; JS `1ecfb3d` + Native `CompilerRecordSupport.buildRecordHashCodeMethod`) | Sintoma histórico: JS `TypeError: hashCode is not a function`; Native `ld: undefined reference to P_hashCode`. `hashCode` sintético de record (acumula `31*h + campo`) emitido nos 3; paridade `==`/`toString`/`hashCode` nos 4 targets. Prova viva: `ConformanceMatrixTest.recordhash` (célula SEM exclusão — roda JVM+Native+Script+JS; 11/11 verde 12/09). |
| `String.length`/`charAt` no Native (UTF-8 vs UTF-16) | ✅ 5 targets (bug 43; cf. STR001) | O Native contava bytes UTF-8 (`"café".length`=5, `charAt(3)`=195); JVM conta code units UTF-16 (4, 233). x86_64 corrigido 10/09 (`RuntimeStringOps`/`RuntimeStringSearch` + `.Lkof_substr_walk`); JS 10/09; riscv64/aarch64 11/09 (na MERGE: fatias `RtB34`/`RtB35` — `kof_string_length`/`char_at`/`substring` + `index_of`/`last_index_of` UTF-16, inclusive as variantes `-from` (`*_2`, §102 cross) e `starts_with2`). Prova: golden JVM==x86==riscv==aarch (`NativeRiscv64/Aarch64E2ETest`+matriz, `assumeToolchain`). **Contrato `substring(0,0)` FECHADO 11/09 (§111):** sentinela "até o fim" mudou de `0` p/ `-1` no call site x86 E cross — `"ab".substring(0,0)` = `""` (JDK) nos nativos. |
| `String.compareTo`/`hashCode`/`equals` no Native | ✅ 5 targets (bug 97, 10–11/09) | Declarados no reference + aceitos pelo typer, mas os nativos não os emitiam (`undefined reference java_lang_String_compareTo`/`_hashCode`). x86_64 10/09 (`RuntimeStringCompare`, code units UTF-16; `.equals` roteado p/ `kof_string_equals` c/ guard isString); JS 10/09 (`JsCallEmitter` + `kofStringCompareTo`/`kofHashCode`); riscv64/aarch64 11/09 na MERGE (fatia `RtB36`: `String_equals`/`String_compareTo`/`String_hashCode` — algoritmo de code-unit port do x86, via `.Lu5_next`; aarch herda do tradutor). **Resíduo: `Object.equals` (receiver `Object`) ainda link-fail — dispatch virtual no Native, gap aberto p/ lane Native.** |
| `println(double)` no Native | ✅ 4 targets (bug 44 10/09 + spelling inf/nan 11/09 + §180 shortest-round-trip 15/09 + **cross 15/09** — fatia `RtB45` via link dinâmico `snprintf`/`strtod`) | JDK `Double.toString`/`Float.toString` via `RuntimeDtoa` (`kof_dtoa_format`/`kof_double_to_string`/`kof_float_to_string`): loop limitado `%.*e`+`strtod` de shortest round-trip + reformatação Java (`1.0E7`, `0.001`), Float com a própria forma, `Infinity`/`-Infinity`/`NaN`. `RuntimePrintNum` (o `print` sem box), `RuntimeJsonEncode` (NaN/±Inf → `null`) e `RuntimeCollectionToString` delegam a ela; `RuntimeStringConv` mantém só int/char/long/bool. Prova `ConformanceMatrixTest.doubleprint` (Native incluído)+`infinityprint`+`floatprint` + `NativeRiscvDtoaTest` (oracle cross) |
| `println(if/switch-expr)` heterogêneo no Native | 🟡 PARCIAL 15/09 (fatia 1, `97d08e54`) | §205: o `Object`-join do if/switch heterogêneo não tinha dispatch de print no Native → SIGSEGV (exit 139). A fatia 1 rebaixa o print DIRETO ramo-a-ramo (cada ramo pelo seu tipo — a mesma regra que o JVM já aplica no box de ramo): células `ifexpr-heterogeneous-direct`/`-else`/`-multi`/`-multi-default` byte-idênticas JVM×Native. Residual (via-`var`/`as Object`) = o ABI boxed polimórfico (§104b-ii), fatia 2. O gate de diagnóstico R6 foi REJEITADO pela mantenedora — regra 5 (mesmo output em todo target) é lei. |
| `finally` c/ `return` no try | ✅ 4 targets (bug 45, DD-01 opção 4a, 13/09) | `FinallyFrame` na IR: `ReturnStmt` com frame ativo → store `#retVal` + jump `returnFinallyLabel`; epílogo roda o finally e retorna (encadeia try aninhado); JS reconstrói try/finally nativo + epílogo return-only. Antes: JVM/Native/interp descartavam o finally, JS devolvia `undefined`. Prova `CoreRegressionE2ETest.finallyReturnJvm`+`finallyReturnJs`; suíte 1627/0 |
| `json.decode<List<Record>>` | `JSN004 (Native)` — gap honesto, bug 48 (09/09) | Interpretador ✅ CORRIGIDO 07/09 (`KofInterpreterRuntime` intercepta `kof_json_decode_object_list`); JVM/JS ok. **Native:** não é mais link-fail silencioso — `ExpressionJsonCallLowerer` (ramo `isNative()`, :131-145) emite o **diagnóstico `JSN004`** ("List<Record>/List<Class> not supported on the Native target yet; use JVM/JS") em vez de chamar `kof_json_decode_object_list` inexistente (R6: gap diagnosticado, nunca stub mudo/link fail). `decode<Record>` no topo ok nos 4 (fix 07/09) |
| `try` aninhado no KofJS | ✅ CORRIGIDO 07/09 (bug 49) | JS `COMP002: try expected KofTryEnd` — `JsControlFlowParser` só processa finally com catch-all `Throwable` e consome o label done só se não for endLabel de try aninhado (`MethodCtx.isTryEndLabel`); JVM/Native/Script/JS rodam |
| `throw` dentro de `catch` (re-throw) no KofJS | ✅ CORRIGIDO 08/09 (bug 52) | JS `unexpected KofCatchStart` (corpo de catch termina em KofThrow) — resolvido como efeito colateral do fix do bug 45 (`c727fee`); 4 targets agora concordam (`ConformanceMatrixTest.catchrethrow` sem exclusão) |
| channel send/recv DENTRO de `spawn` | ✅ CORRIGIDO 09/09 (bug 50) | Histórico: Native SIGSEGV 139 — o `call usleep` no receive-vazio clobberava `%rsi` (=&lock), deixando o `futex` com uaddr errado (nunca setado p/ &lock). Fix: alinha rdi/rsi ao padrão do WAKE + restaura &lock em rsi p/ o próximo cmpxchg, nos 2 pontos. Prova: `KofConcurrency2Test.channelWithSpawnNative` (`v=42`, verde no gate 12/09; antes a suíte só tinha `channelNative` SEM spawn) |
| `app.delete(...)` → COMP002 frame crash (GitHub #29) | ✅ CORRIGIDO 07/09 (bug 54) | `KofIo.instanceMethod` `case "delete"` sem `argCount==0` colide com rota web → `KofPop` extra sobre `kof_web_route` (void) → underflow ASM `Frame.merge`. Fix: guarda `argCount == 0`. `get/post/put/patch/delete/options` compilam e rodam (`KofWebE2ETest.deleteRouteCompilesAndResponds`) |
| Handler web que termina em `return null` responde 404 (GitHub #28) | ✅ CORRIGIDO 07/09 (bug 53) | `ExpressionTyper` caso `LambdaExpr` só varria `ReturnStmt` top-level → `return` aninhado em `if` ignorado → `invoke()` void descartava valor. Fix: varredura recursiva (`firstReturnValueType`). `return null` continua 404 documentado (`KofWebE2ETest.handlerReturningNullAsLastPathStillRespondsValue`) |
| `spawn` concorrente → CME no interpretador (statics) | ✅ CORRIGIDO 07/09 (bug 55) | `staticFields`/`initialized` HashMap compartilhados entre virtual threads → CME no `computeIfAbsent` + corrida de `<clinit>` (perdedor via statics vazios). Fix: ConcurrentHashMap + lock por classe (semântica JVM: clinit uma vez, outros esperam). Probe 120/120 + `concurrentAwaitReturnsOwnTaskResult` |
| `String.split` + `parts.get`/`.size` → ClassFormatError `Illegal class name ""` (GitHub #30) | ✅ CORRIGIDO 07/09 (bug 56) | receiver `ArrayType` no fallback genérico → `KofCall`/`KofLoadField` com owner ArrayType → internalName vazio no constant pool. Fix: `.get(i)` → AALOAD, `.size/.length` → ARRAYLENGTH + typer (componente/Int). Frontend compartilhado, emit JVM. `CoreRegressionE2ETest.stringSplitArrayAccess` (JVM+JS) |
| `await` sobre handle `Object`/`Handle<T>` declarado → VerifyError/ClassNotFoundException (GitHub #31) | ✅ CORRIGIDO 07/09 (bug 57) | `Handle` não era mapeado (só `Channel`) → descriptor `LHandle;` inexistente; e `emitWideningIfNeeded` não unboxava valor apagado (Object/Unknown) p/ slot primitivo. Fix: `Handle<T>`→`CompletableFuture` (Type.of + JvmTypeMapper) + checkcast + unbox no widening. `CoreRegressionE2ETest.awaitOn{HandleThroughObject,TypedHandle}Param` (JVM) |
| `json.decode` de record com campo `List<Record>` → mapas crus / ClassCastException (GitHub #34) | ✅ CORRIGIDO 07/09 (bug 58) | campo emitido sem atributo Signature (genérico apagado) → `kof_json_bind` só via `Class<?>` (erasure) e devolvia a lista crua. Fix: `toGenericSignature` nos campos/record components + bind recursivo por `getGenericType`. `List<List<T>>` → JSN004 (gap honesto). `CoreRegressionE2ETest.jsonDecodeRecordWithListOfRecords` (JVM) |
| `serveDir` `GET /ui/` (barra final) → 404 (GitHub #35.2) | ✅ CORRIGIDO 07/09 (lane JVM web) | `kof_web_static_match` só resolvia index.html para `path.equals(prefix)` (sem barra); `/ui/` caía em rel="" → não-arquivo → 404. Fix: barra final (e raiz `/`) → `index.html`. `KofMediaE2ETest.serveDirTrailingSlashServesIndex` |
| `http.<verb>` com 2+ headers como args separados → crash/SEM025 (GitHub #32) | ✅ CORRIGIDO 07/09 (bug 60) | API só casava aridade 1-3 (headers = 1 String `\n`); 4+ args → null → SEM025 (crash já contido por `65e2dc0`). Fix: headers variádicos — `staticCall` aceita `>=2`/`>=3`, lowering mescla extras com `concat(acc,"\n",h)`. `KofHttpE2ETest.multipleHeadersAsVariadicArgs` (JVM; JS/Native usam o mesmo lowering) |
| REGRESSÃO Native riscv64/aarch64: `println` → `undefined reference kof_static_java_lang_System_out` (59 testes) | ✅ CORRIGIDO 09/09 (bug 59, lane Native) | Histórico: desde `62423bf` (fix bug 41), o `.s` riscv/aarch referenciava o símbolo mas não o definia no `.data` (só o x86 definia) → 59 link-fail. Fix: `emitRiscv`/`emitAarch64` passaram a chamar `collectStaticFields()`/`emitStaticData`. Prova: `NativeRiscv64E2ETest` + `NativeAarch64E2ETest` **39/39 + 39/39 verdes SEM skip** no gate 12/09 neste host (compila → binário existe → roda no qemu → rc=0) |
| `spawn`/`await` (determinístico, ordem por await) | OK nos 4 | `spawn fn(a)` + `await` + `spawn fn` ×2 + canal mesma-thread: saída idêntica nos 4 targets (travado em `ConformanceMatrixTest` lote 3) |

Fechados em 0.2.6-beta (01/09): **`kof.log` no JS (LOG001)** — console.* + `KOF_LOG_LEVEL`, bloco Vulkan no runtime JVM condicional ao uso de `kof.vk` (capability/link-por-uso, R2) + `--enable-preview` só para programas Vulkan (FFM preview API no JDK 21), `spawn` com lambda que captura variável externa (JVM+Native), `&&`/`||` booleanos com short-circuit no JS (bitwise intacto), `Channel<T>` como parâmetro de função (JVM/Native/JS), `println`/`print` antes de `spawn` e `spawn→await→spawn` no Native sem SIGSEGV (alinhamento de stack no `pthread_create`), KofJS `kof.ui` renderizando em browser real (Chrome headless E2E), LSP `references`+`rename`, tracing W3C `traceId`/`spanId` (3 targets), `moduleRoot` por LCA (P1-4), validação tipada de coluna no ORM (P3-10, `ORM003`), **AES-256-GCM no KofJS (`SECN002`)** — AES-GCM puro JS com paridade cross-target JVM↔JS e tamper detection por base64 estrito, **histogram/metrics no Native (`OBS002`)** — store asm + export Prometheus em paridade de conteúdo com o JVM, **`time.interval`/`time.cancel` no Native (`TIME001`)** — reusa o scheduler (SCHED001) via alias `jmp`, mutação por referência validada; **TIME001 fechado no JS (02/09)** — `kofTimeInterval`/`kofTimeCancel` agora comandam uma fila cooperativa `kofTimeJobs` bombeada dentro de `kofTimeSleep` (GraalJS não tem event loop; browser/Node caem no `setInterval` nativo) — `KofTimeE2ETest.jsIntervalRunsPeriodicallyUntilCancelled` (paridade ticks≥2 + freeze após cancel), **`transaction {}` no Native** — `kof_db_transaction` em asm: BEGIN/COMMIT/ROLLBACK via `kf_db_execute`, lambda via vtable (`rdi`=this p/ capturas) e ROLLBACK+re-throw no EH (`kf_throw_string` chega no handler com `%rdi` e a chain p/ o try externo), conexão default = última aberta — `nativeTransactionCommits`/`nativeTransactionRollsBackOnFailure`, **`kof.mq` no Native (`MQ001`)** — pub/sub + filas in-process em asm (store `.bss` + nodes 40B, invoke-com-arg, paridade de output com JVM/JS), **`Set<T>`/`Map<K,V>` como campo/retorno de classe no JVM** — mapper `Set`→`HashSet`/`Map`→`HashMap` + parse de método de classe c/ retorno genérico (`KofMapSetTest.setMapAsFieldAndReturn`, 3 targets), **source map V3 do KofJS** — mappings VLQ em nível de linha (função gerada → linha Kof; `KofJsSourceMapTest`).
Fechados em 0.2.6-beta (30-31/08): `spawn` Native (CONC001 — pthread), FP XMM (FLT001), JSON completo no Native (JSN001/JSN002/JSN003 — objetos/records + arrays incl. Double/Float), WebSocket/SSE JVM (RFC 6455), `kof.http` retry/circuit JVM+JS (30s window, fail-fast), `kof.cache` 3 targets (fix de clobber de registradores), SQLite nativo `.so` direto, UI Fase 7 Router (JS), `kof fmt` + `kof config gen`.
Fechados em 0.2.6-beta (27/08): pattern matching `switch case String s` + record `Point(x,y)` (JVM/Native/JS), `String?` null safety básica, `kof.http` no JS via `Java HttpClient`, `List map/filter/reduce`, large-project `import a.b.C` (`CompilerDriver.java:243`), `List.get`/`listOf`, free-list GC (`kof_free_head`), Windows SIGPIPE.
Fechados em 0.1.0: Map/Set nativo (era COL001), await com unboxing, captura em lambdas (BoxN), resultado de tarefa (`await`).

## Princípio

A semântica Kof é a mesma em todos os targets. Onde um target não suporta
uma feature ainda, o compilador emite um diagnostic explícito — nunca código
que funciona de forma diferente ou quebra silenciosamente.

---

## Convenção de gaps por domínio (visão universal)

Todo gap de capacidade tem um **código de diagnóstico** documentado aqui e
emitido em compile-time. Domínios novos seguem o mesmo padrão dos existentes
(`SECN000`, `WEB002`, `WEB005`, `CRON001`, ...):

| Prefixo | Domínio | Exemplos |
|---------|---------|----------|
| `HTTP`/`WEB`/`DB`/`ORM`/`MQ`/`SCHED`/`TIME`/`CONC`/`SECN`/`CRON`/`MEDIA`/`GPU`/`OBS`/`PROC`/`FFI` | Sistemas atuais | `WEB001`/`WEB002`/`WEB003`/`WEB004`/`WEB005`/`WEB006` (gate web em não-JVM), `SECN000` (todo `kof.sec` em riscv64/aarch64), `SECN002` (chacha20 no Native), `SECN006` (cookies no Native), `SECN007` (OAuth resource-server em não-JVM), `CRON001` (`at(cron)` no Native; JVM/JS real), `MEDIA001`/`MEDIA003` (handles de mídia/mic em não-JVM), `GPU001` (`kof.gpu` no JS), `OBS003` (`exportSpans` no Native), `PROC001` (`kof.process` no Native), `ORM001` (`kof.orm` no Native) |
| `AND` | Android | `AND002` (servidor web embutido no Android), `AND004` (android.jar ausente — warning); `AND001` fechado 31/08; `AND003` é caveat documentado (sem gate de compile-time) |
| `NATIVE` | codegen multiarch | `NATIVE002` (rótulo do trabalho riscv64/aarch64; não é diagnóstico emitido) |
| `INFRA` | infraestrutura / IaC | `INFRA00x` |
| `DATA` | data engineering / dataframe / ML | `DATA00x` |
| `SCI` | scientific computing / HPC | `SCI00x` |
| `BIO` | bioinformática | `BIO00x` |
| `SECPQ` | criptografia pós-quântica | `SECPQ` (gap de target, nunca stub) |

**Códigos reservados (definidos, não emitidos).** Um par
`gapCode`/`supportedOn` pode permanecer no fonte depois que a capacidade
landou, como andaime para um alvo futuro sem suporte. Esses literais existem,
mas **nenhum alvo os alcança hoje**, então não são gaps: `HTTP002`
(`KofHttp.supportedOn` sempre devolve `true`), `MQ001` (`KofMq.supportedOn`
sempre `true`), `SCHED001` (`KofScheduler` só gateia `at(cron)`, como
`CRON001`; `SCRIPT` aborta antes com `COMP003`), `CONC003` (o residual JS vivo
é `CONC003-JS-01`), `UUID001` (gate removido 10/09), `OBS001`, `SECN005`
(`rate_limit`/`session`/`api_key` têm suporte em todo alvo). Não os cite como
emitidos — o diagnóstico só aparece quando um alvo fica de fato sem suporte
(R6: nunca silencioso, e nunca fantasma também).

**Android é um alvo gateado de verdade (medido 17/09).** `--target android`
reusa o `JvmBackend`, mas vários checks de `supportedOn` excluem `ANDROID`,
então o compilador recusa honestamente em compile-time: `DB001` (`db.connect`),
`SECN000` / `SECN001` (`passwords.hash`) / `SECN002` (chacha) / `SECN003`
(`crypto.sha512`) / `SECN004` (`jwt.create`) / `SECN006` / `SECN007`
(`kof.security`), `GPU001` (`kof.gpu`) e `AND002`/`AND004`. Se a exclusão é
intencional (ART sem JDBC/JCA em runtime) ou over-gating é decisão da lane do
compilador (§278); os códigos ficam listados aqui para a matriz não mentir
sobre o que o Android aceita.

Regra (R6): gap de domínio sempre tem **código + entrada nesta matriz** —
nunca stub silencioso, nunca fallback fraco, nunca "paridade parcial" sem
diagnóstico.

## Tiers de estabilidade (R5)

Cada namespace/pacote carrega um tier:

- **`stable`** — garantia de compatibilidade; promove somente com DoD completo
  (3 targets ou gap diagnosticado, E2E por target, benchmark quando plausível,
  docs + `training/` sincronizadas, suíte verde).
- **`experimental`** — pode mudar; a camada de **pacotes oficiais** nasce
  `experimental`.

Domínios pesados (`ml`, `bio`, `hpc`, `infra-<cloud>`) são **pacotes
oficiais** (camada 4), **nunca** stdlib base (R1).